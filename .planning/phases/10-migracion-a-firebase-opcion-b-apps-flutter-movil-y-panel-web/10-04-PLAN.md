---
phase: 10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web
plan: "04"
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - app_cliente/lib/models/sesion_mesa.dart
  - app_cliente/lib/models/pedido.dart
  - app_cliente/lib/models/pedido_item.dart
  - app_cliente/lib/features/sesion_qr/sesion_provider.dart
  - app_cliente/lib/features/sesion_qr/scan_screen.dart
  - app_cliente/lib/features/pedidos/pedidos_provider.dart
  - app_cliente/lib/features/pedidos/carrito_controller.dart
  - app_cliente/lib/features/pedidos/pedido_estado_screen.dart
  - app_cliente/lib/features/pedidos/menu_mesa_screen.dart
  - app_cliente/lib/features/pagos/calificacion_sheet.dart
  - app_cliente/pubspec.yaml
  - app_cliente/lib/main.dart
  - app_cliente/lib/app.dart
autonomous: true
requirements: [MIGRA-01, MIGRA-05, MIGRA-06]

must_haves:
  truths:
    - "El cliente abre sesión en una mesa por su código QR (o input manual) y dos aperturas concurrentes sobre la misma mesa dejan SOLO UNA sesión activa"
    - "El cliente arma un pedido con snapshot de nombre/precio por item y lo envía solo si SU sesión de ESA mesa está activa; el total es int COP"
    - "El estado de sus pedidos se actualiza EN VIVO (onSnapshot) sin polling ni WebSockets propios"
    - "El cliente puede solicitar la cuenta (flag visible para el staff) y NO hay pantalla de pago (diferido)"
    - "Tras sesión cerrada y pedido servido, el cliente califica 1..5 estrellas y el promedio del restaurante se recalcula atómicamente"
    - "app_cliente queda SIN dio, web_socket_channel, flutter_dotenv, flutter_secure_storage, url_launcher, ni archivos legacy (api_client/token_provider/ws_client/env/token_pair/pago)"
  artifacts:
    - path: "app_cliente/lib/features/sesion_qr/sesion_provider.dart"
      provides: "abrirSesion tx sobre sesiones/{mesaId} (una activa por mesa) + stream del doc (banner vivo)"
      contains: "runTransaction"
    - path: "app_cliente/lib/features/pedidos/pedidos_provider.dart"
      provides: "crearPedido tx (sesión propia activa, snapshot items) + pedidosSesionProvider stream realtime"
      contains: "snapshots"
    - path: "app_cliente/lib/features/pagos/calificacion_sheet.dart"
      provides: "calificar tx: pedido servido + sesión cerrada + recompute califProm/califCount en restaurantes/{rid}"
    - path: "app_cliente/pubspec.yaml"
      provides: "deps solo Firebase + stack UI (sin capa HTTP propia)"
  key_links:
    - from: "app_cliente/lib/features/pedidos/pedidos_provider.dart"
      to: "pedidos where sesionId == mesaId"
      via: "snapshots() stream orderBy createdAt desc"
      pattern: "sesionId.*snapshots|snapshots.*sesionId"
    - from: "app_cliente/lib/features/sesion_qr/sesion_provider.dart"
      to: "sesiones/{mesaId}"
      via: "tx.get + tx.set determinista (UNA sesión por mesa)"
      pattern: "sesiones"
    - from: "app_cliente/lib/features/pagos/calificacion_sheet.dart"
      to: "restaurantes/{rid}"
      via: "tx.update califProm/califCount recomputados en la misma tx"
      pattern: "califProm"
---

<objective>
Completar el big-bang de app_cliente: sesión de mesa por QR (tx), pedidos con snapshot (tx + realtime), solicitud de cuenta, calificación post-cierre con agregado atómico, y el PURGE final de toda la capa legacy (dio/ws/token/env/pagos screen + sus tests y deps).

Purpose: Cierra MIGRA-01 (app 100% Firebase), MIGRA-05 (realtime nativo reemplaza Phase 7) y MIGRA-06 (sesión única + pedido con sesión válida). El purge deja el pubspec limpio — señal de big-bang completado antes de tocar el panel (locked: app primero).
Output: app_cliente completa y sin código muerto; suite verde; analyze 0.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-RESEARCH.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-02-SUMMARY.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-03-SUMMARY.md

@app_cliente/lib/features/sesion_qr/sesion_provider.dart
@app_cliente/lib/features/sesion_qr/scan_screen.dart
@app_cliente/lib/features/pedidos/pedidos_provider.dart
@app_cliente/lib/features/pedidos/carrito_controller.dart
@app_cliente/lib/features/pagos/calificacion_sheet.dart
@app_cliente/lib/app.dart
</context>

<interfaces>
<!-- De 10-02/10-03 (ya existen): firestoreProvider, authStateChangesProvider, state_machines.dart (validarTransicion), firebase_fakes.dart. -->

Doc shapes (fuente 10-RESEARCH.md):
sesiones/{mesaId}: restauranteId, mesaId, usuarioId, estado "activa|cerrada|expirada", cuentaSolicitada bool, cuentaPedidaAt?, inicioAt, cerradaAt?
pedidos/{autoId}: restauranteId, mesaId, sesionId(=mesaId), usuarioId, clienteNombre, estado "enviado|aceptado|en_preparacion|servido|rechazado|pagado", items [{productoId, nombre, precio int, cantidad int}], total int, createdAt, updatedAt
calificaciones/{pedidoId}: restauranteId, usuarioId, pedidoId, estrellas 1..5, comentario, createdAt
mesas/{codigoQR}: restauranteId, numero, capacidad, estado, updatedAt

Tx clave (research, patrón 2/3/4/5):
- abrirSesion(db, uid, codigoQR): tx.get(mesas/{codigoQR}) existe (si no → código inválido); tx.get(sesiones/{mesaId}) no existe O estado != 'activa' (si activa → 'Mesa ocupada'); validarTransicion('mesa', actual→'ocupada') acepta desde disponible|reservada; tx.set(sesiones/{mesaId}, {restauranteId, mesaId, usuarioId: uid, estado 'activa', cuentaSolicitada false, inicioAt serverTimestamp}) + tx.update(mesa, estado 'ocupada', updatedAt serverTimestamp).
- crearPedido(db, uid, mesaCodigo): tx.get(sesiones/{mesaId}) estado=='activa' AND usuarioId==uid (paridad anti-spoofing Phase 6); tx.get(mesas) para restauranteId; items armados ANTES con snapshot nombre/precio del doc de producto; total = Σ precio*cantidad (int); tx.set(pedidos.doc(), {estado 'enviado', ...}).
- solicitarCuenta: update(sesiones/{mesaId}, {cuentaSolicitada: true, cuentaPedidaAt: serverTimestamp}) — solo el dueño (rules).
- calificar(db, uid, pedidoId, estrellas, comentario): tx.get(pedidos/{id}) usuarioId==uid && estado=='servido'; tx.get(sesiones/{sesionId}) estado=='cerrada' (calificación tras cierre — locked); tx.set(calificaciones/{pedidoId}, {...}); tx.update(restaurantes/{rid}, {califProm: (califProm*califCount + e)/(califCount+1) redondeado a 2 dec, califCount: califCount+1}) — leídos en la MISMA tx (atómico).

Reglas que aplican (10-01): pedidos create isCliente + sesión activa propia + mesa consistente + items list 1..50 + total >= 0; sesiones update dueño solo {cuentaSolicitada, cuentaPedidaAt}; calificaciones create 1..5 + pedido servido propio + sesión cerrada; update/delete de pedidos y calificaciones: false.

QR: doc ID de mesa = código mostrado en el QR. Formato GRI-MESA-{rid}-{numero:03d} (ej. GRI-MESA-demo-001). Regex de validación del input manual: ^GRI-MESA-[a-z0-9-]+-\d{3}$

Purge LOCKED: DELETE lib/core/{api_client.dart, auth_storage.dart, token_provider.dart, token_provider.g.dart, ws_client.dart, env.dart}; lib/models/{token_pair.dart, token_pair.g.dart, token_pair.freezed.dart, pago.dart, pago.g.dart, pago.freezed.dart}; lib/features/pagos/{pago_screen.dart, pago_controller.dart, pago_controller.g.dart}; assets/.env; test/{ws_client_test.dart, pedidos_ws_test.dart}. pubspec REMOVE: dio, web_socket_channel, flutter_dotenv, flutter_secure_storage, url_launcher + bloque assets. main.dart: quitar dotenv.load. app.dart: quitar ruta /mesa/pago e import de pago_screen.

Entorno: $env:Path += ";C:\src\flutter\bin" (workdir app_cliente/). Codegen tras tocar @riverpod: dart run build_runner build --delete-conflicting-outputs.
</interfaces>

<threat_model>
- **Anti-spoofing sesión (P6 Phase 6):** el pedido exige sesión activa con usuarioId == uid del emisor (tx) y las rules lo re-fuerzan comparando contra sesiones/{sesionId}.usuarioId.
- **Una sesión por mesa:** doc ID determinista sesiones/{mesaId}; set sobre existente se evalúa como update en rules → el "re-abrir" queda bloqueado; contendientes concurrentes se serializan leyendo el mismo doc.
- **Snapshot de precios (gap estructural aceptado):** nombre/precio se congelan del doc de producto al armar el carrito; rules validan estructura (no aritmética de items). Total client-side = solo display/reportes hasta Functions (fase pagos).
- **Calificación acotada:** solo pedido servido propio + sesión cerrada; estrellas 1..5 int; doc ID = pedidoId → 1:1 sin duplicados; update/delete false.
- **Borrado seguro de deps:** el purge es el ÚLTIMO task (todo ya migrado) — si algo aún importa dio/api_client el analyze lo delata antes del commit.
- **pagos diferidos (locked):** NO se implementa checkout/pasarela; la ruta /mesa/pago desaparece; solicitar la cuenta SÍ existe.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sesión QR — models + abrirSesion tx + stream del banner</name>
  <files>app_cliente/lib/models/sesion_mesa.dart, app_cliente/lib/models/pedido.dart, app_cliente/lib/models/pedido_item.dart, app_cliente/lib/features/sesion_qr/sesion_provider.dart, app_cliente/lib/features/sesion_qr/scan_screen.dart, app_cliente/test/sesion_qr/scan_test.dart</files>
  <read_first>archivos listados en files (APIs actuales), bloque interfaces (tx abrirSesion + shapes), test/helpers/firebase_fakes.dart, 10-RESEARCH.md patrón 2</read_first>
  <behavior>
    - Test: abrir sesión sobre mesa disponible → crea sesiones/{mesaId} estado activa con usuarioId dueño y mesa pasa a ocupada
    - Test CONCURRENCIA (MIGRA-06): dos abrirSesion simultáneos misma mesa → 1 gana, el otro recibe error controlado 'Mesa ocupada'
    - Test: mesa en 'limpieza' → TransicionInvalidaException → mensaje controlado
    - Test: código inexistente (GRI-MESA-demo-999) → error 'Código de mesa inválido'
    - Test: sesión ya cerrada en esa mesa → se permite re-abrir (doc existente estado != activa)
    - Test: stream sesionProvider refleja cuentaSolicitada=true emitido por otro writer (fake)
  </behavior>
  <action>Tests primero (behavior arriba) con fakes. Models: sesion_mesa.dart fromDoc (estado, cuentaSolicitada, cuentaPedidaAt?, inicioAt, usuarioId, mesaId, restauranteId); pedido.dart fromDoc (items List via (doc['items'] as List).map a PedidoItem, total int, estado, clienteNombre, sesionId); pedido_item.dart fromDoc/map (productoId, nombre, precio int, cantidad int) — ids String. sesion_provider: abrirSesion(db, uid, codigoQR) con la tx del bloque interfaces (mensajes exactos 'Código de mesa inválido' / 'Mesa ocupada'); sesionProvider(mesaId) → StreamProvider del doc sesiones/{mesaId} via docSnapshots (banner vivo + detección de cierre → snack/logout de mesa igual que hoy hacía el WS). scan_screen: input manual primera clase con regex ^GRI-MESA-[a-z0-9-]+-\d{3}$ y hint 'GRI-MESA-demo-001' (el escáner móvil_scanner se mantiene — decodifica el mismo string); al validar el código navega igual que hoy. Verde total.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/sesion_qr/ (workdir app_cliente)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Los 6 bloques del behavior verdes (incluida concurrencia doble apertura)</ac>
    <ac type="source">abrirSesion usa runTransaction con tx.get de sesiones/{mesaId} y mesas/{codigo}; ningún autoId para sesiones</ac>
    <ac type="source">scan_screen valida el nuevo formato de QR (regex con slug de restaurante)</ac>
  </acceptance_criteria>
  <done>Sesión de mesa con garantía de unicidad bajo concurrencia + banner en vivo por doc stream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pedidos (carrito snapshot + tx) + cuenta + calificación con agregado</name>
  <files>app_cliente/lib/features/pedidos/pedidos_provider.dart, app_cliente/lib/features/pedidos/carrito_controller.dart, app_cliente/lib/features/pedidos/pedido_estado_screen.dart, app_cliente/lib/features/pedidos/menu_mesa_screen.dart, app_cliente/lib/features/pagos/calificacion_sheet.dart, app_cliente/test/pedidos/carrito_test.dart, app_cliente/test/pedidos/cuenta_test.dart, app_cliente/test/pedidos/estado_test.dart, app_cliente/test/pagos/calificacion_sheet_test.dart</files>
  <read_first>los 5 archivos fuente listados (APIs actuales y pantallas), los 4 tests actuales, bloque interfaces (tx crearPedido/calificar), research tabla onSnapshot→pantallas</read_first>
  <behavior>
    - Test: agregar al carrito congela nombre/precio del doc de producto (cambiar el doc después NO altera el carrito)
    - Test: crearPedido con sesión activa propia → doc pedidos estado enviado, items snapshot, total = Σ precio*cantidad int, sesionId == mesaId
    - Test: crearPedido con sesión inactiva o de otro usuario → error controlado, sin doc creado
    - Test: crearPedido con carrito vacío → error (rules piden items >= 1; UX lo anticipa)
    - Test: pedidosSesionProvider stream emite nuevo pedido y cambio de estado (servido) sin refetch (REALTIME MIGRA-05)
    - Test: solicitarCuenta → sesiones/{mesaId}.cuentaSolicitada == true con cuentaPedidaAt
    - Test: calificar pedido servido con sesión cerrada → crea calificaciones/{pedidoId} 1:1 y restaurante.califCount+1 con califProm recomputado (ej. 0,0→5; 5×1→ (5+3)/2=4)
    - Test: calificar pedido servido con sesión aún activa → error controlado (locked: calificación tras cierre)
    - Test: calificar pedido de otro usuario o estado enviado → error controlado
  </behavior>
  <action>Tests primero. carrito_controller: agregar(Producto) → item con snapshot {productoId, nombre, precio} (precio leído del model que viene del doc); cantidad +/-; total int. pedidos_provider: crearPedido(db, uid, mesaCodigo, items) con la tx del bloque interfaces; pedidosSesionProvider(mesaId) → StreamProvider = collection('pedidos').where('sesionId', isEqualTo: mesaId).orderBy('createdAt', descending: true).snapshots() mapeando fromDoc (REALTIME — elimina WS+polling 10s); solicitarCuenta(db, uid, mesaId) → update cuentaSolicitada true + cuentaPedidaAt serverTimestamp. pedido_estado_screen/menu_mesa_screen: rewire al stream (sin Timer de polling; el estado llega por snapshot). calificacion_sheet + su lógica: calificar(db, uid, pedidoId, estrellas 1..5, comentario) con la tx del bloque interfaces (recompute atómico leído en la misma tx); UI: estrellas custom actuales intactas. Verde total + flutter analyze 0.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/pedidos/ test/pagos/ (workdir app_cliente)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Los 9 bloques del behavior verdes (snapshot inmutable, tx sesión, realtime, cuenta, calificación con agregado y sus 2 negativos)</ac>
    <ac type="source">pedidosSesionProvider usa snapshots() (sin Stream.periodic/Timer); crearPedido pasa por runTransaction</ac>
    <ac type="source">calificar recompute califProm/califCount dentro de la misma tx que crea calificaciones/{pedidoId}</ac>
  </acceptance_criteria>
  <done>Core value (sentarse→pedir→seguir en vivo→cuenta→calificar) 100% Firestore con realtime nativo; MIGRA-05/06 cliente completos.</done>
</task>

<task type="auto">
  <name>Task 3: purge legacy de app_cliente — archivos, deps y rutas muertas</name>
  <files>app_cliente/pubspec.yaml, app_cliente/lib/main.dart, app_cliente/lib/app.dart</files>
  <read_first>bloque interfaces de este plan (lista EXACTA de purge), app_cliente/pubspec.yaml, app_cliente/lib/main.dart, app_cliente/lib/app.dart</read_first>
  <action>Ejecutar el purge LOCKED completo: DELETE lib/core/{api_client.dart, auth_storage.dart, token_provider.dart, token_provider.g.dart, ws_client.dart, env.dart}; DELETE lib/models/{token_pair.dart, token_pair.g.dart, token_pair.freezed.dart, pago.dart, pago.g.dart, pago.freezed.dart}; DELETE lib/features/pagos/{pago_screen.dart, pago_controller.dart, pago_controller.g.dart}; DELETE assets/.env; DELETE test/{ws_client_test.dart, pedidos_ws_test.dart}. pubspec.yaml: REMOVE dio, web_socket_channel, flutter_dotenv, flutter_secure_storage, url_launcher y el bloque flutter/assets completo. main.dart: quitar import y llamada dotenv.load (main queda: ensureInitialized + bootstrap + runApp). app.dart: quitar GoRoute /mesa/pago y el import de pago_screen (calificacion_sheet NO se toca — ya migrada). flutter pub get. Verificar cero referencias: rg "api_client|token_provider|ws_client|auth_storage|flutter_dotenv|url_launcher|token_pair" en lib/ y test/ → 0 resultados. flutter analyze 0 issues y flutter test completo VERDE (los tests que usaban mocks de dio/http ya fueron reescritos en 10-02/10-03/Tasks 1-2; si queda alguno referenciando legacy, reescribirlo con los fakes antes de commit).</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter analyze; flutter test; rg "api_client|token_provider|ws_client|auth_storage|dotenv|url_launcher|TokenPair" lib test → 0 matches (workdir app_cliente)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="source">Los archivos legacy y tests WS NO existen en disco (Test-Path negativo por cada uno)</ac>
    <ac type="source">pubspec sin dio/web_socket_channel/flutter_dotenv/flutter_secure_storage/url_launcher y sin assets/.env</ac>
    <ac type="cli">flutter analyze 0 issues; flutter test suite completa verde; flutter pub deps sin la capa HTTP</ac>
  </acceptance_criteria>
  <done>Big-bang app_cliente COMPLETO: app 100% Firebase, sin código muerto ni deps legacy (señal de arranque del panel — locked app primero).</done>
</task>

</tasks>

<verification>
- flutter analyze 0 + flutter test suite completa de app_cliente verde.
- rg de referencias legacy en lib/ y test/ → 0.
- Concurrencia: tests explícitos de doble apertura de sesión y (regresión 10-03) doble reserva del slot siguen verdes.
</verification>

<success_criteria>
- MIGRA-01 completo: app_cliente opera 100% sobre Firebase (auth + datos + realtime) sin backend propio.
- MIGRA-05: estado de pedidos y sesión en vivo por onSnapshot (Phase 7 WS retirada del cliente).
- MIGRA-06 completo: sesión única por mesa + pedido ligado a sesión propia activa, ambos por runTransaction.
- Pagos diferidos respetados: solicitar cuenta sí, checkout no.
</success_criteria>

<output>
After completion, create `.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-04-SUMMARY.md`
</output>
