---
phase: 10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web
plan: "07"
type: execute
wave: 6
depends_on: ["10-01", "10-04", "10-06"]
files_modified:
  - docs/SMOKE-E2E.md
autonomous: false
requirements: [MIGRA-03, MIGRA-04, MIGRA-05, MIGRA-06]
user_setup:
  - service: Firebase CLI (login)
    why: "Deploy de rules+indexes al proyecto real p-gri-b5b40 — OAuth interactivo que solo el usuario puede completar"
    dashboard_config:
      - task: "firebase login (una vez) y luego el deploy lo puede correr Claude/terminal"
        location: "Terminal: npx --prefix scripts firebase login (abre navegador)"
  - service: Java JRE 11+
    why: "Firebase Emulator Suite (auth+firestore) para el smoke e2e local"
  - service: Service Account (solo seed proyecto real)
    why: "Opcional: sembrar el proyecto real (el smoke de fase se hace contra emuladores)"
    env_vars:
      - name: GOOGLE_APPLICATION_CREDENTIALS
        source: "Firebase Console → Project Settings → Service Accounts → Generate new private key (scripts/serviceAccountKey.json, GITIGNORED)"

must_haves:
  truths:
    - "Ambas apps pasan suite completa + analyze 0 contra la base Firebase migrada"
    - "El seed corre dos veces contra emuladores dejando estado idéntico (idempotencia demostrada en vivo)"
    - "El flujo e2e completo funciona contra emuladores: login cliente → reserva → ocupar → QR → pedido → cocina avanza → servido → cuenta → cerrar mesa → calificar → promedio visible"
    - "El usuario verifica el flujo en vivo y despliega rules+indexes al proyecto real (gate oficial de la fase)"
  artifacts:
    - path: "docs/SMOKE-E2E.md"
      provides: "Runbook paso a paso del flujo e2e con URLs, credenciales y resultados esperados por paso"
    - path: "emulator_data/"
      provides: "Estado exportado del emulador (gitignored) reproducible con --import/--export-on-exit"
  key_links:
    - from: "docs/SMOKE-E2E.md"
      to: "docs/FIREBASE_SETUP.md + scripts/seed_firebase.mjs"
      via: "pasos numerados que referencian los comandos exactos"
      pattern: "seed_firebase|emulators:start|USE_EMULATORS"
---

<objective>
Verificación final de la fase: suites completas de ambas apps, idempotencia del seed en vivo contra emuladores, runbook del smoke e2e end-to-end y el checkpoint humano con el deploy de rules+indexes al proyecto real.

Purpose: Gate de cierre de la migración (MIGRA-03/04 se sellan aquí): lo automatizable corre automated; el flujo en vivo y el deploy (OAuth del usuario) son verificación humana asistida con pasos exactos.
Output: docs/SMOKE-E2E.md + fase verificada (o issues listados para gap closure).
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-RESEARCH.md (sección Validation Architecture — phase gate)
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-01-SUMMARY.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-04-SUMMARY.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-06-SUMMARY.md

@docs/FIREBASE_SETUP.md
@scripts/seed_firebase.mjs
</context>

<interfaces>
<!-- Credenciales demo (seed 10-01): carlos@demo.gri.dev / maria@demo.gri.dev (clientes), admin@demo.gri.dev (admin demo), mesero@demo.gri.dev, cocina@demo.gri.dev, admin@gri.dev (super) — password Demo!1234 para todos. -->
<!-- Mesas: GRI-MESA-demo-001..008. Menú: 16 productos (Bandeja Paisa 32000, Limonada de Coco 9000, etc.). -->
<!-- Comandos base (docs/FIREBASE_SETUP.md): emuladores con --import/--export-on-exit=./emulator_data; seed con FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 + FIRESTORE_EMULATOR_HOST=127.0.0.1:8080; apps con --dart-define=USE_EMULATORS=true. -->
<!-- Entorno: $env:Path += ";C:\src\flutter\bin" antes de comandos flutter. -->
</interfaces>

<threat_model>
- **Smoke SIEMPRE con USE_EMULATORS=true:** correr las apps sin el flag durante el smoke tocaría el proyecto real (datos demo en prod + usuarios Auth reales basura). Verificar el flag en la línea de comando antes de cada flutter run.
- **Deploy de rules es destructivo:** rules mal desplegadas cortan el acceso de ambas apps — deploy SOLO desde firestore.rules ya validado (10-01) y tras el smoke verde.
- **serviceAccountKey jamás al repo:** el seed del proyecto real la requiere; confirmar git status limpio de secrets tras el checkpoint.
- **Datos del emulador son volátiles:** usar --import/--export-on-exit para reproducibilidad; documentar dónde queda emulator_data/ (gitignored).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: gates automatizados — suites + analyze + idempotencia del seed + runbook SMOKE-E2E</name>
  <files>docs/SMOKE-E2E.md</files>
  <read_first>docs/FIREBASE_SETUP.md, scripts/seed_firebase.mjs, 10-RESEARCH.md (Validation Architecture), SUMMARYs de 10-01/10-04/10-06</read_first>
  <action>(1) Gates: flutter analyze + flutter test COMPLETOS en app_cliente/ y panel_admin/ (0 issues, todo verde). (2) Idempotencia en vivo: npm ci --prefix scripts; si java -version funciona → $env:FIREBASE_AUTH_EMULATOR_HOST="127.0.0.1:9099"; $env:FIRESTORE_EMULATOR_HOST="127.0.0.1:8080"; levantar emuladores con npx --prefix scripts firebase emulators:exec --only auth,firestore "node scripts/seed_firebase.mjs && node scripts/seed_firebase.mjs" y verificar en el output que la segunda corrida no crea duplicados (categorías/productos count estable); guardar un --export-on-exit ./emulator_data para el checkpoint. Si Java NO está disponible, registrar la limitación en SMOKE-E2E.md (la idempotencia queda validada por código 10-01 + este paso queda pendiente del checkpoint humano). (3) Escribir docs/SMOKE-E2E.md: runbook numerado del flujo e2e completo contra emuladores con comandos PowerShell exactos y resultado esperado por paso — preparar emuladores+seed; levantar panel (flutter run -d chrome --dart-define=USE_EMULATORS=true, workdir panel_admin) y app cliente (ídem app_cliente); [A] login cliente carlos@demo.gri.dev; [B] reservar 2 personas para hoy+1 19:00 en Demo; [C] panel login admin@demo.gri.dev → dashboard/mapa en vivo + reservas del día; [D] panel: marcar mesa ocupada al llegar; [E] cliente: input manual GRI-MESA-demo-00X → menú; [F] cliente: pedir 2 items (ej. Bandeja Paisa + Limonada) → estado en vivo; [G] panel cocina: aceptar → en_preparacion → servido (mesero puede servir); [H] cliente: solicitar cuenta → panel cocina ve aviso; [I] panel: cerrar sesión/mesa → limpieza; [J] cliente: calificar 5★ + comentario; [K] cliente: discover muestra promedio actualizado; [L] realtime check: dos ventanas lado a lado, cambio visible sin refresh; [M] super-admin: login admin@gri.dev → Restaurantes → toggle; [N] DEPLOY REAL (gate oficial): npx --prefix scripts firebase login (OAuth usuario) → npx --prefix scripts firebase deploy --only firestore:rules,firestore:indexes → verificar output "rules released"/"indexes released"; [O] opcional seed real con GOOGLE_APPLICATION_CREDENTIALS; [P] git status sin serviceAccountKey.json ni emulator_data/. Cada paso con URL/creds/esperado.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter analyze; flutter test (ambas apps, workdirs app_cliente y panel_admin) y rg -c "^\[?[A-N]\]?\." docs/SMOKE-E2E.md >= 12 y rg "USE_EMULATORS" docs/SMOKE-E2E.md</automated>
  </verify>
  <acceptance_criteria>
    <ac type="cli">analyze 0 + suites completas verdes en AMBAS apps</ac>
    <ac type="behavior">Seed doble corrida contra emuladores sin duplicados (si Java disponible; si no, limitación registrada)</ac>
    <ac type="source">SMOKE-E2E.md con los pasos A-P numerados, comandos exactos y esperado por paso</ac>
  </acceptance_criteria>
  <done>Todo lo automatizable verde; runbook listo para la verificación humana.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — smoke e2e en emuladores + deploy de rules/indexes al proyecto real</name>
  <action>Verificación humana del flujo e2e completo siguiendo docs/SMOKE-E2E.md (pasos A-P): emuladores + seed, panel y app cliente con --dart-define=USE_EMULATORS=true, flujo reserva→ocupar→QR→pedido→cocina→servido→cuenta→cerrar→calificar→promedio, realtime en dos ventanas, super-admin toggle, y el deploy oficial de rules+indexes (firebase login + deploy --only firestore:rules,firestore:indexes). No hay código que escribir — este checkpoint sella la fase.</action>
  <what-built>Migración completa a Firebase (Opción B): app_cliente y panel_admin 100% sobre Firebase Auth + Firestore con security rules, realtime onSnapshot, transacciones de concurrencia (reservas/sesiones/pedidos), seed demo idempotente y guía de operación. Suites de ambas apps verdes.</what-built>
  <how-to-verify>
    Seguir docs/SMOKE-E2E.md paso a paso (resumen):
    1. Terminal 1: emuladores + seed (comandos del runbook — todo con USE_EMULATORS=true).
    2. Levantar panel y app cliente (flutter run -d chrome --dart-define=USE_EMULATORS=true en cada workdir).
    3. Ejecutar el flujo A-M del runbook: cliente reserva → panel ocupa mesa → cliente abre GRI-MESA-demo-00X → pide → cocina avanza hasta servido (en vivo, dos ventanas) → cliente pide cuenta (aviso en panel) → panel cierra mesa (limpieza) → cliente califica → promedio visible en discover → super-admin toggle restaurantes.
    4. Gate oficial de fase (N): firebase login + deploy --only firestore:rules,firestore:indexes al proyecto p-gri-b5b40 y verificar release en output (o reportar si se prefiere diferir el deploy — decisión del usuario).
    5. Confirmar que git status no muestra serviceAccountKey.json ni emulator_data/.
    Esperado general: cada paso del runbook con el resultado indicado; ningún permission-denied inesperado; cambios en vivo sin refresh.
  </how-to-verify>
  <resume-signal>Escribir "approved" si el flujo completo funciona (y deploy OK o diferido conscientemente), o listar los pasos que fallaron con lo observado</resume-signal>
</task>

</tasks>

<verification>
- Suites + analyze de ambas apps verdes (Task 1).
- Checkpoint humano: flujo e2e completo contra emuladores + deploy de rules/indexes al proyecto real.
</verification>

<success_criteria>
- MIGRA-03 sellado: rules desplegadas y verificadas en el proyecto real (o diferimiento explícito del usuario registrado en el SUMMARY).
- MIGRA-04 sellado: seed idempotente demostrado en vivo (emuladores) y ejecutable contra el real.
- MIGRA-05/06 observados en vivo por el usuario (realtime + concurrencia del flujo).
- Fase 10 completa: las dos apps operan sin backend propio.
</success_criteria>

<output>
After completion, create `.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-07-SUMMARY.md`
</output>
