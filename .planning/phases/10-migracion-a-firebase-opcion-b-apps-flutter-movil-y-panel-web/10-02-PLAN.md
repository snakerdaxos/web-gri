---
phase: 10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app_cliente/pubspec.yaml
  - app_cliente/lib/firebase_options.dart
  - app_cliente/lib/core/firebase_bootstrap.dart
  - app_cliente/lib/core/firebase_providers.dart
  - app_cliente/lib/core/state_machines.dart
  - app_cliente/lib/main.dart
  - app_cliente/lib/app.dart
  - app_cliente/lib/features/auth/auth_controller.dart
  - app_cliente/lib/features/perfil/perfil_controller.dart
  - app_cliente/test/state_machines_test.dart
  - app_cliente/test/helpers/firebase_fakes.dart
  - app_cliente/test/auth/login_register_test.dart
  - app_cliente/test/perfil/perfil_edit_test.dart
autonomous: true
requirements: [MIGRA-01]

must_haves:
  truths:
    - "La app cliente inicializa Firebase antes de runApp y solo toca emuladores con --dart-define=USE_EMULATORS=true"
    - "Un cliente puede registrarse (Auth) y el registro crea usuarios/{uid} con role 'cliente' y restauranteId null"
    - "Un cliente puede iniciar/cerrar sesión y la sesión persiste (persistencia nativa del SDK)"
    - "El cliente puede editar su nombre y cambiar su contraseña desde perfil"
    - "Las state machines Dart tienen EXACTAMENTE las mismas transiciones que state_machines.py"
  artifacts:
    - path: "app_cliente/lib/firebase_options.dart"
      provides: "Config Android+Web del proyecto p-gri-b5b40"
      contains: "DefaultFirebaseOptions"
    - path: "app_cliente/lib/core/firebase_bootstrap.dart"
      provides: "initApp + gate de emuladores secuencial (antes de runApp)"
      contains: "USE_EMULATORS"
    - path: "app_cliente/lib/core/state_machines.dart"
      provides: "Port 1:1 de las 5 máquinas (mesa, pedido, reserva, pago, sesion_mesa)"
      contains: "validarTransicion"
    - path: "app_cliente/lib/core/firebase_providers.dart"
      provides: "Providers de FirebaseAuth/FirebaseFirestore (override point de tests)"
    - path: "app_cliente/test/helpers/firebase_fakes.dart"
      provides: "FakeFirestore con seed mínimo + MockFirebaseAuth reutilizables"
  key_links:
    - from: "app_cliente/lib/main.dart"
      to: "app_cliente/lib/core/firebase_bootstrap.dart"
      via: "await bootstrap() antes de runApp"
      pattern: "bootstrap"
    - from: "app_cliente/lib/features/auth/auth_controller.dart"
      to: "usuarios/{uid}"
      via: "set al registrar (role cliente)"
      pattern: "collection\\('usuarios'\\)"
---

<objective>
Migrar app_cliente a la base Firebase: deps, firebase_options (con fallback manual), bootstrap con emuladores por flag, providers inyectables, port 1:1 de state machines con tests, y la migración completa de auth (login/registro/logout) y perfil.

Purpose: Primera mitad del big-bang de app_cliente (locked). Auth es transversal a todas las features; las features de datos (10-03/10-04) migran sobre estos cimientos.
Output: app compilando con Firebase Auth funcional (fakes en tests), state machines Dart verificadas, sin borrar aún el core legacy (dio/ws aún viven hasta 10-04).
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-RESEARCH.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-01-SUMMARY.md

@app_cliente/lib/main.dart
@app_cliente/lib/app.dart
@app_cliente/lib/features/auth/auth_controller.dart
@app_cliente/lib/features/perfil/perfil_controller.dart
@backend/app/core/state_machines.py
@documentos/google-services.json
@documentos/firebase-config-web.js
</context>

<interfaces>
<!-- Valores para el FALLBACK MANUAL de firebase_options.dart (fuentes: documentos/google-services.json y documentos/firebase-config-web.js — proyecto p-gri-b5b40) -->
Android: apiKey "AIzaSyBZe8QtDCsv3RTZc9ykoQ9wBJskboyOzwk", appId "1:703827387403:android:b55b9ee758dc5108e6d30e", messagingSenderId "703827387403", projectId "p-gri-b5b40", storageBucket "p-gri-b5b40.firebasestorage.app"
Web: apiKey "AIzaSyAXPPuBMkMUgt_piyLg6uvWiEY0ff4kiC4", appId "1:703827387403:web:08ae995e35ce9516e6d30e", authDomain "p-gri-b5b40.firebaseapp.com", messagingSenderId "703827387403", projectId "p-gri-b5b40", storageBucket "p-gri-b5b40.firebasestorage.app", measurementId "G-8H4SQ9ZHV5"
NOTA: con options vía Dart NO se requiere el plugin Gradle de google-services.

<!-- Port EXACTO de state_machines.py (mismas transiciones, mismos terminales) -->
mesa: disponible→[reservada,ocupada], reservada→[ocupada,disponible], ocupada→[limpieza], limpieza→[disponible]
pedido: borrador→[enviado], enviado→[aceptado,rechazado], aceptado→[en_preparacion], en_preparacion→[servido], servido→[pagado], rechazado→[] TERMINAL, pagado→[] TERMINAL
reserva: pendiente→[confirmada,cancelada], confirmada→[cancelada], cancelada→[] TERMINAL
pago: pendiente→[aprobado,rechazado], aprobado→[] TERMINAL, rechazado→[] TERMINAL
sesion_mesa: activa→[cerrada,expirada], cerrada→[] TERMINAL, expirada→[] TERMINAL
API Dart: class TransicionInvalidaException implements Exception { final String maquina, actual, nueva; } — void validarTransicion(String maquina, String actual, String nueva) — bool puedeTransicionar(String maquina, String actual, String nueva)

<!-- Deps LOCKED (pins exactos) -->
firebase_core: 4.13.0, firebase_auth: 6.5.7, cloud_firestore: 6.8.0; dev: fake_cloud_firestore: 4.2.0, firebase_auth_mocks: 0.15.2

Entorno: TODO comando flutter requiere antes $env:Path += ";C:\src\flutter\bin" (PowerShell, workdir app_cliente/).
Codegen riverpod: tras editar providers anotados con @riverpod correr dart run build_runner build --delete-conflicting-outputs.
</interfaces>

<threat_model>
- **Emuladores por flag:** useAuthEmulator/useFirestoreEmulator se llaman SOLO con bool.fromEnvironment('USE_EMULATORS', defaultValue: false) y ANTES de que cualquier provider resuelva .instance — si no, la app dev toca el proyecto real (Pitfall 2 del research).
- **Auto-registro no escala privilegios:** el registro SIEMPRE escribe usuarios/{uid} con role 'cliente' y restauranteId null (rules lo fuerzan); el doc espejo jamás autoriza.
- **Claims solo del seed:** la app nunca escribe claims; el login lee role/rid del idTokenResult solo para enrutado/gating de UI (la autorización real vive en rules).
- **Sin borrar deps aún:** dio/flutter_dotenv/etc. siguen en pubspec hasta 10-04 (Pitfall 10: borrar antes rompe N imports de features no migradas).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: deps Firebase + firebase_options + bootstrap + providers</name>
  <files>app_cliente/pubspec.yaml, app_cliente/lib/firebase_options.dart, app_cliente/lib/core/firebase_bootstrap.dart, app_cliente/lib/core/firebase_providers.dart, app_cliente/lib/main.dart</files>
  <read_first>app_cliente/pubspec.yaml, app_cliente/lib/main.dart, 10-RESEARCH.md (Code Examples > Bootstrap), docs/FIREBASE_SETUP.md (sección 7 fallback manual)</read_first>
  <action>pubspec.yaml: AGREGAR (no quitar nada) firebase_core 4.13.0, firebase_auth 6.5.7, cloud_firestore 6.8.0 en dependencies y fake_cloud_firestore 4.2.0, firebase_auth_mocks 0.15.2 en dev_dependencies (pins exactos del bloque interfaces). flutter pub get. firebase_options.dart: intentar primero dart pub global activate flutterfire_cli 1.4.1 y flutterfire configure (Android gri.app + web) — REQUIERE firebase login interactivo; si el CLI no está logueado/disponible, FALLBACK MANUAL: escribir lib/firebase_options.dart a mano con clase DefaultFirebaseOptions (static FirebaseOptions get currentPlatform) devolviendo androidOptions y webOptions con los valores EXACTOS del bloque interfaces y supportedPlatforms [android, web] (los tests nunca lo tocan; shape documentado en docs/FIREBASE_SETUP.md §7). core/firebase_bootstrap.dart: Future<void> bootstrap() que hace WidgetsFlutterBinding.ensureInitialized (no — eso va en main), Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform) y, SOLO si const bool.fromEnvironment('USE_EMULATORS', defaultValue: false), await FirebaseAuth.instance.useAuthEmulator('127.0.0.1', 9099) y FirebaseFirestore.instance.useFirestoreEmulator('127.0.0.1', 8080) — en ese ORDEN y antes de runApp. core/firebase_providers.dart: @riverpod firebaseAuthProvider → Provider<FirebaseAuth> retornando FirebaseAuth.instance; @riverpod firestoreProvider → Provider<FirebaseFirestore> retornando FirebaseFirestore.instance (override point de tests); @riverpod authStateChangesProvider → StreamProvider<User?> que observa firebaseAuthProvider.authStateChanges(). main.dart: WidgetsFlutterBinding.ensureInitialized + await bootstrap() + runApp — MANTENER dotenv.load por ahora (env.dart legacy aún lo usa; se elimina en 10-04). Correr build_runner. flutter analyze debe quedar en 0 issues.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter analyze (workdir app_cliente) y rg "USE_EMULATORS" lib/core/firebase_bootstrap.dart y rg "DefaultFirebaseOptions" lib/firebase_options.dart</automated>
  </verify>
  <acceptance_criteria>
    <ac type="cli">flutter pub get resuelve los 5 paquetes con las versiones exactas del bloque interfaces</ac>
    <ac type="source">firebase_options.dart contiene projectId p-gri-b5b40 y opciones android + web</ac>
    <ac type="source">El gate de emuladores es const bool.fromEnvironment y se evalúa antes de runApp; sin el flag no se llama useAuthEmulator/useFirestoreEmulator</ac>
    <ac type="cli">flutter analyze 0 issues; suite existente sigue verde</ac>
  </acceptance_criteria>
  <done>App inicializa Firebase (real por defecto, emuladores solo con flag), providers inyectables listos para features y tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: port 1:1 de state_machines.dart + tests porteados</name>
  <files>app_cliente/lib/core/state_machines.dart, app_cliente/test/state_machines_test.dart</files>
  <read_first>backend/app/core/state_machines.py, backend/tests (tests de state machines — buscar test*state*), bloque interfaces de este plan (tablas exactas)</read_first>
  <behavior>
    - Test: cada transición declarada valida (ej. validarTransicion('mesa','disponible','reservada') NO lanza; puedeTransicionar('pedido','enviado','aceptado') == true)
    - Test: transición NO declarada lanza TransicionInvalidaException con mensaje que contiene [maquina] (ej. validarTransicion('mesa','disponible','limpieza') lanza)
    - Test: terminales rechazan todo (rechazado, pagado, cancelada, cerrada, expirada → cualquier destino lanza)
    - Test: puedeTransicionar es la versión booleana consistente con validarTransicion
  </behavior>
  <action>Escribir PRIMERO test/state_machines_test.dart portando los casos de los tests Python de state machines del backend (buscar en backend/tests) más los 4 bloques del behavior. Correr flutter test test/state_machines_test.dart — DEBE fallar (rojo). Luego crear lib/core/state_machines.dart: Map<String, Set<String>> por máquina con las tablas EXACTAS del bloque interfaces (claves 'mesa','pedido','reserva','pago','sesion_mesa' en un Map<String, Map<String, Set<String>>> _all), TransicionInvalidaException con toString "[maquina] transición 'actual' → 'nueva' no permitida", validarTransicion y puedeTransicionar idénticos en semántica a Python. Verde. Módulo PURO: sin imports de Firestore/Auth.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/state_machines_test.dart (workdir app_cliente)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Suite state_machines_test verde cubriendo válidas, inválidas y terminales de las 5 máquinas</ac>
    <ac type="source">Las tablas Dart son idénticas a state_machines.py (diff manual línea a línea documentado en el commit)</ac>
  </acceptance_criteria>
  <done>state_machines.dart puro y verificado — base de las transacciones de 10-03/10-04 y de los botones del panel.</done>
</task>

<task type="auto">
  <name>Task 3: helpers de fakes + migración auth (login/registro/logout) y perfil</name>
  <files>app_cliente/test/helpers/firebase_fakes.dart, app_cliente/lib/features/auth/auth_controller.dart, app_cliente/lib/features/perfil/perfil_controller.dart, app_cliente/lib/app.dart, app_cliente/test/auth/login_register_test.dart, app_cliente/test/perfil/perfil_edit_test.dart</files>
  <read_first>app_cliente/lib/features/auth/auth_controller.dart (API actual que las screens consumen), app_cliente/lib/features/perfil/perfil_controller.dart, app_cliente/lib/app.dart (guard), app_cliente/test/auth/login_register_test.dart, app_cliente/test/perfil/perfil_edit_test.dart, 10-RESEARCH.md (Auth provider pattern)</read_first>
  <action>test/helpers/firebase_fakes.dart: (a) FakeFirebaseFirestore buildFakeFirestoreConSeed() que siembra restaurantes/demo + 2-3 mesas (GRI-MESA-demo-001..003) + 2 categorías + 4 productos (reutilizable por 10-03/10-04); (b) helper mockAuth({String? email, String uid = 'test-uid'}) con MockFirebaseAuth de firebase_auth_mocks firmado; (c) documentar el patrón de override: tests hacen overrides de firebaseAuthProvider/firestoreProvider — para claims/routing se overridea directo el provider de claims con valores, no se mockea el token. auth_controller.dart (reescrito, misma forma de consumo para las screens): login(email, pass) → signInWithEmailAndPassword mapeando FirebaseException a mensaje usuario (invalid-credential → 'Credenciales inválidas'; network → sin conexión); register(nombre, email, pass) → createUserWithEmailAndPassword + updateDisplayName(nombre) + set(collection('usuarios')/doc(uid), {nombre, email, role: 'cliente', restauranteId: null, createdAt: FieldValue.serverTimestamp()}) — NUNCA otro role; logout → signOut; authStateProvider → StreamProvider<User?> = authStateChangesProvider (reemplaza token_provider para el router); claimsProvider → FutureProvider<(String role, String? rid)> leyendo idTokenResult.claims (solo para UI; authz vive en rules). app.dart: rewire del guard y del refreshListenable al nuevo authState (misma lógica redirect: no logueado → /login; logueado en /login|/register → /inicio). Mantener la ruta /mesa/pago y su import hasta 10-04. perfil_controller.dart: cargar perfil desde FirebaseAuth.currentUser + doc usuarios/{uid} (get); actualizarNombre(nombre) → update usuarios/{uid} SOLO campo nombre (rules congelan el resto); cambiarPassword(actual, nueva) → reauthenticateWithCredential(EmailAuthProvider.credential(email, actual)) y luego updatePassword(nueva), mapeando wrong-password → 'Contraseña actual incorrecta' y requires-recent-login → re-auth ya hecho. Tests: reescribir login_register_test (login OK/credenciales inválidas/registro crea usuarios/{uid} role cliente en FakeFirestore + logout) y perfil_edit_test (actualizar nombre persiste un solo campo; cambiar password con actual incorrecta error) usando los helpers + overrides.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/auth test/perfil test/state_machines_test.dart y flutter analyze (workdir app_cliente)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">login/registro/logout verdes con MockFirebaseAuth; el test de registro asserts usuarios/{uid}.role == 'cliente' y restauranteId == null</ac>
    <ac type="test">perfil: update de nombre toca solo 'nombre' (assert de keys); password incorrecta → mensaje mapeado</ac>
    <ac type="source">auth_controller no importa dio/api_client/token_provider; usa firebaseAuthProvider vía ref</ac>
    <ac type="cli">flutter analyze 0 issues; resto de la suite sin regresiones (features no migradas intactas)</ac>
  </acceptance_criteria>
  <done>Auth y perfil 100% sobre Firebase con tests verdes; helpers de fakes reutilizables listos para 10-03/10-04.</done>
</task>

</tasks>

<verification>
- flutter analyze 0 issues y flutter test completo en app_cliente (legacy incluido) verde.
- Bootstrap secuencial verificado: sin USE_EMULATORS no hay llamadas a useAuthEmulator/useFirestoreEmulator.
- Registro escribe espejo usuarios/{uid} con role cliente; ningún código de app escribe claims.
</verification>

<success_criteria>
- MIGRA-01 (parcial): app_cliente arranca sobre Firebase, auth/registro/logout/persistencia y perfil funcionan con FirebaseAuth; state machines Dart idénticas a Python y testeadas.
- Fakes helpers + providers inyectables dejan la base para migrar datos en 10-03/10-04 sin tocar emuladores en CI.
</success_criteria>

<output>
After completion, create `.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-02-SUMMARY.md`
</output>
