---
phase: 10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web
plan: "06"
type: execute
wave: 5
depends_on: ["10-05"]
files_modified:
  - panel_admin/lib/features/mesas/mesas_crud.dart
  - panel_admin/lib/features/mesas/mesas_screen.dart
  - panel_admin/lib/features/mesas/mesa_form_dialog.dart
  - panel_admin/lib/features/mesas/qr_dialog.dart
  - panel_admin/lib/features/menu/menu_provider.dart
  - panel_admin/lib/features/clientes/clientes_provider.dart
  - panel_admin/lib/features/reservas/reservas_provider.dart
  - panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart
  - panel_admin/lib/features/reportes/reportes_provider.dart
  - panel_admin/pubspec.yaml
  - panel_admin/lib/main.dart
  - panel_admin/lib/app.dart
  - panel_admin/test/mesas/mesas_screen_test.dart
  - panel_admin/test/mesas/qr_dialog_test.dart
  - panel_admin/test/menu/menu_screen_test.dart
  - panel_admin/test/clientes/clientes_screen_test.dart
  - panel_admin/test/reservas/reservas_screen_test.dart
  - panel_admin/test/reportes/reportes_screen_test.dart
  - panel_admin/test/configuracion/restaurantes_test.dart
autonomous: true
requirements: [MIGRA-02, MIGRA-05]

must_haves:
  truths:
    - "El admin crea/edita/elimina mesas y el QR se genera en el panel a partir del código determinista GRI-MESA-{rid}-{numero:03d}; crear una mesa con número existente falla (doc ID determinista)"
    - "El admin gestiona categorías y productos (crear/editar, activo/disponible) y los cambios se reflejan en vivo en el menú y en el discover del cliente"
    - "El admin ve los clientes del restaurante con su historial de pedidos (derivado de pedidos, sin leer usuarios/)"
    - "El admin ve las reservas del día en vivo y puede marcar la mesa ocupada al llegar el cliente (transición válida)"
    - "El super-admin ve la lista de restaurantes y los desactiva (toggle activo) — un restaurante inactivo desaparece del discover"
    - "El admin ve reportes por rango de fechas (total, número de pedidos, top platos) computados desde pedidos servidos"
    - "panel_admin queda SIN dio, web_socket_channel, flutter_dotenv, flutter_secure_storage ni archivos legacy (api_client/token_provider/ws_client/env/token_pair)"
  artifacts:
    - path: "panel_admin/lib/features/mesas/mesas_crud.dart"
      provides: "crearMesa con doc ID GRI-MESA-{rid}-{numero:03d} + update/delete + QR por qr_flutter"
      contains: "GRI-MESA-"
    - path: "panel_admin/lib/features/menu/menu_provider.dart"
      provides: "CRUD categorías/productos con activo/disponible sobre Firestore"
    - path: "panel_admin/lib/features/reportes/reportes_provider.dart"
      provides: "Reportes por rango: fold en cliente de pedidos servidos (total, count, top platos)"
    - path: "panel_admin/pubspec.yaml"
      provides: "Deps solo Firebase + stack UI panel"
  key_links:
    - from: "panel_admin/lib/features/mesas/mesas_crud.dart"
      to: "mesas/{GRI-MESA-rid-num}"
      via: "doc determinista — colisión = mesa ya existe"
      pattern: "GRI-MESA-"
    - from: "panel_admin/lib/features/clientes/clientes_provider.dart"
      to: "pedidos where restauranteId == rid"
      via: "fold distinct usuarioId + clienteNombre (sin leer usuarios/)"
      pattern: "pedidos"
    - from: "panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart"
      to: "restaurantes/{slug}"
      via: "update hasOnly ['activo'] (super, rules)"
      pattern: "activo"
---

<objective>
Big-bang del panel_admin (mitad 2 y final): gestión completa (mesas CRUD + QR, menú CRUD, clientes + historial, reservas del día, super-admin restaurantes, reportes) y el PURGE final del core legacy del panel.

Purpose: Cierra MIGRA-02. Con esto ambas apps quedan 100% Firebase y el backend queda formalmente archivado (ningún import vivo).
Output: Panel completo sin código muerto; suite completa verde; analyze 0.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-RESEARCH.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-05-SUMMARY.md

@panel_admin/lib/features/mesas/mesas_screen.dart
@panel_admin/lib/features/mesas/qr_dialog.dart
@panel_admin/lib/features/menu/menu_provider.dart
@panel_admin/lib/features/clientes/clientes_provider.dart
@panel_admin/lib/features/reservas/reservas_provider.dart
@panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart
@panel_admin/lib/features/reportes/reportes_screen.dart
</context>

<interfaces>
<!-- De 10-05 (ya existen): firestoreProvider, claimsProvider (role, rid), restauranteActivoProvider, mesasProvider stream + cambiarEstadoMesa, pedidosStaffProvider + avanzarPedido, state_machines, firebase_fakes. -->

Reglas que aplican (10-01) — el CRUD debe encajar en ellas:
- mesas create/delete: menuStaffOf (admin_restaurante del rid o super); update completo (numero/capacidad) también menuStaffOf; cambio de estado staffOf + soloEstado + transMesa
- categorias/productos write: menuStaffOf; productos read público exige activo==true && disponible==true
- restaurantes update super: diff hasOnly(['activo'])
- reservas update: staff puede cancelar (no-show); estado mesa solo por transición

QR determinista (locked): código = doc ID = GRI-MESA-{rid}-{numero:03d} (rid slug, ej. GRI-MESA-demo-012). El panel genera el QR con qr_flutter (contenido = el código string); YA NO hay endpoint /m/{codigo}.

Clientes (diseño): los clientes del restaurante se derivan de pedidos (rules no permiten a staff leer usuarios/ ajenos): query pedidos where restauranteId==rid orderBy createdAt DESC → fold distinct por usuarioId usando clienteNombre denormalizado; historial de un cliente = pedidos where restauranteId==rid + usuarioId==X. v1 demo: get + fold en cliente (documentar coste; agregaciones formales = fase futura).

Reportes (diseño, decisión planner — fold en cliente): pedidos where restauranteId==rid where estado in ['servido'] where createdAt >= from && createdAt <= to → get() y fold: total (Σ total int), número de pedidos, top platos (map nombre→(cantidad Σ, venta Σ) desde items snapshot). Índice 10-01 (restauranteId+estado+createdAt) cubre la query. NOTA documentada: validación fuerte de montos llega con Functions (fase pagos).

Reservas staff del día: mismo stream que stats (reservas where restauranteId==rid fecha >= inicioHoy < mañana); acción "marcar ocupada" = cambiarEstadoMesa(reservada→ocupada) ya implementada en 10-05 — aquí solo se expone en la pantalla de reservas (y opcionalmente cancelar no-show → estado cancelada, staff lo permite rules).

Models panel a migrar con fromDoc (ids String): mesa.dart (hecho en 10-05), categoria_staff.dart, producto_staff.dart (precio int, disponible, activo), pedido_staff.dart (hecho 10-05), reserva.dart (fecha Timestamp, estado, mesaNumero, numPersonas, usuarioId), cliente_resumen.dart (desde fold de pedidos: usuarioId, clienteNombre, nPedidos, totalConsumo, ultimoPedido), reporte.dart (fromFold interno — rellenar desde el fold), restaurante.dart (califProm/califCount).

Purge LOCKED panel: DELETE lib/core/{api_client.dart, auth_storage.dart, token_provider.dart, token_provider.g.dart, ws_client.dart, env.dart}; lib/models/token_pair.* ; assets/.env; test/{ws_client_test.dart, pedidos_ws_test.dart, mesas_ws_test.dart}. pubspec REMOVE dio, web_socket_channel, flutter_dotenv, flutter_secure_storage + assets block. main.dart quitar dotenv.load.

Entorno: $env:Path += ";C:\src\flutter\bin" (workdir panel_admin/). Codegen: dart run build_runner build --delete-conflicting-outputs.
</interfaces>

<threat_model>
- **QR determinista = anti duplicado:** crear mesa con número existente colisiona el doc ID → error controlado 'Ya existe una mesa con ese número' (set/create rechazado); jamás dos mesas comparten QR por construcción.
- **Staff no lee usuarios/ ajenos:** clientes derivados de pedidos (clienteNombre denormalizado) — cero expansión de rules sobre usuarios/.
- **Toggle de restaurante acotado:** super update con diff hasOnly(['activo']) — ni nombre ni calificaciones tocables desde el panel.
- **Reportes leen solo servido:** venta = estado 'servido' (pagado inalcanzable v1); montos client-side = display hasta Functions.
- **Purge al final:** orden tareas garantiza que ningún import legacy quede vivo antes del delete masivo (analyze lo verifica).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mesas CRUD con QR determinista</name>
  <files>panel_admin/lib/features/mesas/mesas_crud.dart, panel_admin/lib/features/mesas/mesas_screen.dart, panel_admin/lib/features/mesas/mesa_form_dialog.dart, panel_admin/lib/features/mesas/qr_dialog.dart, panel_admin/test/mesas/mesas_screen_test.dart, panel_admin/test/mesas/qr_dialog_test.dart</files>
  <read_first>panel_admin/lib/features/mesas/ (4 archivos: flujo actual contra API), test/mesas/ actuales, bloque interfaces (QR determinista + rules mesas)</read_first>
  <behavior>
    - Test: crearMesa(rid demo, numero 9, capacidad 4) → doc mesas/GRI-MESA-demo-009 con estado disponible y aparece en el stream del mapa
    - Test: crearMesa con número ya existente (009 de nuevo) → error controlado 'Ya existe una mesa con ese número' (doc determinista colisiona)
    - Test: actualizar capacidad persiste SOLO capacidad (+updatedAt); eliminar mesa quita el doc del stream
    - Test: qr_dialog renderiza QrImageView cuyo data == código del doc (GRI-MESA-demo-009) — sin llamada a endpoint
  </behavior>
  <action>Tests primero con fakes. mesas_crud.dart: crearMesa(db, rid, numero, capacidad) → docId = 'GRI-MESA-' + rid + '-' + numero.toString().padLeft(3, '0'); existe = (await db.doc('mesas/' + docId).get()).exists → throw controlado si existe; si no tx.set con {restauranteId, numero, capacidad, estado 'disponible', updatedAt serverTimestamp}; actualizarMesa → update {capacidad?, numero?, updatedAt}; eliminarMesa → delete. mesas_screen/mesa_form_dialog rewired (validación numero 1..999, capacidad 1..20). qr_dialog: QrImageView(data: mesa.id) + código visible + print_service intacto. El mapa ya refleja altas/bajas por el stream de 10-05. Verde + analyze 0.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/mesas/ (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Los 4 bloques del behavior verdes (doc ID determinista, colisión, update quirúrgico, QR sin endpoint)</ac>
    <ac type="source">mesas_crud no usa autoId ni endpoint HTTP; qr_dialog usa qr_flutter con data = código</ac>
  </acceptance_criteria>
  <done>Gestión de mesas completa con QR autogenerado — paridad MESA-01 sobre Firestore.</done>
</task>

<task type="auto">
  <name>Task 2: menú CRUD + clientes/historial + reservas staff + super-admin restaurantes</name>
  <files>panel_admin/lib/features/menu/menu_provider.dart, panel_admin/lib/features/clientes/clientes_provider.dart, panel_admin/lib/features/reservas/reservas_provider.dart, panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart, panel_admin/lib/models/categoria_staff.dart, panel_admin/lib/models/producto_staff.dart, panel_admin/lib/models/reserva.dart, panel_admin/lib/models/cliente_resumen.dart, panel_admin/lib/models/restaurante.dart, panel_admin/test/menu/menu_screen_test.dart, panel_admin/test/clientes/clientes_screen_test.dart, panel_admin/test/reservas/reservas_screen_test.dart, panel_admin/test/configuracion/restaurantes_test.dart</files>
  <read_first>los 4 providers fuente listados, las screens correspondientes (menu_screen, clientes_screen+historial_dialog, reservas_screen, configuracion_screen), los 4 tests actuales, bloque interfaces (clientes derivados de pedidos + rules)</read_first>
  <action>Models fromDoc según bloque interfaces (categoria_staff: orden/activo; producto_staff: precio int, disponible, activo, categoriaId; reserva: fecha Timestamp, estado, mesaNumero, numPersonas, usuarioId; restaurante: activo, califProm, califCount). menu_provider: stream categorias/productos where restauranteId==rid (para el CRUD con inactivos visibles) + crear/actualizar categoría (nombre, orden, activo) y producto (nombre, descripcion, precio int, categoriaId, disponible, activo) + toggles activo/disponible — alta con doc() autoId (menú no requiere determinismo). clientes_provider: clientesProvider(rid) → pedidos where restauranteId==rid orderBy createdAt DESC get + fold distinct usuarioId → List<ClienteResumen>{usuarioId, clienteNombre, nPedidos, totalConsumo, ultimoPedido}; historialProvider(rid, usuarioId) → mismos pedidos filtrados (historial_dialog intacto). reservas_provider (panel): reservasHoyProvider(rid) → Stream (misma query del dashboard 10-05) mapeando fromDoc; acción marcarOcupada(mesa) → cambiarEstadoMesa de 10-05 (reservada→ocupada); acción cancelarNoShow(reserva) → update estado 'cancelada' (staff permitido por rules). restaurantes_admin_provider: restaurantesProvider (solo super) → Stream restaurantes where activo==true (más los inactivos si el toggle los debe mostrar: usar get() de TODOS los restaurantes — super lee todo por rules — para poder re-activar) + toggleActivo(slug, valor) → update {activo: valor, } SOLO esa key. Tests de las 4 features reescritos con fakes (menu: crear producto visible en stream + toggle activo lo oculta del público; clientes: fold de 3 pedidos de 2 usuarios → 2 resúmenes con conteos; reservas: stream del día + marcar ocupada pasa mesa; restaurantes: toggle activo persiste solo esa key). Verde + analyze 0.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/menu/ test/clientes/ test/reservas/ test/configuracion/ (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Las 4 suites verdes (menu CRUD+toggles, clientes fold, reservas hoy+ocupada, super toggle)</ac>
    <ac type="source">clientes_provider NO lee collection usuarios/; restaurantes_admin update toca solo 'activo'</ac>
    <ac type="source">todas las queries llevan where restauranteId == rid (o son de super sobre restaurantes)</ac>
  </acceptance_criteria>
  <done>Gestión completa del restaurante (MENU-01/02, ADMN-03/04, RESV-05, PLAT-05) sobre Firestore.</done>
</task>

<task type="auto">
  <name>Task 3: reportes por rango (fold) + purge legacy del panel</name>
  <files>panel_admin/lib/features/reportes/reportes_provider.dart, panel_admin/lib/models/reporte.dart, panel_admin/test/reportes/reportes_screen_test.dart, panel_admin/pubspec.yaml, panel_admin/lib/main.dart</files>
  <read_first>panel_admin/lib/features/reportes/reportes_screen.dart y provider actuales, test/reportes/reportes_screen_test.dart, bloque interfaces (diseño fold + purge LOCKED), panel_admin/pubspec.yaml, panel_admin/lib/main.dart</read_first>
  <action>PARTE A — reportes: reportesProvider(rid, from, to) → Future: pedidos where restauranteId==rid where estado in ['servido'] where createdAt >= Timestamp(from) && createdAt <= Timestamp(to) get() + fold → Reporte{totalVentas int, numeroPedidos, topPlatos List<(nombre, cantidad, venta)>} (top platos desde items snapshot: Σ cantidad y Σ precio*cantidad por nombre); rellenar reporte.dart desde el fold; reportes_screen intacta (mismo model, rango picker actual). Tests: sembrar 4 pedidos servidos (2 en rango, 1 fuera, 1 enviado) + items → total/count solo servidos-en-rango; top platos ordenado por cantidad. PARTE B — purge LOCKED del panel: DELETE lib/core/{api_client.dart, auth_storage.dart, token_provider.dart, token_provider.g.dart, ws_client.dart, env.dart}; DELETE lib/models/token_pair.{dart,g.dart,freezed.dart}; DELETE assets/.env; DELETE test/{ws_client_test.dart, pedidos_ws_test.dart, mesas_ws_test.dart}; pubspec REMOVE dio, web_socket_channel, flutter_dotenv, flutter_secure_storage + bloque assets; main.dart quitar dotenv.load. rg "api_client|token_provider|ws_client|auth_storage|dotenv|TokenPair" lib test → 0. flutter pub get; flutter analyze 0; flutter test COMPLETO verde.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter analyze; flutter test; rg "api_client|token_provider|ws_client|auth_storage|dotenv|TokenPair" lib test → 0 matches (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Reportes verde: solo servidos en rango computan; top platos correcto desde snapshots</ac>
    <ac type="source">Archivos legacy y tests WS inexistentes; pubspec sin capa HTTP; main sin dotenv</ac>
    <ac type="cli">flutter analyze 0 issues; suite completa del panel verde; pub deps limpio</ac>
  </acceptance_criteria>
  <done>MIGRA-02 completo: panel 100% Firebase sin código muerto — ambas apps migradas (backend formalmente archivado).</done>
</task>

</tasks>

<verification>
- flutter analyze 0 + flutter test suite completa del panel verde.
- rg de legacy en panel lib/ y test/ → 0.
- Revisión de aislamiento: ninguna query de negocio sin where restauranteId.
</verification>

<success_criteria>
- MIGRA-02 completo: panel (login, dashboard/mapa, cocina, mesas+QR, menú, clientes, reservas, reportes, super-admin) 100% Firestore.
- MIGRA-05 completo: todas las vistas de operación en vivo (mapa/cola/reservas/cuenta del 10-05 + gestión 10-06).
- Ambas apps sin deps HTTP propias — prontas para el smoke e2e (10-07).
</success_criteria>

<output>
After completion, create `.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-06-SUMMARY.md`
</output>
