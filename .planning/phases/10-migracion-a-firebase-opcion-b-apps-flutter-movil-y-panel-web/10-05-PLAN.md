---
phase: 10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web
plan: "05"
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - panel_admin/pubspec.yaml
  - panel_admin/lib/firebase_options.dart
  - panel_admin/lib/core/firebase_bootstrap.dart
  - panel_admin/lib/core/firebase_providers.dart
  - panel_admin/lib/core/state_machines.dart
  - panel_admin/lib/main.dart
  - panel_admin/lib/app.dart
  - panel_admin/lib/features/auth/login_controller.dart
  - panel_admin/lib/features/dashboard/restaurante_provider.dart
  - panel_admin/lib/features/dashboard/restaurantes_list_provider.dart
  - panel_admin/lib/features/dashboard/mesas_provider.dart
  - panel_admin/lib/features/dashboard/stats_provider.dart
  - panel_admin/lib/features/dashboard/mesa_actions_sheet.dart
  - panel_admin/lib/features/cocina/pedidos_staff_provider.dart
  - panel_admin/test/state_machines_test.dart
  - panel_admin/test/helpers/firebase_fakes.dart
  - panel_admin/test/auth/login_form_test.dart
  - panel_admin/test/dashboard/mesa_tile_color_test.dart
  - panel_admin/test/dashboard/mesa_actions_test.dart
  - panel_admin/test/dashboard/stats_render_test.dart
  - panel_admin/test/cocina/cola_test.dart
autonomous: true
requirements: [MIGRA-02, MIGRA-05]

must_haves:
  truths:
    - "El staff (admin/mesero/cocina) inicia sesión en el panel y entra directo al dashboard de SU restaurante (claims rid); super_admin elige restaurante de la lista; un usuario cliente NO entra al panel"
    - "El mapa de mesas se actualiza EN VIVO cuando cambia el estado de una mesa (onSnapshot) y solo ofrece transiciones válidas por máquina de estados"
    - "El dashboard muestra estadísticas reales derivadas de streams (mesas por estado, reservas de hoy, pedidos activos)"
    - "La cocina ve la cola de pedidos activos EN VIVO y avanza estados según la matriz rol×transición; el aviso de cuenta solicitada es visible en vivo"
  artifacts:
    - path: "panel_admin/lib/features/auth/login_controller.dart"
      provides: "Login FirebaseAuth + routing por claims {role, rid}"
      contains: "idTokenResult"
    - path: "panel_admin/lib/features/dashboard/mesas_provider.dart"
      provides: "Stream mapa de mesas where restauranteId == rid"
      contains: "snapshots"
    - path: "panel_admin/lib/features/cocina/pedidos_staff_provider.dart"
      provides: "Stream cola cocina + avanzar estado con validarTransicion"
    - path: "panel_admin/lib/core/firebase_providers.dart"
      provides: "Providers inyectables + claimsProvider (role, rid)"
  key_links:
    - from: "panel_admin/lib/features/dashboard/mesas_provider.dart"
      to: "mesas where restauranteId == rid"
      via: "snapshots() orderBy numero"
      pattern: "restauranteId.*snapshots|snapshots.*restauranteId"
    - from: "panel_admin/lib/features/auth/login_controller.dart"
      to: "FirebaseAuth idTokenResult"
      via: "claims role/rid deciden la ruta inicial (super→selector, staff→dashboard, cliente→rechazo)"
      pattern: "claims"
---

<objective>
Big-bang del panel_admin (mitad 1): bootstrap Firebase (web), login por claims con routing staff/super, dashboard con mapa de mesas y stats en vivo, y cocina con cola de pedidos realtime + matriz rol×transición + aviso de cuenta.

Purpose: MIGRA-02 arranca sobre los patrones ya probados de app_cliente (10-04 completo — locked app primero). El panel es web-only: firebase_options solo web.
Output: Panel compilando con login/dashboard/cocina 100% Firestore realtime, suite de esas features verde, core legacy aún presente hasta 10-06.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-RESEARCH.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-02-SUMMARY.md
@.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-04-SUMMARY.md

@panel_admin/lib/main.dart
@panel_admin/lib/app.dart
@panel_admin/lib/features/auth/login_controller.dart
@panel_admin/lib/features/dashboard/mesas_provider.dart
@panel_admin/lib/features/dashboard/stats_provider.dart
@panel_admin/lib/features/cocina/pedidos_staff_provider.dart
</context>

<interfaces>
<!-- COPY-PASTE de app_cliente (10-02/10-04 ya verificados) — adaptar imports del package name (gri_panel_admin): -->
core/firebase_bootstrap.dart: initApp + gate USE_EMULATORS (useAuthEmulator 127.0.0.1:9099 / useFirestoreEmulator 127.0.0.1:8080 antes de runApp)
core/firebase_providers.dart: firebaseAuthProvider, firestoreProvider, authStateChangesProvider
core/state_machines.dart + test/state_machines_test.dart: port 1:1 idéntico
test/helpers/firebase_fakes.dart: buildFakeFirestoreConSeed() (restaurantes/demo + mesas + menú) + mockAuth

<!-- firebase_options.dart del panel: SOLO web (appId de grip.web) -->
Web: apiKey "AIzaSyAXPPuBMkMUgt_piyLg6uvWiEY0ff4kiC4", appId "1:703827387403:web:08ae995e35ce9516e6d30e", authDomain "p-gri-b5b40.firebaseapp.com", messagingSenderId "703827387403", projectId "p-gri-b5b40", storageBucket "p-gri-b5b40.firebasestorage.app", measurementId "G-8H4SQ9ZHV5" (fallback manual idéntico al de app_cliente; docs/FIREBASE_SETUP.md §7)

Deps LOCKED: firebase_core 4.13.0, firebase_auth 6.5.7, cloud_firestore 6.8.0; dev: fake_cloud_firestore 4.2.0, firebase_auth_mocks 0.15.2

Queries realtime del research (tabla onSnapshot):
- Mapa mesas: mesas where restauranteId==rid orderBy numero ASC → snapshots
- Cola cocina: pedidos where restauranteId==rid where estado in [enviado, aceptado, en_preparacion] orderBy createdAt ASC → snapshots (índice 10-01)
- Reservas hoy: reservas where restauranteId==rid where fecha >= inicioHoy && fecha < inicioMañana → snapshots (inicioHoy computado en cliente TZ local)
- Aviso cuenta: sesiones where restauranteId==rid where cuentaSolicitada==true where estado==activa → snapshots
- Stats: DERIVADO de los streams (sin endpoint): mesas por estado (count del stream mesas), reservas de hoy (count stream), pedidos activos (count stream cola)

Matriz rol×transición (paridad rules 10-01 — cocina/admin: enviado→aceptado|rechazado, aceptado→en_preparacion; admin/cocina/mesero: en_preparacion→servido; 'pagado' inalcanzable v1):
enviado→[aceptado, rechazado]: admin_restaurante, cocina (y super)
aceptado→[en_preparacion]: admin_restaurante, cocina (y super)
en_preparacion→[servido]: admin_restaurante, cocina, mesero (y super)
Cambio de estado de mesa desde el mapa: SOLO {estado, updatedAt} con transMesa válida (rules re-validan).

Entorno: $env:Path += ";C:\src\flutter\bin" (workdir panel_admin/). Codegen: dart run build_runner build --delete-conflicting-outputs.
</interfaces>

<threat_model>
- **Cliente no entra al panel:** login rechaza usuarios sin claims staff/super (role cliente o sin role) con mensaje claro — defense UX; rules igualmente denegarían toda query staff.
- **Aislamiento tenant (AUTH-03 equivalente):** TODA query del panel lleva where restauranteId == rid de claims (Pitfall 4: rules se evalúan por-doc; un doc ajeno en el listener deniega la query completa). Jamás el rid viene de un input del usuario.
- **Emuladores por flag:** mismo gate USE_EMULATORS que app_cliente; sin flag toca proyecto real.
- **Estado de pedido/mesa solo por transición:** avanzar usa validarTransicion + update de SOLO estado/updatedAt; las rules re-fuerzan transMesa y la matriz — doble barrera.
- **Claims solo lectura:** el panel jamás escribe claims ni el campo role de usuarios/{uid}.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: infra panel — deps, options web, bootstrap, providers, state machines, fakes</name>
  <files>panel_admin/pubspec.yaml, panel_admin/lib/firebase_options.dart, panel_admin/lib/core/firebase_bootstrap.dart, panel_admin/lib/core/firebase_providers.dart, panel_admin/lib/core/state_machines.dart, panel_admin/lib/main.dart, panel_admin/test/state_machines_test.dart, panel_admin/test/helpers/firebase_fakes.dart</files>
  <read_first>panel_admin/pubspec.yaml, panel_admin/lib/main.dart, app_cliente/lib/core/firebase_bootstrap.dart, app_cliente/lib/core/firebase_providers.dart, app_cliente/lib/core/state_machines.dart, app_cliente/test/helpers/firebase_fakes.dart (copiar de 10-02/10-04 — fuentes verificadas)</read_first>
  <action>Copiar el patrón verificado de app_cliente adaptando imports al package gri_panel_admin: pubspec ADD firebase_core 4.13.0, firebase_auth 6.5.7, cloud_firestore 6.8.0 + dev fake_cloud_firestore 4.2.0, firebase_auth_mocks 0.15.2 (sin quitar nada aún); firebase_options.dart SOLO webOptions (valores del bloque interfaces; intentar flutterfire configure primero y fallback manual idéntico al de app_cliente); firebase_bootstrap.dart (mismo gate USE_EMULATORS); firebase_providers.dart (firebaseAuthProvider, firestoreProvider, authStateChangesProvider — más claimsProvider FutureProvider<(String role, String? rid)> vía idTokenResult); state_machines.dart port 1:1 + test copiado; test/helpers/firebase_fakes.dart copia de app_cliente (mismo seed mínimo). main.dart: ensureInitialized + await bootstrap() + runApp (MANTENER dotenv.load hasta 10-06). build_runner + flutter analyze 0.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/state_machines_test.dart; flutter analyze (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="cli">Suite state machines verde; analyze 0 issues; suite existente del panel sin regresiones</ac>
    <ac type="source">firebase_options con projectId p-gri-b5b40 (web); bootstrap con gate USE_EMULATORS antes de runApp</ac>
  </acceptance_criteria>
  <done>Panel con la misma base Firebase verificada de app_cliente.</done>
</task>

<task type="auto">
  <name>Task 2: login por claims + routing super/staff + restaurante activo</name>
  <files>panel_admin/lib/features/auth/login_controller.dart, panel_admin/lib/app.dart, panel_admin/lib/features/dashboard/restaurante_provider.dart, panel_admin/lib/features/dashboard/restaurantes_list_provider.dart, panel_admin/test/auth/login_form_test.dart</files>
  <read_first>panel_admin/lib/features/auth/login_controller.dart, panel_admin/lib/app.dart (guard actual), panel_admin/lib/features/dashboard/restaurante_provider.dart y restaurantes_list_provider.dart, test/auth/login_form_test.dart</read_first>
  <action>login_controller: login(email, pass) → signInWithEmailAndPassword + await user.getIdTokenResult(true) para leer claims role/rid al instante (evita carrera del token cacheado); enrutado: role == 'super_admin' → vista selección de restaurante; role in [admin_restaurante, mesero, cocina] → dashboard con rid = claims.rid; cualquier otro (cliente o sin role) → error 'Esta aplicación es solo para personal del restaurante' + signOut. logout. Guard de app.dart rewire al authState nuevo (misma lógica redirect actual). restaurante_provider: restauranteActivoProvider(rid) → StreamProvider del doc restaurantes/{rid} (nombre para TopBar); seleccionRestauranteProvider para super (persistir elección en state/sessionStorage del router state). restaurantes_list_provider (solo super): Stream lista restaurantes where activo==true (super lee todos por rules). Tests login_form_test: login staff entra a dashboard con rid correcto; login super va a selector; login cliente rechazado con mensaje; credenciales inválidas → mensaje mapeado (invalid-credential). Claims se proveen en tests overrideando claimsProvider (patrón 10-02, NO se mockea el token).</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/auth/ (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Los 4 casos del login verde (staff/super/cliente-rechazado/credenciales inválidas)</ac>
    <ac type="source">login_controller usa idTokenResult para claims; el rid SIEMPRE viene de claims/selección, nunca de input libre</ac>
    <ac type="source">login_controller no importa dio/api_client/token_provider</ac>
  </acceptance_criteria>
  <done>Staff entra directo a su restaurante; super elige; cliente bloqueado — routing por claims 100%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: dashboard/mapa en vivo + stats derivadas + cocina cola realtime + aviso cuenta</name>
  <files>panel_admin/lib/features/dashboard/mesas_provider.dart, panel_admin/lib/features/dashboard/stats_provider.dart, panel_admin/lib/features/dashboard/mesa_actions_sheet.dart, panel_admin/lib/features/cocina/pedidos_staff_provider.dart, panel_admin/test/dashboard/mesa_tile_color_test.dart, panel_admin/test/dashboard/mesa_actions_test.dart, panel_admin/test/dashboard/stats_render_test.dart, panel_admin/test/cocina/cola_test.dart</files>
  <read_first>los 4 providers/sheets fuente listados, los 4 tests actuales, bloque interfaces (queries realtime + matriz rol×transición), app_cliente/lib/features/pedidos/pedidos_provider.dart (patrón snapshots verificado)</read_first>
  <behavior>
    - Test: stream de mesas — al escribir estado de una mesa en el fake, el widget del mapa refleja el nuevo color SIN rebuild manual (MIGRA-05)
    - Test: mesa_actions solo ofrece destinos válidos (puedeTransicionar) — ocupada NO ofrece 'disponible' (solo limpieza); limpieza→disponible sí
    - Test: cambiar estado de mesa actualiza SOLO estado/updatedAt y usa runTransaction o update con validarTransicion previo
    - Test: stats derivadas — 3 mesas disponibles/1 ocupada + 2 reservas hoy + 3 pedidos activos → tarjetas correctas (derivado de los 3 streams)
    - Test: cola cocina solo muestra pedidos en [enviado, aceptado, en_preparacion] ordenados por createdAt ASC; un servido desaparece del stream
    - Test: avanzar pedido enviado→aceptado con rol cocina OK; enviado→servido (salto) lanza TransicionInvalidaException → botón no disponible; mesero NO puede aceptar (enviado→aceptado) pero SÍ servir (en_preparacion→servido) según matriz
    - Test: aviso cuenta — sesión con cuentaSolicitada true estado activa aparece en el stream; cerrada desaparece
  </behavior>
  <action>Tests primero con fakes + claimsProvider overrideeado por rol. mesas_provider: mesasProvider(rid) → StreamProvider collection('mesas').where('restauranteId', isEqualTo: rid).orderBy('numero').snapshots() mapeando fromDoc (crear/portar Mesa.fromDoc en models/mesa.dart si no existe: id=codigoQR, restauranteId, numero, capacidad, estado, updatedAt); cambiarEstadoMesa(db, rid, mesa, nuevo) → update doc SOLO {estado, updatedAt} tras validarTransicion('mesa', ...) (rules re-validan). stats_provider: statsProvider(rid) → derivado combinando los 3 streams (mesas/reservasHoy/pedidosActivos — mismas queries del bloque interfaces; inicioHoy = DateTime.now() local truncado a día, inicioMañana +1d) manteniendo el model DashboardStats actual para que las cards no cambien. mesa_actions_sheet: opciones = destinos de MESA_TRANSITIONS filtrados por puedeTransicionar (misma UX actual). pedidos_staff_provider: pedidosStaffProvider(rid) → Stream cola (query del bloque interfaces); avanzarPedido(db, rol, pedido, nuevo) → validarTransicion('pedido', actual, nuevo) + chequeo matriz rol del bloque interfaces + update {estado, updatedAt}; avisoCuentaProvider(rid) → Stream sesiones where restauranteId==rid + cuentaSolicitada==true + estado=='activa' (badge en cocina/sidebar igual al badge actual del WS). cocina_screen/pedido_card intactos (models/pedido_staff.dart con fromDoc: items, total, clienteNombre, mesaId → mostrar número de mesa). Verde total + analyze 0.</action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/dashboard/ test/cocina/ (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    <ac type="test">Los 7 bloques del behavior verdes (mapa vivo, transiciones válidas, stats, cola realtime, matriz rol, aviso cuenta)</ac>
    <ac type="source">mesas/cola/reservasHoy/avisoCuenta son snapshots() con where restauranteId == rid — sin polling Timer ni ws_client</ac>
    <ac type="source">avanzarPedido valida transición + matriz rol antes del update</ac>
  </acceptance_criteria>
  <done>Panel operativo en vivo: login → dashboard/mapa/cocina con realtime nativo (MIGRA-02/05 mitad 1 completada).</done>
</task>

</tasks>

<verification>
- flutter analyze 0 + flutter test completo del panel (features migradas + legacy aún vivo sin regresión).
- Todas las queries staff llevan where restauranteId == rid (revisión rg: ningún .collection('mesas'|'pedidos'|'reservas'|'sesiones') del panel sin filtro restauranteId).
</verification>

<success_criteria>
- MIGRA-02 (parcial): panel arranca sobre Firebase con auth por claims y operación en vivo (dashboard/mapa/cocina).
- MIGRA-05: mapa, cola, reservas del día y aviso de cuenta por onSnapshot — polling y WS retirados de estas vistas.
</success_criteria>

<output>
After completion, create `.planning/phases/10-migracion-a-firebase-opcion-b-apps-flutter-movil-y-panel-web/10-05-SUMMARY.md`
</output>
