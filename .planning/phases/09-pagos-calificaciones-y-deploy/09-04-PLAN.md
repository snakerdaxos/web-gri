---
phase: 09-pagos-calificaciones-y-deploy
plan: 04
type: execute
wave: 3
depends_on: ["09-01"]
files_modified:
  - deploy/docker-compose.prod.yml
  - deploy/nginx/gri.conf
  - deploy/nginx/local-verify.conf
  - .env.production.example
  - deploy/README.md
  - deploy/verify_local.ps1
  - deploy/verify_ws.py
autonomous: true
requirements: [INFR-01, INFR-02, INFR-03]
user_setup:
  - service: "Ubuntu Server (deploy real — POST-fase, manual)"
    why: "No hay SSH al servidor desde la máquina de desarrollo; el deploy real lo ejecuta el usuario con la guía"
    env_vars:
      - name: "MYSQL_ROOT_PASSWORD / MYSQL_APP_PASSWORD / JWT_SECRET / SUPER_ADMIN_*"
        source: "Generar con secrets.token_urlsafe(48) — comandos en deploy/README.md"
    dashboard_config:
      - task: "DNS: registro A del dominio (p.ej. gri.example.com) → IP del servidor"
        location: "Panel del registrador de dominios"
      - task: "Aperturas de puertos 80/443 en el firewall"
        location: "Servidor Ubuntu (ufw allow)"
  - service: "Wompi (cuando el KYC esté aprobado — POST-fase)"
    why: "Credenciales de pasarela reales"
    env_vars:
      - name: "WOMPI_PUBLIC_KEY / WOMPI_PRIVATE_KEY / WOMPI_EVENTS_SECRET / WOMPI_INTEGRITY_SECRET + SANDBOX_MODE=false"
        source: "Dashboard Wompi (merchant)"
    dashboard_config:
      - task: "Configurar webhook https://{dominio}/webhooks/pago"
        location: "Dashboard Wompi → Configuración → Webhooks"

must_haves:
  truths:
    - "docker compose --env-file .env.production -f deploy/docker-compose.prod.yml config valida sin errores"
    - "La configuración nginx pasa nginx -t (vía contenedor) e incluye proxy_http_version 1.1 + headers Upgrade para /ws/ y proxy_read_timeout 3600s"
    - "El stack prod local (production-like) levanta: health OK, el panel sirve index.html, /pagos/sandbox/* responde 404 real (SANDBOX_MODE=false) y un WebSocket se conecta a través de nginx"
    - "deploy/README.md permite a un usuario reproducir el deploy en Ubuntu 24.04 paso a paso sin conocimiento previo del repo"
  artifacts:
    - path: "deploy/docker-compose.prod.yml"
      provides: "Stack prod: mysql:8.4.11 pin, api 1 worker, restart unless-stopped, sin puertos BD, alembic en boot"
      contains: "8.4.11"
    - path: "deploy/nginx/gri.conf"
      provides: "Reverse proxy TLS: /ws/ upgrade, /api-paths → :8000, / → panel estático SPA"
      contains: "proxy_http_version 1.1"
    - path: ".env.production.example"
      provides: "Template de secretos prod (DEMO_MODE=false, SANDBOX_MODE=false)"
    - path: "deploy/README.md"
      provides: "Guía Ubuntu paso a paso + troubleshooting"
      min_lines: 80
    - path: "deploy/verify_local.ps1"
      provides: "Smoke production-like local automatizado"
  key_links:
    - from: "deploy/nginx/gri.conf"
      to: "127.0.0.1:8000"
      via: "proxy_pass en locations /ws/ y API; el api contenedor publica SOLO 127.0.0.1:8000"
      pattern: "proxy_pass http://127\\.0\\.0\\.1:8000"
    - from: "deploy/docker-compose.prod.yml"
      to: "backend/Dockerfile"
      via: "mismo build (CMD ya corre alembic upgrade head → INFR-03 en prod)"
      pattern: "build:.*backend"
    - from: "deploy/nginx/local-verify.conf"
      to: "deploy/verify_local.ps1"
      via: "nginx contenedor local :8080 sin ssl proxyando al stack prod local + estáticos del panel"
      pattern: "listen 8080"
---

<objective>
Artefactos de despliegue a producción en Ubuntu Server 24.04: docker-compose prod endurecido (MySQL pinneado sin puertos expuestos, API 1 worker), nginx en el HOST con TLS/certbot y proxy WebSocket correcto, template de secretos, guía reproducible paso a paso, y verificación local production-like automatizada.

Purpose: Verificación final de INFR-01/02/03 en configuración de producción. No hay SSH al servidor: el plan produce artefactos + guía + validación local; el deploy real lo ejecuta el usuario siguiendo deploy/README.md.
Output: directorio deploy/ completo + .env.production.example + verificación local ejecutada y documentada.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pagos-calificaciones-y-deploy/09-RESEARCH.md
@.planning/phases/09-pagos-calificaciones-y-deploy/09-01-SUMMARY.md

@docker-compose.yml
@backend/Dockerfile
@backend/app/core/config.py
@backend/app/main.py

<interfaces>
<!-- Hechos del codebase que los artefactos deben respetar. -->

docker-compose.yml (dev — el prod es su versión endurecida): mysql con flags utf8mb4/-05:00/healthcheck mysqladmin + volumen gri_mysql_data; api con DB_HOST=mysql, depends_on service_healthy, env interpolada de .env raíz. El PROD cambia: image mysql:8.4.11 (pin exacto), SIN ports 3306, env_file/vars desde .env.production, api publica SOLO 127.0.0.1:8000:8000 (nginx del host proxya), restart unless-stopped en ambos, project name distinto (gri-prod) para no colisionar con dev.

backend/Dockerfile CMD: sh -c "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port 8000" — YA aplica migraciones en boot (INFR-03); 1 worker SIN --workers (broadcaster in-memory — prohibido añadirlos). Mismo Dockerfile para prod, sin cambios.

Settings (tras 09-01): SANDBOX_MODE, WOMPI_* (public/private/events/integrity), DEMO_MODE, ENVIRONMENT, CORS_ORIGINS, JWT_SECRET, SUPER_ADMIN_*, DB_*. SANDBOX_MODE=false en prod → rutas sandbox 404 (montaje condicional verificado por 09-01).

Routers API montados en RAÍZ (sin prefijo /api): /auth, /public, /cliente, /staff, /admin, /health, /docs, /openapi.json, /redoc, /webhooks, /pagos, /ws/staff, /ws/cliente.

Patrón nginx WS (verificado nginx.org — research §Patrón 7): map $http_upgrade $connection_upgrade {default upgrade; '' close;} + location /ws/ con proxy_http_version 1.1 + proxy_set_header Upgrade $http_upgrade + proxy_set_header Connection $connection_upgrade + proxy_read_timeout 3600s (default 60s mata WS idle; nginx < 1.29.7 REQUIERE el http_version explícito — Ubuntu 24.04 trae 1.24).

Panel admin: flutter build web --release (panel_admin/build/web) → estáticos servidos por nginx en / con try_files SPA. Same-origin por paths → CERO CORS para el panel; CORS_ORIGINS se setea igual como defensa.

WS auth: /ws/cliente y /ws/staff reciben JWT (query param token — patrón api/ws.py); verify_ws.py puede registrar un usuario vía API, loguear y conectar.
</interfaces>
</context>

<threat_model>
- **BD inaccesible desde fuera:** el compose prod NO publica 3306 (solo red interna de docker). Dev compose lo publica — documentar la diferencia.
- **API no expuesta directamente:** solo 127.0.0.1:8000 (nginx del host es el único cliente). Nadie bypasea TLS/nginx.
- **SANDBOX_MODE=false en prod:** el sandbox de pagos NO debe existir en producción (montaje condicional 09-01 + verificación local 404). .env.production.example lo trae false y README lo marca obligatorio.
- **Secrets de producción:** .env.production.example SOLO placeholders; README instruye generar JWT_SECRET/DB passwords con secrets.token_urlsafe — jamás commitear .env.production (verificar .gitignore).
- **TLS termination:** certbot --nginx en el host con auto-renew systemd; HTTP→HTTPS redirect; nginx en HOST (no Docker) para evitar el reload-dance (Pitfall 7).
- **DEMO_MODE=false:** el seed demo no corre en prod (gate 03-02).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.prod.yml + nginx/gri.conf + local-verify.conf + .env.production.example</name>
  <files>deploy/docker-compose.prod.yml, deploy/nginx/gri.conf, deploy/nginx/local-verify.conf, .env.production.example</files>
  <read_first>docker-compose.yml (base a endurecer), backend/Dockerfile (CMD con alembic), backend/app/core/config.py (vars Settings tras 09-01), backend/app/main.py (routers montados en raíz), 09-RESEARCH.md §Patrón 7 + §Deploy verificado + Pitfalls 4/7</read_first>
  <action>
  1. **deploy/docker-compose.prod.yml** (name: gri-prod): servicio mysql — image mysql:8.4.11 (PIN exacto), container_name gri-prod-mysql, restart: unless-stopped, environment MYSQL_ROOT_PASSWORD/${MYSQL_APP_PASSWORD} vars, SIN sección ports, volumen gri_prod_mysql_data, mismos flags --character-set-server utf8mb4 --collation-server utf8mb4_unicode_ci --default-time-zone=-05:00, healthcheck idéntico al dev. Servicio api — build: ../backend (el compose vive en deploy/), container_name gri-prod-api, restart: unless-stopped, depends_on mysql service_healthy, environment completo (DB_HOST mysql, DEMO_MODE false, ENVIRONMENT production, SANDBOX_MODE false, JWT_SECRET, SUPER_ADMIN_*, CORS_ORIGINS, WOMPI_*), ports: "127.0.0.1:8000:8000" (SOLO localhost — nginx del host). Volumen nombrado. Comentarios: prohibido --workers (broadcaster in-memory), 3306 no publicado a propósito.
  2. **deploy/nginx/gri.conf**: copiar el patrón §Patrón 7 del research COMPLETO: (a) map $http_upgrade $connection_upgrade arriba; (b) server :80 con server_name gri.example.com (PLACEHOLDER marcado con comentario TODO-reemplazar) que SOLO hace return 301 https (certbot inserta su location de challenge ACME al correr --nginx — nota en comentario); (c) server :443 ssl http2 con ssl_certificate/ssl_certificate_key de letsencrypt (rutas con el placeholder del dominio); (d) location /ws/ con los 3 headers OBLIGATORIOS + proxy_http_version 1.1 + proxy_read_timeout 3600s + proxy_set_header Host; (e) location regex ^/(auth|public|cliente|staff|admin|health|docs|openapi\.json|redoc|webhooks|pagos)(/|$) → proxy_pass 127.0.0.1:8000 + Host + X-Forwarded-For + X-Forwarded-Proto + client_max_body_size 5m; (f) location / con root /var/www/gri-panel + try_files $uri $uri/ /index.html + sub-location de estáticos (js|json|png|jpg|svg|wasm|otf|ttf) con expires 7d. Comentarios explicando cada bloque (el archivo es también documentación).
  3. **deploy/nginx/local-verify.conf**: variante para verificación local en contenedor — UN solo server listen 8080 (sin ssl, sin redirect), MISMAS locations /ws/ y API (proxy_pass http://gri-prod-api:8000 — por nombre de contenedor dentro de la red docker) y / con root /usr/share/nginx/html + try_files SPA. Este archivo NO se usa en el servidor real (allí corre el gri.conf nativo) — docstring lo aclara.
  4. **.env.production.example** (raíz del repo): todas las vars del compose prod con placeholders seguros y comentarios: MYSQL_ROOT_PASSWORD, MYSQL_APP_PASSWORD (comando de generación python secrets), JWT_SECRET (idem + advertencia), SUPER_ADMIN_EMAIL/PASSWORD, DEMO_MODE=false (comentado: prod jamás true), SANDBOX_MODE=false (comentado: solo false cuando haya credenciales Wompi reales — mientras tanto el pago sandbox queda deshabilitado en prod), WOMPI_PUBLIC_KEY/PRIVATE_KEY/EVENTS_SECRET/INTEGRITY_SECRET (placeholders pub_test_/prv_test_ + nota KYC), CORS_ORIGINS=https://gri.example.com. Verificar que .gitignore cubre .env.production (añadirlo si falta).
  </action>
  <verify>
    <automated>Copy-Item .env.production.example .env.production.temp; docker compose --env-file .env.production.temp -f deploy/docker-compose.prod.yml config -q; if ($?) { Write-Output "COMPOSE-OK" }; docker run --rm -v "${PWD}\deploy\nginx\local-verify.conf:/etc/nginx/conf.d/default.conf:ro" nginx:1.24 nginx -t; Remove-Item .env.production.temp</automated>
  </verify>
  <acceptance_criteria>
  - docker compose config -q del prod valida (con env file temporal del example)
  - nginx -t (contenedor nginx:1.24) pasa para local-verify.conf; gri.conf pasa validación de sintaxis al sustituir el dominio (mismo test con sed del placeholder o nota en SUMMARY si los paths letsencrypt lo impiden — en tal caso validar una variante sin ssl_certificate)
  - El compose prod NO contiene "3306:3306" en ports ni --workers; mysql usa 8.4.11 literal
  - .env.production.example tiene DEMO_MODE=false y SANDBOX_MODE=false; .gitignore incluye .env.production
  </acceptance_criteria>
  <done>Los 4 artefactos existen y validan mecánicamente: compose prod config OK, nginx conf sintácticamente válida, secrets template completo.</done>
</task>

<task type="auto">
  <name>Task 2: README guía Ubuntu + verify_local.ps1 + verify_ws.py + verificación local ejecutada</name>
  <files>deploy/README.md, deploy/verify_local.ps1, deploy/verify_ws.py</files>
  <read_first>deploy/docker-compose.prod.yml y deploy/nginx/*.conf (Task 1), backend/app/api/ws.py (auth del WS para verify_ws.py), backend/tests/conftest.py (patrón register/login contra localhost), 09-RESEARCH.md §Patrón 7 (verificación local) + §Pitfall 4, panel_admin/build (existe tras flutter build web)</read_first>
  <action>
  1. **deploy/README.md** — guía paso a paso Ubuntu 24.04 desde cero, secciones numeradas: (1) Prerrequisitos (servidor Ubuntu 24.04, dominio, DNS A → IP); (2) Instalar Docker (curl -fsSL https://get.docker.com | sh + sudo usermod -aG docker $USER); (3) Clonar repo + traer el código al servidor; (4) cp .env.production.example .env.production + editar TODOS los secrets (comandos de generación python -c secrets); (5) apt install nginx certbot python3-certbot-nginx; (6) construir el panel (flutter build web --release en máquina dev) y copiar panel_admin/build/web → /var/www/gri-panel (rsync/scp) — alternativa documentada si el servidor no tiene Flutter (build local + copia); (7) cp deploy/nginx/gri.conf /etc/nginx/sites-available/gri + symlink sites-enabled + reemplazar gri.example.com por el dominio real + nginx -t + systemctl reload; (8) docker compose --env-file .env.production -f deploy/docker-compose.prod.yml up -d --build (las migraciones corren solas en el boot — INFR-03); (9) certbot --nginx -d {dominio} (TLS + redirect + auto-renew systemd timer — verificar con systemctl list-timers); (10) Verificación post-deploy (curl https://{dominio}/health, panel carga, WS conecta, /pagos/sandbox → 404); (11) Operación: actualizar (git pull + up -d --build), logs (docker logs), backup MySQL (docker exec mysqldump), rotación de secretos; (12) Troubleshooting: WS se corta a los 60s (proxy_read_timeout), certbot renewal falla (nginx en Docker = anti-patrón), sandbox 404 esperado sin credenciales, Wompi futuro (SANDBOX_MODE=false + keys + webhook https://{dominio}/webhooks/pago en el dashboard). Cada comando en su bloque exacto.
  2. **deploy/verify_ws.py**: script Python (corre con uv run desde backend/ — httpx + httpx-ws ya están): registra usuario único, login, conecta ws://{host}:{port}/ws/cliente?token={access} con httpx_ws.aconnect_ws, espera 2s sin error → imprime WS-OK; cualquier fallo → exit 1. Args: --base http://localhost:8080 (pasa por nginx).
  3. **deploy/verify_local.ps1** (PowerShell 5.1 compatible — sin Get-Date -AsUTC): pasos — (a) docker compose down (stack dev, guarda nota para restaurar); (b) Copy-Item .env.production.example .env.production.local + sobreescribe DB passwords con valores test; (c) docker compose --env-file .env.production.local -f deploy/docker-compose.prod.yml up -d --build; (d) esperar health (loop con retry: GET http://localhost:8000/health hasta 200, timeout 120s); (e) assert GET http://localhost:8000/pagos/sandbox/test → 404 (SANDBOX_MODE=false real); (f) construir el panel si build/web no existe (flutter build web --release con $env:Path += ";C:\src\flutter\bin"); (g) docker network create gri-verify si no existe + docker run -d --name gri-nginx-verify --network container:gri-prod-api NO — usar red compartida: conectar nginx a la red del proyecto (docker network connect) con -v de local-verify.conf y -v de panel_admin/build/web:/usr/share/nginx/html:ro -p 8080:8080 nginx:1.24; (h) asserts vía nginx :8080 — GET / → 200 con index.html del panel (contiene "GRI"), GET /health → 200 (a través del proxy), uv run python deploy/verify_ws.py --base http://localhost:8080 (WS por nginx); (i) teardown: docker rm -f gri-nginx-verify + docker compose --env-file .env.production.local -f deploy/docker-compose.prod.yml down + Remove-Item .env.production.local + docker compose up -d (restaura dev). Salida: PASO/FAIL por check + exit code. Timeout de espera de health con [DateTimeOffset]::UtcNow (no Get-Date -AsUTC).
  4. **Ejecutar la verificación local** end-to-end y dejar el resultado en el SUMMARY (checks verdes). Si el paso de WS por nginx falla, diagnosticar con los headers del local-verify.conf (los 3 obligatorios + http_version) ANTES de tocar nada más.
  </action>
  <verify>
    <automated>powershell -File deploy/verify_local.ps1; if ($LASTEXITCODE -eq 0) { Write-Output "VERIFY-LOCAL-OK" } else { exit 1 }</automated>
  </verify>
  <acceptance_criteria>
  - verify_local.ps1 termina exit 0 con TODOS los checks verdes: health por nginx 200, panel index.html 200 vía /, sandbox 404 en modo prod real, WS conecta a través de nginx :8080
  - El stack dev queda restaurado arriba al final (docker compose ps muestra mysql+api)
  - deploy/README.md cubre las 12 secciones con comandos concretos ejecutables (nada de "configurar nginx" sin el comando exacto)
  - verify_ws.py se conecta con JWT real (no un test de handshake rechazado)
  </acceptance_criteria>
  <done>La configuración de producción está validada localmente de punta a punta (nginx + stack prod + WS + panel + sandbox-off) y la guía permite reproducirla en Ubuntu real sin pasos faltantes.</done>
</task>

</tasks>

<verification>
- docker compose -f deploy/docker-compose.prod.yml config -q (con env temporal)
- verify_local.ps1 verde completo (incluye nginx -t implícito al levantar el contenedor)
- README revisión cruzada: cada artefacto referenciado existe; cada comando usa rutas reales del repo
- Deploy REAL a Ubuntu = manual post-fase (usuario_setup) — documentado en README sección verificación post-deploy
</verification>

<success_criteria>
- INFR-01/02 re-verificados en configuración prod: MySQL Docker persistente sin exposición, API por configuración de entorno
- INFR-03 re-verificado: migraciones automáticas en boot del stack prod (alembic upgrade head en CMD)
- Verificación local production-like completa y reproducible por script
- Guía de deploy lista para el servidor real
</success_criteria>

<output>
After completion, create `.planning/phases/09-pagos-calificaciones-y-deploy/09-04-SUMMARY.md`
</output>
