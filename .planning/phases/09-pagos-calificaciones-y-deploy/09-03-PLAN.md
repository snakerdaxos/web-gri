---
phase: 09-pagos-calificaciones-y-deploy
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - app_cliente/pubspec.yaml
  - app_cliente/lib/models/pago.dart
  - app_cliente/lib/models/restaurante.dart
  - app_cliente/lib/models/restaurante_detalle.dart
  - app_cliente/lib/core/api_client.dart
  - app_cliente/lib/features/pagos/pago_controller.dart
  - app_cliente/lib/features/pagos/pago_screen.dart
  - app_cliente/lib/features/pagos/calificacion_sheet.dart
  - app_cliente/lib/features/pedidos/pedido_estado_screen.dart
  - app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart
  - app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart
  - app_cliente/lib/app.dart
  - app_cliente/test/pagos/pago_screen_test.dart
  - app_cliente/test/pagos/calificacion_sheet_test.dart
  - app_cliente/test/restaurantes/list_test.dart
autonomous: false
requirements: [PAGO-02, CALI-01, CALI-02]

must_haves:
  truths:
    - "El cliente con la cuenta pedida ve un botón 'Pagar en línea' que abre el checkout (sandbox en dev) en el navegador"
    - "Al aprobar el pago, la app muestra el estado de éxito al volver (polling) sin necesidad de recargar nada"
    - "Tras el pago el cliente puede calificar con estrellas y comentario desde la propia pantalla de pago"
    - "La lista de restaurantes muestra ⭐ {promedio} ({count}) con datos reales y '—' cuando no hay calificaciones"
  artifacts:
    - path: "app_cliente/lib/features/pagos/pago_screen.dart"
      provides: "Pantalla de pago: total, botón Pagar (launchUrl), polling de estado, éxito/rechazo"
      min_lines: 60
    - path: "app_cliente/lib/features/pagos/pago_controller.dart"
      provides: "intención → launch (inyectable para tests) → poll 2.5s"
    - path: "app_cliente/lib/features/pagos/calificacion_sheet.dart"
      provides: "Bottom sheet 5 estrellas custom + comentario + enviar"
    - path: "app_cliente/test/pagos/pago_screen_test.dart"
      provides: "widget tests del flujo de pago"
    - path: "app_cliente/test/pagos/calificacion_sheet_test.dart"
      provides: "widget tests del sheet de calificación"
  key_links:
    - from: "app_cliente/lib/features/pedidos/pedido_estado_screen.dart"
      to: "app_cliente/lib/features/pagos/pago_screen.dart"
      via: "cuenta pedida → botón 'Pagar en línea' → context.push('/mesa/pago')"
      pattern: "push\\('/mesa/pago'\\)|/mesa/pago"
    - from: "app_cliente/lib/features/pagos/pago_controller.dart"
      to: "url_launcher"
      via: "launchUrl(Uri.parse(url), mode: LaunchMode.externalApplication) DENTRO del onPressed del botón (user action — Pitfall 8)"
      pattern: "launchUrl"
    - from: "app_cliente/lib/core/api_client.dart"
      to: "/cliente/pagos/intencion + /cliente/pagos/{id} + /cliente/calificaciones"
      via: "crearIntencionPago() / getPagoEstado(id) / crearCalificacion(...)"
      pattern: "/cliente/pagos"
    - from: "app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart"
      to: "models/restaurante.dart"
      via: "totalCalificaciones > 0 → '⭐ 4.8 (245)' sino '—'"
      pattern: "totalCalificaciones"
---

<objective>
App cliente: cierre del ciclo en UI — pagar la cuenta en línea (intención → checkout externo → polling → éxito), calificar con estrellas tras el pago, y rating real en la lista/detalle de restaurantes.

Purpose: Lado UI de PAGO-02, CALI-01 y CALI-02. Depende de 09-01 (contratos de pago) y 09-02 (campo total_calificaciones + endpoint de calificación).
Output: features/pagos/ completa (controller + screen + sheet), models regen, botón de pago en la pantalla de pedidos, rating real en restaurantes, 2 suites de widget tests (~10 tests) + checkpoint humano e2e sandbox.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-pagos-calificaciones-y-deploy/09-RESEARCH.md
@.planning/phases/09-pagos-calificaciones-y-deploy/09-01-SUMMARY.md
@.planning/phases/09-pagos-calificaciones-y-deploy/09-02-SUMMARY.md

@app_cliente/lib/core/api_client.dart
@app_cliente/lib/core/env.dart
@app_cliente/lib/core/format.dart
@app_cliente/lib/models/pedido.dart
@app_cliente/lib/models/restaurante.dart
@app_cliente/lib/models/restaurante_detalle.dart
@app_cliente/lib/features/pedidos/pedido_estado_screen.dart
@app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart
@app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart
@app_cliente/lib/app.dart
@app_cliente/lib/core/ws_client.dart

<interfaces>
<!-- Contratos backend (ya construidos por 09-01/09-02) y patrones Flutter existentes. -->

API backend (09-01/09-02 — respuesta JSON):
- POST /cliente/pagos/intencion (auth cliente, body vacío) → 201/200 {pago_id: int, referencia: str, monto: float, estado: "pendiente", checkout_url: str} — checkout_url RELATIVA en sandbox ("/pagos/sandbox/{ref}") → anteponer Env.apiBaseUrl; 404 sin sesión, 409 pedidos en curso/sin pedidos
- GET /cliente/pagos/{pago_id} → {pago_id, estado: "pendiente"|"aprobado"|"rechazado", referencia, monto: float, pedido_ids: [int]} — 404 ajeno
- POST /cliente/calificaciones (auth cliente) body {pedido_id: int, estrellas: 1-5, comentario?: str<=1000} → 201; 404 ajeno, 409 no pagado/ya calificado, 422 rango
- GET /public/restaurantes y /public/restaurantes/{id} → cada restaurante ahora incluye calificacion: float|null (1 decimal) + total_calificaciones: int

ApiClient (core/api_client.dart): métodos existentes usan _dio.post/get con auth automático del interceptor; pedirCuenta() es el ejemplo de POST sin body. Nuevo métodos siguen el MISMO patrón (r.data es Map<String, dynamic>).

Env (core/env.dart): Env.apiBaseUrl (http://localhost:8000 en dev). formatCOP(double) en core/format.dart para el monto.

Models Flutter: freezed + json_serializable; monto llega como double (backend serializa Decimal→float con field_serializer — igual que Pedido.total). Restaurante.calificacionLabel ya existe ("4.8" o "—"). build_runner: NO usar --delete-conflicting-outputs (removido).

Widget tests: patrón ProviderScope inline por test (Riverpod 3.4 no exporta Override); mocks del ApiClient via overrides: [apiClientProvider.overrideWithValue(fakeApi)] con FakeApi implements/extends ApiClient (patrón tests existentes de pedidos/cuenta). Timer/polling en tests: NO depender de timers reales — exponer método público de poll/refresh en el controller e invocarlo directo (patrón estado_test).

Routes (app.dart): GoRoute push fullscreen existentes /mesa, /mesa/pedidos — añadir /mesa/pago igual. sesionProvider (features/sesion_qr) colapsa a Stream.empty tras sesion.cerrada (07-03) — la PagoScreen NO depende de sesionProvider para su estado (usa su propio controller).

Gotchas STATE.md: flutter necesita $env:Path += ";C:\src\flutter\bin" en cada comando; freezed pin 4.0.0-dev.3 (ya en pubspec); screens en tests sin Scaffold → envolver Material; ListView lazy → physicalSize si hace falta.
</interfaces>
</context>

<threat_model>
- **El pago NUNCA se marca por el retorno del checkout:** la UI solo cree en GET /cliente/pagos/{id} (fuente: backend con webhook verificado). El launch/polling es UX — ninguna mutación local de "pagado".
- **launchUrl solo en user action:** dentro del onPressed del botón Pagar (browser bloquea programmatic launch — Pitfall 8). Fallback: si launchUrl retorna false → SnackBar con la URL.
- **Calificación post-pago only:** el sheet solo se abre desde el estado aprobado del pago y envía pedido_id proveniente de pago.pedido_ids (el backend revalida — defensa en el server).
- **Datos de rating:** read-only del endpoint público; sin lógica local de promedio (el número que llega ES el promedio).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: url_launcher + models + ApiClient + feature pago (controller/screen) + wiring + tests</name>
  <files>app_cliente/pubspec.yaml, app_cliente/lib/models/pago.dart, app_cliente/lib/core/api_client.dart, app_cliente/lib/features/pagos/pago_controller.dart, app_cliente/lib/features/pagos/pago_screen.dart, app_cliente/lib/features/pedidos/pedido_estado_screen.dart, app_cliente/lib/app.dart, app_cliente/test/pagos/pago_screen_test.dart</files>
  <read_first>app_cliente/lib/core/api_client.dart (patrón de métodos + pedirCuenta), app_cliente/lib/core/env.dart, app_cliente/lib/core/format.dart, app_cliente/lib/features/pedidos/pedido_estado_screen.dart (_cuentaSection — ahí vive el wiring), app_cliente/lib/app.dart (rutas), app_cliente/test/pedidos/cuenta_test.dart (patrón FakeApi + ProviderScope inline), 09-RESEARCH.md §Patrón 6 + §Pitfall 8</read_first>
  <behavior>
  - Test: PagoScreen renderiza el monto de la intención (FakeApi que retorna intención con monto 50000.0 → Text con formatCOP(50000))
  - Test: tap en "Pagar" llama al launcher inyectado con la URL absoluta (Env.apiBaseUrl + checkout_url relativa) y NO muta el estado local a aprobado (sigue pendiente hasta poll)
  - Test: controller.poll() con FakeApi que retorna estado aprobado → UI muestra éxito y el CTA de calificación visible; con rechazado → UI de error con botón reintentar (que crea nueva intención)
  - Test: 409 del backend (pedidos en curso) al crear intención → SnackBar con el detail del server
  </behavior>
  <action>
  1. **pubspec.yaml**: añadir url_launcher: ^6.3.2 (ÚNICA dependencia nueva — decision research). flutter pubspec get.
  2. **models/pago.dart** (freezed + json): PagoIntencion {pago_id (JsonKey pago_id), referencia, monto double, estado String, checkout_url (JsonKey checkout_url)}; PagoEstado {pago_id, estado, referencia, monto double, pedido_ids List<int> (JsonKey pedido_ids)}. Correr build_runner (sin flags removidos).
  3. **ApiClient**: Future<PagoIntencion> crearIntencionPago() → POST /cliente/pagos/intencion (sin data — body vacío); Future<PagoEstado> getPagoEstado(int pagoId) → GET /cliente/pagos/{pagoId}. Mismo patrón de parseo que los métodos existentes.
  4. **pago_controller.dart**: PagoController (Notifier o clase simple manejada por la screen — seguir el estilo de reserva_controller): estados idle/creando/pendiente/aprobado/rechazado/error + campos pagoIntencion y ultimoEstado. Método pagar(): crearIntencionPago() → resolver URL absoluta (si checkout_url empieza por http la usa tal cual; sino Env.apiBaseUrl + checkout_url) → launcher inyectable (campo public Future<bool> Function(Uri url)? launcher que defaultea a launchUrl(uri, mode: LaunchMode.externalApplication) — patrón backoffBuilder de ws_client para testabilidad) → queda en pendiente. Método poll(): getPagoEstado → aprobado → state aprobado (guarda pedido_ids para el sheet); rechazado → state rechazado. La screen monta Timer.periodic 2.5s que llama poll() SOLO en pendiente (cancel en dispose) + WidgetsBindingObserver didChangeAppLifecycleState resumed → poll() inmediato.
  5. **pago_screen.dart**: ConsumerStatefulWidget — vista resumen: "Total a pagar" + formatCOP(monto), botón primario "Pagar 💳" (onPressed → controller.pagar(); deshabilitado mientras crea/pendiente-con-launch-ya-hecho muestra "Esperando pago…" + botón reintentar poll), estado aprobado → vista de éxito (✅ "Pago aprobado" + monto + CTA "Califica tu experiencia ⭐" que abre el sheet del Task 2), estado rechazado → vista de error con botón "Intentar de nuevo" (nueva intención — el backend crea pago nuevo porque el anterior quedó terminal). SnackBar de errores con detail del DioException (patrón _pedirCuenta del pedido_estado_screen).
  6. **Wiring**: app.dart añade GoRoute path /mesa/pago → PagoScreen (push fullscreen, fuera del shell). pedido_estado_screen.dart _cuentaSection: cuando yaPedida, el container "Cuenta solicitada ✓" se reemplaza por columna: texto "Cuenta solicitada ✓" + botón "Pagar en línea 💳" (style primario) → context.push('/mesa/pago'). Mantener _cuentaYaPedida mirror.
  7. **test/pagos/pago_screen_test.dart**: los 4 bloques de <behavior> con FakeApi (override apiClientProvider inline ProviderScope) + launcher spy (registra la URI llamada, retorna true). Sin timers reales: llamar controller.poll() directo tras pump (patrón estado_test.dart). flutter analyze 0 issues.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; cd app_cliente; flutter analyze; flutter test test/pagos/ test/pedidos/ -q</automated>
  </verify>
  <acceptance_criteria>
  - flutter analyze 0 issues; flutter test test/pagos/ verde (≥4 tests nuevos) y test/pedidos/ sin regresiones (cuenta_test actualizado si cambió el widget — el botón Pagar solo aparece cuando yaPedida)
  - El launcher inyectado recibe la URL absoluta (assert del spy: empieza por Env.apiBaseUrl cuando el backend devolvió relativa)
  - Ningún lugar de la UI marca un pago como aprobado sin pasar por GET /cliente/pagos/{id}
  - build_runner corrió sin errores (pago.freezed.dart/pago.g.dart generados)
  </acceptance_criteria>
  <done>El cliente puede iniciar el pago desde la pantalla de pedidos, el checkout se abre en el navegador, y la app refleja éxito/rechazo por polling sin intervención manual.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CalificacionSheet post-pago + rating real en restaurantes + tests + e2e</name>
  <files>app_cliente/lib/features/pagos/calificacion_sheet.dart, app_cliente/lib/features/pagos/pago_screen.dart, app_cliente/lib/models/restaurante.dart, app_cliente/lib/models/restaurante_detalle.dart, app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart, app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart, app_cliente/test/pagos/calificacion_sheet_test.dart, app_cliente/test/restaurantes/list_test.dart</files>
  <read_first>app_cliente/lib/models/restaurante.dart (calificacionLabel), app_cliente/lib/models/restaurante_detalle.dart, app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart (_RestauranteCard fila ⭐), app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart (dónde mostrar el rating), app_cliente/test/restaurantes/list_test.dart (tests existentes), 09-RESEARCH.md §Patrón 6 (estrellas custom + rating lista)</read_first>
  <behavior>
  - Test: sheet renderiza 5 IconButtons (star_border); tap en la 4ta → 4 estrellas llenas (Icons.star), valor interno 4
  - Test: botón "Enviar" deshabilitado con 0 estrellas; habilitado tras tap; tap Enviar llama FakeApi.crearCalificacion(pedidoId correcto, estrellas 4, comentario) y cierra el sheet + SnackBar de gracias
  - Test: tarjeta de restaurante con calificacion 4.8 + total_calificaciones 245 → muestra "⭐ 4.8 (245)"; con total 0 → "⭐ —"
  </behavior>
  <action>
  1. **models**: restaurante.dart añade @JsonKey(name: 'total_calificaciones') int totalCalificaciones con default 0 (@Default(0)); extender calificacionLabel o nuevo getter ratingLabel → "4.8 (245)" cuando totalCalificaciones > 0. restaurante_detalle.dart hereda/comporta igual (si RestauranteDetalle del Flutter es modelo aparte, añadir el campo también). Correr build_runner (regen freezed/json de ambos).
  2. **calificacion_sheet.dart**: StatelessWidget/Consumer con parametros pedidoId + onEnviada callback. Row(mainAxisSize: min) de 5 IconButtons — icon: Icons.star si índice <= valor sino Icons.star_border, color ámbar Color(0xFFF5A623), tap setea valor (setState via StatefulBuilder o convertir a StatefulWidget pequeño). TextField comentario (multiline, maxLines 3, hint "Cuéntanos cómo fue todo (opcional)"). Botón "Enviar calificación" deshabilitado hasta estrellas > 0 → ref.read(apiClientProvider).crearCalificacion(pedidoId, estrellas, comentario: vacío→null) → Navigator.pop + SnackBar "¡Gracias por calificar! 🙌" (errores → SnackBar con detail; 409 ya calificado → mensaje específico "Este pedido ya fue calificado").
  3. **pago_screen.dart**: en la vista de éxito, CTA "Califica tu experiencia ⭐" → showModalBottomSheet con CalificacionSheet(pedidoId: último de pagoEstado.pedidoIds). El CTA permanece disponible aunque se descarte el sheet (calificar no es obligatorio).
  4. **ApiClient**: Future<void> crearCalificacion(int pedidoId, int estrellas, {String? comentario}) → POST /cliente/calificaciones con body {pedido_id, estrellas, if comentario != null comentario}.
  5. **restaurantes_list_screen.dart**: la fila ⭐ pasa de calificacionLabel a ratingLabel (mantiene "—" cuando no hay datos). **restaurante_detalle_screen.dart**: añadir la misma fila ⭐ ratingLabel bajo el nombre del restaurante (CALI-02 lista Y detalle).
  6. **tests**: calificacion_sheet_test.dart (2 bloques) + extender test/restaurantes/list_test.dart (bloque 3: mock de Restaurante con total>0 y con 0 — actualizar los tests existentes que construyan Restaurante si el nuevo campo rompe su constructor... con @Default(0) los constructores existentes siguen compilando). flutter analyze 0. Correr suite COMPLETA del cliente (41 + ~10 nuevos).
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; cd app_cliente; flutter analyze; flutter test -q (suite completa)</automated>
  </verify>
  <acceptance_criteria>
  - flutter test suite completa verde (baseline 41 + nuevos); analyze 0
  - Sheet: estrellas custom sin dependencias nuevas (Row de IconButtons), enviar deshabilitado sin selección
  - Lista Y detalle de restaurantes muestran el rating real; "—" cuando total_calificaciones == 0
  - Restaurante.detalle model regenerado con total_calificaciones (@Default(0) — compatibilidad con tests existentes)
  </acceptance_criteria>
  <done>El ciclo post-pago está completo en UI: pagar → éxito → calificar → el promedio aparece en discover. Toda la suite del cliente verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint e2e sandbox — pago → éxito → calificación → rating en Chrome</name>
  <files>(sin archivos — verificación humana del flujo ya automatizado)</files>
  <action>Todo lo automatizable ya está verificado por los tests de Task 1/2 (montos, launcher, polling, sheet, rating). Este checkpoint es el pase visual final del flujo completo contra el stack dev real (backend 09-01/09-02 incluido): botón Pagar → checkout sandbox en el navegador → aprobar → éxito en la app → calificar 5★ → promedio visible en la lista de restaurantes. El ejecutor prepara el entorno (stack arriba + app corriendo) y entrega los pasos al usuario.</action>
  <what-built>Flujo completo de pago sandbox + calificación + rating en la app cliente web, contra el stack dev real (backend 09-01/09-02 incluidos): botón Pagar → checkout sandbox en el navegador → aprobar → éxito en la app → calificar 5★ → promedio visible en la lista de restaurantes.</what-built>
  <how-to-verify>
  Preparación (el ejecutor deja listo y verifica automaticamente lo automatizable; esto es el pase visual final):
  1. Stack dev arriba: docker compose up -d (API en http://localhost:8000)
  2. Lanzar la app: cd app_cliente; $env:Path += ";C:\src\flutter\bin"; flutter run -d chrome --web-port 5174
  3. Registrarse/ingresar como cliente nuevo
  4. Abrir sesión: Escanear → input manual con el código de una mesa DISPONIBLE (verde) del panel admin (http://localhost:5173, cocina@demo.gri.dev / Demo!1234) — o crear una mesa nueva desde el panel
  5. Agregar 2 productos al carrito y enviar el pedido
  6. Avanzar el pedido a servido: panel admin → 📋 Pedidos → Aceptar → En preparación → Marcar servido (o curl con token de cocina@demo.gri.dev: POST /staff/pedidos/{id}/estado con {"estado": "aceptado"}, luego "en_preparacion", luego "servido")
  7. En la app: Mis pedidos → "Pedir la cuenta" → aparece "Pagar en línea 💳" → tap
  8. Se abre el checkout sandbox (localhost:8000/pagos/sandbox/...) con el total correcto → botón "Aprobar"
  9. Volver a la app: en ≤3s debe mostrar "✅ Pago aprobado" con el monto
  10. Tap "Califica tu experiencia ⭐" → 5 estrellas + comentario → Enviar → SnackBar de gracias
  11. Tab Restaurantes: la tarjeta del restaurante demo muestra "⭐ X.X (1)"
  Resultado esperado: TODOS los pasos sin errores; el pedido desaparece de la cola del panel (pagado); el mapa muestra la mesa en limpieza (azul).
  </how-to-verify>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; cd app_cliente; flutter test -q (suite completa verde previa al checkpoint humano)</automated>
  </verify>
  <done>Usuario escribe "approved" confirmando el flujo e2e completo: pagar → aprobar sandbox → éxito en app → calificar → rating visible.</done>
  <resume-signal>Escribe "approved" o describe qué paso falló y qué viste</resume-signal>
</task>

</tasks>

<verification>
- flutter analyze 0 issues; flutter test suite completa verde (41 + ~10 nuevos)
- El checkpoint humano valida el e2e sandbox completo (pago → efectos → calificación → rating)
- Ninguna mutación local de estado de pago fuera de GET /cliente/pagos/{id}
</verification>

<success_criteria>
- PAGO-02 (UI): botón Pagar abre checkout real y el éxito se refleja por polling
- CALI-01 (UI): sheet de 5 estrellas + comentario tras pago aprobado, con pedido_id correcto
- CALI-02 (UI): lista y detalle muestran promedio real; "—" sin datos
- Checkpoint humano approved
</success_criteria>

<output>
After completion, create `.planning/phases/09-pagos-calificaciones-y-deploy/09-03-SUMMARY.md`
</output>
