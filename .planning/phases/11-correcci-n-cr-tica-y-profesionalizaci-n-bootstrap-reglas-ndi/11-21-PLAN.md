---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 21
type: execute
wave: 6
depends_on: ["11-10", "11-11"]
files_modified:
  - panel_admin/lib/core/gri_icons.dart
  - panel_admin/lib/features/shared/app_shell.dart
  - panel_admin/lib/features/shared/responsive_page.dart
  - panel_admin/lib/features/dashboard/widgets/stat_card.dart
  - panel_admin/lib/features/dashboard/dashboard_screen.dart
  - panel_admin/lib/features/mesas/mesas_screen.dart
  - panel_admin/lib/features/menu/menu_screen.dart
  - panel_admin/lib/features/reservas/reservas_screen.dart
  - panel_admin/lib/features/cocina/cocina_screen.dart
  - panel_admin/lib/features/clientes/clientes_screen.dart
  - panel_admin/lib/features/reportes/reportes_screen.dart
  - panel_admin/lib/features/configuracion/configuracion_screen.dart
  - panel_admin/lib/features/equipo/equipo_screen.dart
  - panel_admin/lib/features/**
  - panel_admin/test/shared/responsive_test.dart
  - panel_admin/test/reservas/reservas_screen_test.dart
  - panel_admin/test/dashboard/stats_render_test.dart
  - panel_admin/test/bootstrap/bootstrap_router_test.dart
  - panel_admin/test/core/sin_emojis_test.dart
  - docs/ICONOS-panel_admin.md
autonomous: true
requirements: [DS-02, DS-03]

must_haves:
  truths:
    - "El sidebar del panel deja de desbordar 85px en el logo y 33px en los items del menú"
    - "Las stat cards no desbordan cuando el grid pasa a 4 columnas en pantallas anchas"
    - "Todas las pantallas del panel se adaptan al viewport y el contenido no se estira de borde a borde"
    - "Ningún test del repo sigue filtrando excepciones de overflow para poder pasar"
    - "Ningún emoji se usa como icono de interfaz en el panel"
  artifacts:
    - path: "panel_admin/lib/features/shared/responsive_page.dart"
      provides: "Envoltorio de página con ancho máximo y breakpoints compartidos"
      min_lines: 30
    - path: "panel_admin/lib/core/gri_icons.dart"
      provides: "Mapa único de iconos semánticos del panel"
      min_lines: 25
    - path: "panel_admin/test/core/sin_emojis_test.dart"
      provides: "Gate contra la reaparición de emojis como iconografía"
      min_lines: 25
    - path: "docs/ICONOS-panel_admin.md"
      provides: "Tabla emoji → Icons.* revisable, con archivo:línea"
      min_lines: 30
  key_links:
    - from: "panel_admin/lib/features/**"
      to: "GriBreakpoints"
      via: "LayoutBuilder con los breakpoints nombrados"
      pattern: "GriBreakpoints"
    - from: "panel_admin/lib/features/shared/app_shell.dart"
      to: "GriIcons"
      via: "items del sidebar con Icon en lugar de Text(emoji)"
      pattern: "Icon\\("
---

<objective>
Arreglar TODO lo visible del panel admin en un solo plan: los tres desbordamientos conocidos, el
responsive de las pantallas que faltan y la sustitución de sus ~47 emojis por iconos de Material.

Purpose: es exactamente aquello de lo que se quejó el usuario —"se ve todo muy desordenado", "hay
errores visuales"— y hoy es visible en producción: el sidebar desborda 85px **a cualquier ancho**.
Este plan y su gemelo del cliente (**11-13**) se cortaron **por app** precisamente para que puedan
ejecutarse **en paralelo**: no comparten ni un archivo.
Output: sidebar y `StatCard` corregidos, `ResponsivePage` aplicado, filtros de overflow retirados,
`GriIcons` del panel, gate anti-emojis y `docs/ICONOS-panel_admin.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-CONTEXT.md
@.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/deferred-items.md
@.planning/codebase/audit/UI-REVIEW-panel_admin.md
@panel_admin/lib/features/shared/app_shell.dart
@panel_admin/lib/features/dashboard/dashboard_screen.dart
@panel_admin/lib/features/dashboard/widgets/stat_card.dart
@panel_admin/lib/features/reservas/reservas_screen.dart
@panel_admin/lib/core/design_tokens.dart
</context>

<interfaces>
<!-- Los tres desbordamientos del panel, con su diagnóstico numérico ya hecho. -->

| # | Dónde | Cuánto | Cuándo |
|---|---|---|---|
| 1 | `shared/app_shell.dart:171` (logo del sidebar) | **85px a la derecha** | **a CUALQUIER ancho** |
| 2 | `shared/app_shell.dart:264` (item del menú) | **33px a la derecha** | a cualquier ancho |
| 3 | `dashboard/widgets/stat_card.dart:50` | **31px abajo** | viewport >= 1100px (grid a 4 columnas) |
| 4 | `reservas/reservas_screen.dart:181-227` (`_ReservaCard`) | variable | ~650px de contenido |

**#1 y #2:** el sidebar es un `Container(width: 220)` con `padding` horizontal de 15 → 190px útiles.
El `Row` del logo mete un badge de 45px + 12px de hueco + un `Column` con `GRI` y
`Gestión de Restaurante`, que a 14px pide ~248px sin restricción de ancho. Arreglo: envolver el
`Column` de textos en `Expanded` (y el `Text(label)` del item de menú igual), con
`overflow: TextOverflow.ellipsis` donde haga falta. **Es un defecto real visible en producción hoy**,
no un artefacto de test: no depende del viewport. Lo detectó `bootstrap_router_test.dart` (plan 11-07),
el primer test del repo que renderiza el `AppShell` completo — los tests del dashboard pumpean la
pantalla suelta, así que nunca se había ejercitado.

**#3:** con `maxWidth >= 1100` el grid pasa a 4 columnas (`dashboard_screen.dart:37-39`) y
`childAspectRatio: 2.6` deja 59.8px de alto para ~91px de contenido en el `Column` de `stat_card.dart:50`.

**#4:** un único `Row` con hora (fontSize 20 bold) + `Expanded` + chip + DOS botones sin restringir.

**Deuda de test que este plan DEBE saldar** (si no, esos filtros seguirán ocultando regresiones):
- `panel_admin/test/bootstrap/bootstrap_router_test.dart:53` filtra las excepciones
  `A RenderFlex overflowed`, con el motivo documentado en el propio archivo. **Retirar el filtro** al
  arreglar el sidebar; el test debe seguir pasando sin él.
- Los tests de dashboard del plan 11-02 evitan el problema pumpeando a **800px** (camino de 1 columna)
  en lugar de a 1200px, con un comentario que lo explica. **Añadir un caso a 1200px**, que es el que
  reproduce el desbordamiento de `StatCard`, y actualizar ese comentario.

**Responsive.** Hoy solo 3 de las pantallas adaptan (`dashboard_screen`, `mesas_screen`, `app_shell`);
`MediaQuery` no aparece en `lib/`. En un monitor de 1920 quedan ~1650px de contenido y las tarjetas se
estiran enteras, que es justo lo que se lee como "desordenado".
Breakpoints: usar los ya nombrados en `GriBreakpoints` (plan 11-11) — **750 y 1100**, los valores hoy
vigentes en `app_shell.dart:72` y `dashboard_screen.dart:37`. NO migrar a los 600/840/1200 de Material
3: desplazaría el punto de colapso del sidebar y del grid, que es un cambio visual prohibido.
`LayoutBuilder` y no `MediaQuery`: `MediaQuery` da el tamaño de la VENTANA, y dentro de un shell con
sidebar de 220px eso es el número equivocado.

**Emojis → iconos. DESBLOQUEADO por el usuario** (11-CONTEXT.md, 2026-08-19: *"podemos cambiar emoji
por iconos si no se ha hecho"*). Esto **revierte** la interpretación con la que se planificó la fase.
Inventario real contado en el árbol: **47 apariciones en `panel_admin`**.

⚠️ **No todas son iconografía.** Tres usos, y solo se sustituye el primero:
1. **Icono de interfaz**: el glifo ES el icono de un item del sidebar, una stat-card, una acción o un
   estado vacío.
2. **Comentario o documentación** en el código. **No se tocan.**
3. **Caso de prueba o texto expresivo**: los `★★★` de `features/configuracion/slug.dart` son la entrada
   de un caso de slug inválido (`"★★★"` → slug vacío) y **no se tocan**; el `🎉` del estado vacío de
   cocina es expresivo y se decide caso a caso.

Equivalencias propuestas para esta app (el ejecutor puede ajustar, dejando constancia):
| Emoji | Dónde | Icono |
|---|---|---|
| 🏠 | sidebar Dashboard | `Icons.home_outlined` |
| 🪑 | sidebar Mesas, mesa | `Icons.table_restaurant_outlined` |
| 📋 | sidebar Pedidos, cola de cocina | `Icons.receipt_long_outlined` |
| 📅 | sidebar Reservas, fechas | `Icons.calendar_today_outlined` |
| 👥 | sidebar Clientes, personas | `Icons.people_outline` |
| 📊 | sidebar Reportes | `Icons.bar_chart` |
| ⚙ | sidebar Configuración | `Icons.settings_outlined` |
| 🍽 | logo de marca | `Icons.restaurant` |
| 🏪 | restaurante | `Icons.storefront_outlined` |
| 📷 | ver QR de mesa | `Icons.qr_code_2` |
| ✏ | editar | `Icons.edit_outlined` |
| 🖨 | imprimir QR | `Icons.print_outlined` |
| 📝 | notas del pedido | `Icons.edit_note` |
| 💵 | ventas en reportes | `Icons.payments_outlined` |
| 🧾 | ticket / ticket medio | `Icons.receipt_outlined` |
| ⚠ | aviso EN UI (no en comentarios) | `Icons.warning_amber_outlined` |
| 👇 | indicación "abajo" | eliminar el glifo; el texto basta |
| 🎉 | estado vacío de cocina | evaluar: conservar o `Icons.celebration_outlined` |

**Lo que NO cambia:** paleta `#FF4C05`, layouts, anchos de contenedores y orden de los elementos.
El `size` del `Icon` debe **igualar el `fontSize` del `Text` que sustituye**, y el color debe salir de
`GriColors`/`IconTheme`, nunca de un literal.

⚠️ **Este plan NO toca `app_cliente`.** Todo lo del cliente (SafeArea, `maxWidth`, wizard, sus iconos)
es el plan **11-13**, que corre en la MISMA ola. Tocar el cliente desde aquí rompería el paralelismo.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Sidebar y StatCard, y retirada de los filtros de overflow</name>
  <files>panel_admin/lib/features/shared/app_shell.dart, panel_admin/lib/features/dashboard/widgets/stat_card.dart, panel_admin/lib/features/dashboard/dashboard_screen.dart, panel_admin/test/dashboard/stats_render_test.dart, panel_admin/test/bootstrap/bootstrap_router_test.dart</files>
  <behavior>
    - `AppShell` renderizado con sesión, a 800px y a 1920px, no produce ninguna excepción de RenderFlex:
      ni el logo del sidebar ni los items del menú desbordan.
    - Con el sidebar colapsado (< 750px) el comportamiento es el de hoy: solo icono, 70px de ancho.
    - Con el sidebar expandido, los textos `GRI` y `Gestión de Restaurante` siguen visibles y legibles
      (recortados con ellipsis solo si no caben).
    - El dashboard pumpeado a **1200px** de ancho (grid a 4 columnas) no produce ninguna excepción de
      RenderFlex: `StatCard` cabe en el alto que le da su `childAspectRatio`.
    - A 1280px y 1440px el número de columnas del grid es el mismo que antes del cambio.
    - `bootstrap_router_test.dart` pasa **sin** el filtro de excepciones de overflow.
    - `grep -rn "RenderFlex overflowed" panel_admin/test` no devuelve ningún filtro activo.
  </behavior>
  <action>
**Desbordamientos #1 y #2 — sidebar.** Envolver en `Expanded` el `Column` de textos del `Row` del logo
(`app_shell.dart:171`) y el `Text(label)` del item de menú (`:264`), con
`overflow: TextOverflow.ellipsis` donde haga falta. No cambiar el ancho del sidebar (220px), ni el
tamaño del badge, ni la tipografía: el objetivo es que el contenido quepa, no rediseñar.

**Desbordamiento #3 — `StatCard`.** Arreglar por el lado del contenido o del ratio, lo que menos altere
el aspecto a los anchos habituales: bajar el `childAspectRatio` para el tramo de 4 columnas, o hacer
que el `Column` de la card se ajuste (`mainAxisSize`, `Flexible`, tipografía del `textTheme` en vez de
tamaños fijos). PROHIBIDO cambiar colores o el orden de los elementos.

**Retirada de los filtros — parte de la definición de hecho, no opcional.**
1. Quitar de `panel_admin/test/bootstrap/bootstrap_router_test.dart:53` el filtro de excepciones
   `A RenderFlex overflowed` (y el comentario que lo justificaba), y comprobar que el test sigue verde.
2. Añadir a `panel_admin/test/dashboard/stats_render_test.dart` un caso a **1200px** de ancho — hoy los
   tests pumpean a 800px justamente para esquivar el desbordamiento— y actualizar el comentario que
   explicaba por qué se evitaba.
Si al retirar el filtro aparece un desbordamiento distinto de los arreglados aquí, NO volver a poner el
filtro: arreglarlo o, si queda fuera de alcance, anotarlo en `deferred-items.md` con la misma
disciplina y dejarlo visible en el SUMMARY.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test test/bootstrap/ test/dashboard/</automated>
    <automated>cd panel_admin &amp;&amp; test $(grep -rn "RenderFlex overflowed" test | wc -l) -eq 0 &amp;&amp; echo "SIN_FILTROS"</automated>
  </verify>
  <done>Los dos desbordamientos del sidebar y el de `StatCard` están cerrados con test de regresión, y ningún test del panel filtra ya excepciones de overflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: ResponsivePage en todas las pantallas y overflow de reservas</name>
  <files>panel_admin/lib/features/shared/responsive_page.dart, panel_admin/lib/features/menu/menu_screen.dart, panel_admin/lib/features/reservas/reservas_screen.dart, panel_admin/lib/features/cocina/cocina_screen.dart, panel_admin/lib/features/clientes/clientes_screen.dart, panel_admin/lib/features/reportes/reportes_screen.dart, panel_admin/lib/features/configuracion/configuracion_screen.dart, panel_admin/lib/features/equipo/equipo_screen.dart, panel_admin/lib/features/mesas/mesas_screen.dart, panel_admin/test/shared/responsive_test.dart, panel_admin/test/reservas/reservas_screen_test.dart</files>
  <behavior>
    - `ResponsivePage` limita su hijo a 1200px de ancho y lo centra cuando hay más espacio.
    - A 1920px de ancho de contenido, cada pantalla de la lista real (incluida `equipo_screen`)
      renderiza su contenido dentro de un ancho <= 1200.
    - A 900px, el contenido ocupa el ancho disponible sin recortes.
    - Ninguna pantalla cambia su padding vertical ni su tipografía respecto al estado anterior.
    - `_ReservaCard` con nombre de mesa largo, chip y los dos botones, a 650px de ancho de contenido:
      sin excepción de RenderFlex; los botones pasan a una segunda fila o el cluster se envuelve.
    - A 1200px, `_ReservaCard` conserva exactamente la disposición actual de una sola fila.
    - El gate de cobertura compara las pantallas adaptativas contra el TOTAL REAL de pantallas del
      árbol, no contra una constante escrita a mano.
  </behavior>
  <action>
Crear `panel_admin/lib/features/shared/responsive_page.dart` con
`ResponsivePage({required Widget Function(BuildContext, double ancho) builder, double maxWidth = 1200, EdgeInsets? padding})`
que envuelva en `LayoutBuilder` + `Center` + `ConstrainedBox`, usando `GriBreakpoints` para exponer
banderas útiles (`esCompacto`, `esExpandido`). Documentar por qué `LayoutBuilder` y no `MediaQuery`.

Aplicarlo a TODAS las pantallas que no adaptan. Al iniciar la tarea, obtener la lista real con
`ls panel_admin/lib/features/*/*_screen.dart` en lugar de fiarse de una cuenta fija: además de las que
detectó la auditoría (`menu_screen`, `reservas_screen`, `cocina_screen`, `clientes_screen`,
`reportes_screen`, `configuracion_screen`), esta fase añadió `equipo_screen.dart` (plan 11-10) y
`bootstrap_screen.dart` (plan 11-07, que ya nace con `ConstrainedBox(maxWidth: 400)` propio y puede
quedar exenta si se documenta). Conservar el padding exacto que cada pantalla usa hoy (30 vs 24 vs
`fromLTRB(24,16,24,4)`): homogeneizarlos sería un cambio visual y está fuera de alcance.

En `dashboard_screen.dart` y `mesas_screen.dart`, sustituir los `GridView.count` de tope 4 columnas por
`GridView.builder` con `SliverGridDelegateWithMaxCrossAxisExtent(maxCrossAxisExtent: 260)` para que en
pantallas ultraanchas aparezca una quinta columna en vez de tarjetas gigantes. Verificar que a 1280 y
1440 el número de columnas coincide con el actual; si no coincide, ajustar `maxCrossAxisExtent` hasta
que coincida — "igual en los anchos de hoy, mejor en los anchos grandes".

**Overflow #4 — `_ReservaCard`** (`reservas_screen.dart:181-227`): envolver el grupo chip + botones en
un `Wrap(spacing: GriSpacing.sm, runSpacing: GriSpacing.sm)` o, si eso alterase el aspecto en anchos
grandes, usar el `LayoutBuilder` de `ResponsivePage` para bajar los botones a una segunda fila por
debajo de 900px. Elegir la opción que conserve EXACTAMENTE el aspecto actual a anchos de escritorio;
comprobarlo con el test de 1200px.

Crear `panel_admin/test/shared/responsive_test.dart` con los casos de `<behavior>`, pumpeando cada
pantalla a 900 y 1920px con los providers ya fakeados del repo. Ampliar
`panel_admin/test/reservas/reservas_screen_test.dart` con los casos de `_ReservaCard`, montando la
pantalla dentro de un `SizedBox` de ancho controlado.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test test/shared/responsive_test.dart test/reservas/</automated>
    <automated>cd panel_admin &amp;&amp; TOT=$(ls lib/features/*/*_screen.dart | wc -l); ADAPT=$(grep -rln "LayoutBuilder\|ResponsivePage" lib/features --include=*_screen.dart | wc -l); echo "adaptan $ADAPT de $TOT"; test "$ADAPT" -eq "$TOT"</automated>
  </verify>
  <done>Todas las pantallas del panel adaptan (contadas del árbol real), el contenido tiene techo de 1200px en monitores anchos, los grids son fluidos sin cambiar columnas a los anchos habituales y el overflow de reservas está cerrado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Emojis a iconos de Material en el panel</name>
  <files>panel_admin/lib/core/gri_icons.dart, panel_admin/lib/features/**, panel_admin/test/core/sin_emojis_test.dart, docs/ICONOS-panel_admin.md</files>
  <behavior>
    - Los 7 items del sidebar renderizan `Icon` y conservan sus etiquetas y su ruta.
    - Con el sidebar colapsado (< 750px) los items siguen mostrando solo el icono, ahora un `Icon` real.
    - Las stat-cards del dashboard, las acciones de mesa, el botón de imprimir QR y los estados vacíos
      usan `Icon` en lugar de emoji.
    - Ninguna pantalla de `panel_admin/lib/features/**` contiene un emoji usado como icono.
    - Los `★★★` de `configuracion/slug.dart` (caso de prueba de slug inválido) siguen intactos.
    - Los tests existentes que localizaban elementos por emoji los encuentran ahora por `find.byIcon`.
    - `sin_emojis_test.dart` falla si se introduce un emoji nuevo en un literal renderizado sin exención.
  </behavior>
  <action>
Crear `panel_admin/lib/core/gri_icons.dart` con `abstract final class GriIcons`, nombrando cada icono
por su SIGNIFICADO y no por su forma: `dashboard`, `mesas`, `pedidos`, `reservas`, `clientes`,
`reportes`, `configuracion`, `equipo`, `restaurante`, `marca`, `verQr`, `imprimir`, `editar`, `notas`,
`ventas`, `ticket`, `aviso`. Cabecera con comentario de sincronización nombrando el gemelo de
`app_cliente` (`app_cliente/lib/core/gri_icons.dart`, plan 11-13), como ya hacen `design_tokens.dart` y
`password_field.dart`.

Inventariar antes de tocar nada y guardar la lista en el SUMMARY:
`grep -rnP "[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}]" panel_admin/lib --include=*.dart`
(deben salir ~47). Clasificar cada aparición en los tres usos del bloque `<interfaces>` y sustituir
SOLO las del uso 1. Ojo con los dos falsos positivos ya verificados: los `★` de
`features/configuracion/slug.dart` y los `⚠` de comentarios de aviso.

Sustituir `Text(emoji)` por `Icon(GriIcons.x, size: <el mismo fontSize que tenía>, color: <el mismo>)`.
Empezar por `features/shared/app_shell.dart` (los 7 items del sidebar, partiendo del resultado de la
Tarea 1), que es donde más se nota.

Actualizar los tests que buscan por emoji a `find.byIcon`: cambio de finder, no de aserción.

Crear `panel_admin/test/core/sin_emojis_test.dart`: test puro con `dart:io` que recorre `lib/**/*.dart`,
ignora líneas de comentario (`//`, `///`, `/*`) y falla si encuentra un emoji dentro de un literal de
cadena que se renderiza, imprimiendo archivo, línea y glifo. Exención explícita con
`// EMOJI-OK: motivo` en la misma línea (los `★★★` de `slug.dart` la usarán).

Escribir `docs/ICONOS-panel_admin.md` con la tabla REAL resultante: una fila por sustitución con
`emoji | archivo:línea | Icons.* elegido | nombre en GriIcons`; una sección **Conservados** con los que
NO se sustituyeron y por qué; y una nota de **cómo añadir un icono nuevo** (siempre por `GriIcons`,
nunca un `Icons.x` suelto, y con `semanticLabel`, que es lo que consume el plan 11-14). El gemelo del
cliente es `docs/ICONOS-app_cliente.md` (plan 11-13): se documentan por separado porque los dos planes
corren en paralelo y no pueden escribir el mismo archivo; enlazarlos mutuamente en la cabecera.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
    <automated>cd panel_admin &amp;&amp; flutter test test/core/sin_emojis_test.dart</automated>
    <automated>test $(grep -c "^|" docs/ICONOS-panel_admin.md) -ge 10 &amp;&amp; grep -q "Conservados" docs/ICONOS-panel_admin.md &amp;&amp; grep -q "GriIcons" docs/ICONOS-panel_admin.md &amp;&amp; echo "TABLA_ICONOS_OK"</automated>
  </verify>
  <done>El panel usa iconos de Material en sidebar, dashboard, acciones y estados vacíos, sin tocar layouts ni paleta, con la equivalencia documentada y un gate anti-regresión.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna | Plan de layout e iconografía; no toca autorización, datos ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-21-01 | Denial of Service | RenderFlex overflow visible al usuario | mitigate | Los cuatro desbordamientos del panel quedan cerrados con test de regresión: shell completo con sesión a 800 y 1920px, dashboard a 1200px, y `_ReservaCard` a 650px |
| T-11-21-02 | Repudiation | filtros de excepciones que ocultan regresiones futuras | mitigate | Retirar el filtro de `bootstrap_router_test.dart` y añadir el caso a 1200px forma parte de la definición de hecho; un `grep` en la verificación comprueba que no queda ninguno |
| T-11-21-03 | Tampering | cambio del punto de colapso del sidebar o de las columnas del grid | mitigate | Se reutilizan los breakpoints 750/1100 ya vigentes; el test comprueba que el número de columnas a 1280 y 1440 no cambia |
| T-11-21-04 | Tampering | pérdida de significado al mapear emoji → icono | mitigate | Tabla propuesta en el plan y tabla real en `docs/ICONOS-panel_admin.md` con archivo:línea y sección de conservados; corregible en un solo sitio (`GriIcons`) |
| T-11-21-05 | Tampering | deriva de tamaño o color al sustituir emojis | mitigate | Regla explícita: el `size` del `Icon` iguala el `fontSize` del `Text` sustituido y el color sale de `GriColors`; layouts y orden intactos |
| T-11-21-06 | Denial of Service | tests que localizaban widgets por su emoji | mitigate | Actualización de finders a `find.byIcon` en el mismo commit, sin cambiar aserciones de comportamiento |
</threat_model>

<verification>
- `cd panel_admin && flutter test && flutter analyze` → verde, 0 issues.
- `grep -rn "RenderFlex overflowed" panel_admin/test` → sin resultados (ningún filtro activo).
- El gate de cobertura responsive compara `ADAPT` contra `TOT` calculados del árbol, no contra un número escrito a mano.
- `docs/ICONOS-panel_admin.md` cubre todas las sustituciones y los conservados.
- Este plan NO debe tocar ni un archivo de `app_cliente/`: `git diff --name-only` no puede mostrar ninguno.
</verification>

<success_criteria>
- El defecto más visible del panel en producción (sidebar desbordado a cualquier ancho) queda cerrado.
- Todas las pantallas adaptan y el contenido deja de estirarse de borde a borde en monitores anchos.
- La interfaz del panel deja de depender de glifos que cambian según el sistema operativo.
- El plan del cliente (11-13) puede correr a la vez sin ningún conflicto de archivos.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-21-SUMMARY.md` al terminar.
</output>
