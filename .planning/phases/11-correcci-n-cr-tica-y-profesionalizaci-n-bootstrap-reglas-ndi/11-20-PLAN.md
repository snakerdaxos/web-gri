---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 20
type: execute
wave: 13
depends_on: ["11-16"]
files_modified:
  - docs/DEPLOY-FASE-11.md
  - docs/SMOKE-E2E-v2.md
autonomous: false
requirements: [E2E-02]
user_setup:
  - service: firebase-blaze
    why: "Cloud Functions solo se pueden DESPLEGAR con el plan Blaze. El usuario YA DECIDIÓ activarlo (decisión LOCKED del 2026-08-19): sin funciones, el alta de staff y el bootstrap se quedan en el repo y la plataforma seguiría dependiendo de scripts"
    env_vars:
      - name: BOOTSTRAP_EMAIL
        source: "YA MATERIALIZADO en functions/.env (gitignored). Es la cuenta personal del usuario. No hay nada que decidir"
      - name: BOOTSTRAP_SECRET
        source: "YA MATERIALIZADO en functions/.env (gitignored), 48 bytes aleatorios. PROHIBIDO copiar su VALOR a ningún archivo versionado, SUMMARY, STATE o log: referenciarlo siempre por su ubicación"
    dashboard_config:
      - task: "Subir el proyecto p-gri-b5b40 al plan Blaze (pago por uso). DECISIÓN YA TOMADA por el usuario; queda la ejecución en consola"
        location: "Firebase Console → ⚙ Configuración del proyecto → Uso y facturación → Detalles y configuración"
      - task: "Fijar una alerta de presupuesto al activar Blaze (recomendado por el usuario en la misma decisión)"
        location: "Firebase Console → Uso y facturación → Presupuestos y alertas"
      - task: "Verificar el correo de BOOTSTRAP_EMAIL tras registrar la cuenta (la función exige email_verified)"
        location: "Bandeja de entrada del correo configurado / Firebase Console → Authentication → Usuarios"

must_haves:
  truths:
    - "Las dos Cloud Functions están desplegadas y son invocables desde el panel en producción"
    - "El primer super_admin se crea desde /bootstrap contra el proyecto real y la segunda llamada falla"
    - "Un admin_restaurante da de alta staff en producción y ese staff puede iniciar sesión"
    - "El flujo completo del runbook queda ejecutado de punta a punta"
  artifacts:
    - path: "docs/SMOKE-E2E-v2.md"
      provides: "Runbook ejecutado, con el resultado anotado"
      contains: "bootstrap"
  key_links:
    - from: "panel_admin en producción"
      to: "callables desplegadas en us-central1"
      via: "httpsCallable con la misma región declarada en ambos lados"
      pattern: "us-central1"
---

<objective>
Desplegar las Cloud Functions al proyecto real y ejecutar el flujo completo de punta a punta, cerrando
el checkpoint pendiente desde la Fase 10.

Purpose: **Blaze ya está decidido** (decisión LOCKED del usuario, 2026-08-19: "se activa"), así que
este plan deja de ser una pregunta y pasa a ser una ejecución. Sigue aislado del plan 11-16 por una
razón que se mantiene: la verificación de los bugs P0 de Firestore no necesita Blaze y ya está cerrada
allí, así que un tropiezo en el despliegue de funciones no invalida nada de lo anterior.
Output: funciones desplegadas, bootstrap y alta de staff verificados en producción, y el runbook E2E
ejecutado y anotado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DEPLOY-FASE-11.md
@docs/SMOKE-E2E-v2.md
@.planning/STATE.md
@functions/index.js
</context>

<interfaces>
<!-- Hechos verificados. -->
- Blaze es requisito SOLO del despliegue de funciones. El plan 11-16 ya desplegó y verificó rules e
  índices en Spark.
- Cloud Functions v2 requiere Cloud Build, Artifact Registry, Cloud Run y Eventarc habilitados. El CLI
  los habilita en el primer despliegue, pero puede fallar por permisos.
- Node local 24 vs runtime desplegado 22: el CLI avisará del desajuste. Es esperado y no bloquea.
- Región: `us-central1` declarada en el `onCall` y en `FirebaseFunctions.instanceFor` del panel. Un
  desajuste produce un 404 opaco que en Flutter Web se disfraza de error de CORS.
- `BOOTSTRAP_EMAIL` y `BOOTSTRAP_SECRET` **ya existen** en `functions/.env` (decisión LOCKED del
  usuario, materializada el 2026-08-19; el archivo está gitignored y se verificó que git lo ignora).
  No hay que generarlos ni pedirlos: el runbook los LEE de ahí.
  ⚠️ **REGLA DURA para el ejecutor:** el VALOR del secreto no se escribe en ningún archivo versionado
  —ni en el SUMMARY, ni en STATE.md, ni en la salida de un gate, ni en un mensaje al usuario—. Se
  referencia siempre por su ubicación (`functions/.env`). Si un comando fuera a imprimirlo, redirigir o
  filtrar la salida. La función falla cerrada si faltan, así que su ausencia se detecta sin exponerlos.
- La función exige `email_verified === true`, así que la cuenta del fundador debe verificar su correo
  antes de inicializar. Una cuenta de Google llega ya verificada; una de email/contraseña no.
- El emulador de Auth **no** implementa el flujo real de Google Sign-In: el paso de login con Google
  del runbook solo puede probarse contra el proyecto real.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 1: Checkpoint humano B — Blaze, despliegue de funciones y smoke E2E completo</name>
  <files>docs/DEPLOY-FASE-11.md y docs/SMOKE-E2E-v2.md (lectura y anotación del resultado)</files>
  <action>
Pausar y presentar el checkpoint. NO intentar la subida a Blaze, `firebase login` ni
`firebase deploy --only functions` de forma autónoma: exigen credenciales interactivas y una decisión
de facturación que solo el usuario puede tomar. Presentar `<what-built>`, las instrucciones de
`<how-to-verify>` y esperar el resume-signal.

⚠️ **Nunca imprimir ni transcribir `BOOTSTRAP_SECRET`.** Si un comando del runbook fuera a volcarlo
(por ejemplo un `cat functions/.env`), filtrar la salida. En el SUMMARY se referencia por ubicación.

Si el usuario reporta fallos, recogerlos literalmente (paso y mensaje exacto) para alimentar un plan
de cierre de brechas.
  </action>
  <what-built>
Toda la fase 11 está implementada y verificada contra emuladores, y la parte de Firestore ya está
verificada contra el proyecto real (plan 11-16: rules e índices desplegados, bugs P0 del menú
comprobados en producción).

Lo que queda y por qué requiere tu decisión:
- `bootstrapPlataforma` — crea el primer `super_admin`, una sola vez. Guarda atómica probada con
  llamadas concurrentes, exige correo verificado + secreto de despliegue, y deja rastro auditable.
- `crearUsuarioStaff` — alta de staff con la matriz de roles que decidiste, validada en el servidor:
  nadie puede asignar `super_admin` y un `admin_restaurante` nunca puede tocar otro restaurante.
  Casos unitarios de escalada + pruebas e2e con tokens reales, y la política de contraseñas aplicada
  también en el servidor (plan 11-22).
- `cambiarEstadoStaff` — baja y readmisión reversibles (plan 11-24): deshabilita la cuenta y le retira
  los claims sin borrar nada, con dos prohibiciones nuevas probadas (nadie desactiva a un `super_admin`
  ni a sí mismo).

**Blaze ya está decidido por ti**, así que aquí solo queda ejecutarlo. Recordatorio de por qué el
coste esperado tiende a cero para este uso: son tres funciones invocadas unas pocas veces al día, sin
triggers de Firestore, y Blaze mantiene la misma cuota gratuita de Spark cobrando solo por encima. La
alerta de presupuesto que recomendaste queda como paso del runbook.
  </what-built>
  <how-to-verify>
Sigue la **FASE B** de `docs/DEPLOY-FASE-11.md`. En resumen:

1. **Activa Blaze** (ya lo decidiste): Firebase Console → ⚙ Configuración del proyecto → Uso y
   facturación → Detalles y configuración → cambiar a Blaze. Aprovecha para **fijar una alerta de
   presupuesto**, que es lo que tú mismo recomendaste al decidirlo.
2. `functions/.env` **ya está creado** con `BOOTSTRAP_EMAIL` y `BOOTSTRAP_SECRET`, y git lo ignora.
   No hace falta que hagas nada aquí; solo tenlo a mano para el paso 4, donde pegarás el secreto en la
   pantalla. No lo copies a ningún otro sitio.
3. Desde `scripts/`: `firebase deploy --only functions`.
   El primer despliegue puede pedir habilitar APIs de Google Cloud (Cloud Build, Artifact Registry,
   Cloud Run, Eventarc). El CLI lo hace solo; si falla por permisos, el runbook explica qué mirar.
   Verás un aviso de que tu Node local (24) no coincide con el runtime (22): es esperado.
4. Verifica contra el proyecto REAL, con el panel apuntando a producción (sin `--dart-define=USE_EMULATORS=true`):
   a. Regístrate en `/bootstrap` con el correo que está en `BOOTSTRAP_EMAIL` y **verifica ese correo**
      antes de pulsar (la función exige `email_verified`). Pega el secreto tal cual está en
      `functions/.env`. Debe crear tu `super_admin` y llevarte al panel ya con ese rol.
   b. Vuelve a intentar `/bootstrap` con otra cuenta: debe fallar.
   c. Crea un restaurante, y desde `/equipo` da de alta un `admin_restaurante`. Cierra sesión, entra
      con esa cuenta: debe ver su restaurante y poder crear un mesero de SU restaurante.
   d. Con esa cuenta de admin, intenta crear staff de otro restaurante: debe rechazarlo.
   e. Da de baja a ese mesero desde `/equipo` y comprueba que ya no puede iniciar sesión; reactívalo y
      comprueba que vuelve a entrar con su rol. Intenta desactivarte a ti mismo: debe rechazarlo.
   f. Prueba una contraseña como `12345678` al dar de alta: debe rechazarla diciendo qué le falta.
5. Ejecuta `docs/SMOKE-E2E-v2.md` completo, partiendo de base vacía. Incluye el paso de login con
   Google en la app cliente, que **solo se puede probar contra el proyecto real** (el emulador de Auth
   no implementa el flujo de Google).
6. Anota en el propio runbook el resultado de cada paso.

Si algo falla, anota el paso y el mensaje exacto.
  </how-to-verify>
  <verify>
    <automated>cd scripts &amp;&amp; npm run gates</automated>
    <human-check>Funciones desplegadas, bootstrap y alta de staff verificados en p-gri-b5b40, y runbook SMOKE-E2E-v2 ejecutado</human-check>
  </verify>
  <resume-signal>Escribe "approved" si el deploy y el smoke pasaron, o describe qué falló y en qué paso. NO pegues el secreto en tu respuesta</resume-signal>
  <done>El usuario confirma el despliegue y el smoke completo, o declina Blaze de forma explícita (que se registra como decisión, no como fallo), o reporta las fallas para un plan de cierre de brechas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → callables desplegadas | A partir del deploy, las funciones son invocables desde fuera del producto |
| operador → facturación | Blaze abre un coste variable |
| `functions/.env` → secretos de bootstrap | El secreto de inicialización vive en un archivo local no versionado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-20-01 | Elevation of Privilege | ventana entre el deploy de functions y el bootstrap | mitigate | `BOOTSTRAP_EMAIL` y `BOOTSTRAP_SECRET` se configuran ANTES del deploy y la función falla cerrada sin ellos; además exige `email_verified`. No existe ventana en la que un desconocido pueda hacerse super_admin |
| T-11-20-02 | Information Disclosure | `BOOTSTRAP_SECRET` | mitigate | Vive solo en `functions/.env`, excluido por `.gitignore` (verificado). Regla dura en el plan: se referencia por ubicación, nunca por valor, y ningún paso del runbook, SUMMARY, STATE ni salida de gate puede imprimirlo |
| T-11-20-03 | Denial of Service | coste inesperado en Blaze | mitigate | Presupuesto de alerta como paso obligatorio del runbook; se explica al usuario el orden de magnitud esperado y que Blaze mantiene la cuota gratuita |
| T-11-20-04 | Spoofing | callables invocadas por bots | accept | App Check queda DIFERIDO y documentado como deuda de seguridad conocida; mitigación parcial vía `maxInstances` y la matriz de autorización del servidor |
| T-11-20-05 | Repudiation | fase cerrada sin ejecutar el smoke | mitigate | El resume-signal exige "approved" o el detalle del fallo. Blaze ya no es una bifurcación: está decidido |
</threat_model>

<verification>
- `cd scripts && npm run gates` → exit 0 antes de pedir nada al humano.
- Confirmación humana de los cuatro puntos del paso 4 y del runbook completo, o declinación explícita de Blaze.
- `firebase functions:list` muestra `bootstrapPlataforma` y `crearUsuarioStaff` en `us-central1`.
</verification>

<success_criteria>
- El producto es autosuficiente en producción: se arranca desde una pantalla, y el staff se da de alta y se da de baja desde el panel.
- El flujo completo, desde base vacía hasta calificar un pedido, queda ejecutado y anotado.
- El despliegue de funciones queda aislado del de Firestore, así que un tropiezo aquí no invalida la verificación de los bugs P0 ya cerrada en el plan 11-16.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-20-SUMMARY.md` al terminar.
</output>
