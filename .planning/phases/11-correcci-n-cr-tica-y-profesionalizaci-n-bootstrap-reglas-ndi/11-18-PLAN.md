---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 18
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - app_cliente/tool/gen_branding.dart
  - app_cliente/pubspec.yaml
  - app_cliente/assets/branding/
  - app_cliente/web/index.html
  - app_cliente/web/manifest.json
  - app_cliente/web/favicon.png
  - app_cliente/web/icons/
  - app_cliente/android/app/src/main/res/
  - panel_admin/web/index.html
  - panel_admin/web/manifest.json
  - panel_admin/web/favicon.png
  - panel_admin/web/icons/
  - scripts/audit_branding.mjs
  - scripts/package.json
autonomous: true
requirements: [UX-03]

must_haves:
  truths:
    - "Ni el panel ni la app cliente muestran 'A new Flutter project' ni el azul #0175C2 en ningún archivo web"
    - "La pestaña del navegador, el favicon y el instalador PWA de LAS DOS apps muestran identidad GRI"
    - "La app móvil arranca con ícono y splash de GRI, no con el logo por defecto de Flutter"
    - "Un comando automatizado detecta si cualquiera de las dos apps vuelve a los valores por defecto"
  artifacts:
    - path: "app_cliente/tool/gen_branding.dart"
      provides: "Generador determinista de todos los assets de marca de las dos apps"
      min_lines: 60
    - path: "scripts/audit_branding.mjs"
      provides: "Gate de branding sobre app_cliente/web y panel_admin/web"
      min_lines: 30
    - path: "app_cliente/web/manifest.json"
      provides: "Manifiesto de la app cliente con identidad GRI"
      contains: "#FF4C05"
  key_links:
    - from: "scripts/package.json"
      to: "scripts/audit_branding.mjs"
      via: "npm script audit:branding"
      pattern: "audit_branding\\.mjs"
---

<objective>
Sustituir en las DOS apps el branding por defecto de Flutter por la identidad GRI, generando los
assets de forma determinista y dejando un gate que impida la regresión.

Purpose: es el hallazgo más dañino para la percepción de profesionalidad y el más barato de arreglar.
Verificado en el repo: **las dos apps** tienen los valores de plantilla, no solo el panel —
`app_cliente/web/index.html:21` y `manifest.json:8` dicen literalmente "A new Flutter project.",
`index.html:32` tiene `<title>gri_cliente</title>` y `manifest.json:7` usa `#0175C2`, el azul de demo
de Flutter, que ni siquiera es el color de marca. Y el runbook del plan 11-15 arranca la app cliente
con `flutter run -d chrome`, así que el usuario lo va a ver.
Output: generador de assets, branding web de ambas apps, ícono y splash del móvil, y `npm run audit:branding`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/audit/UI-REVIEW-panel_admin.md
@.planning/codebase/audit/UI-REVIEW-app_cliente.md
@app_cliente/web/index.html
@app_cliente/web/manifest.json
@panel_admin/web/index.html
@panel_admin/web/manifest.json
@app_cliente/lib/core/theme.dart
@app_cliente/pubspec.yaml
</context>

<interfaces>
<!-- Estado verificado archivo por archivo. Los DOS proyectos están sin tocar desde `flutter create`. -->

| Archivo | Hoy | Objetivo |
|---|---|---|
| `panel_admin/web/manifest.json` `name`/`short_name` | `gri_panel_admin` | `GRI — Panel Administrativo` / `GRI Panel` |
| `panel_admin/web/manifest.json` `description` | `A new Flutter project.` | descripción real |
| `panel_admin/web/manifest.json` colores | `#0175C2` | `theme_color: #FF4C05`, `background_color: #F5F6F8` |
| `panel_admin/web/index.html` `<title>` | `gri_panel_admin` | `GRI — Panel Administrativo` |
| `app_cliente/web/manifest.json` `name`/`short_name` | `gri_cliente` | `GRI` / `GRI` |
| `app_cliente/web/manifest.json` `description` | `A new Flutter project.` | descripción real |
| `app_cliente/web/manifest.json` colores | `#0175C2` | `theme_color: #FF4C05`, `background_color: #F7F7F7` (fondo del mockup del cliente, distinto del panel a propósito) |
| `app_cliente/web/index.html` `<title>` (línea 32) | `gri_cliente` | `GRI` |
| `app_cliente/web/index.html` `<meta name="description">` (línea 21) | `A new Flutter project.` | descripción real |
| `app_cliente/web/index.html` `apple-mobile-web-app-title` | `gri_cliente` | `GRI` |
| `favicon.png` + `icons/*` de ambas | logo de Flutter | marca GRI |
| `app_cliente/android/.../mipmap-*/ic_launcher.png` | asset de plantilla de 544 bytes | ícono GRI |
| `app_cliente/android/.../drawable/launch_background.xml` | plantilla en blanco | splash GRI |

Marca GRI (del mockup `documentos/index.html`, líneas 74-86 y 613-625): cuadrado redondeado
(`border-radius: 12px` sobre 45×45, o sea ~27% del lado) con `background: var(--primary)` = `#FF4C05`
y el glifo 🍽️ en blanco, junto al wordmark "GRI".
**No existe ningún archivo de logo en el repo** (`documentos/` solo tiene los mockups, `mockup.png`,
`flujograma.png` y `sdk.png`): el asset se GENERA. Prohibido descargar imágenes o pedirle un PNG al usuario.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Generador determinista de los assets de marca</name>
  <files>app_cliente/tool/gen_branding.dart, app_cliente/pubspec.yaml, app_cliente/assets/branding/, app_cliente/web/favicon.png, app_cliente/web/icons/, panel_admin/web/favicon.png, panel_admin/web/icons/</files>
  <action>
Crear `app_cliente/tool/gen_branding.dart`, script Dart puro que use `package:image` (añadir como
`dev_dependency` de `app_cliente`; comprobar antes el publisher en pub.dev con
`curl -s https://pub.dev/api/packages/image/publisher` y pinear versión exacta) para dibujar el ícono:
- lienzo cuadrado configurable (1024 por defecto), fondo transparente
- cuadrado redondeado que ocupa todo el lienzo, radio ≈ 27% del lado (proporción del mockup), relleno `#FF4C05`
- glifo blanco de "plato con cubiertos" dibujado con **primitivas**: círculo con borde grueso centrado
  (el plato) y dos barras redondeadas verticales a los lados (los cubiertos). Sin fuentes ni emoji:
  su rasterizado no es determinista entre plataformas y este script debe dar el mismo PNG en cualquier máquina.
- salidas:
  `app_cliente/assets/branding/icon_1024.png`, `icon_512.png` (fuente de los generadores móviles)
  `app_cliente/web/favicon.png` y `panel_admin/web/favicon.png` (64×64)
  `app_cliente/web/icons/` y `panel_admin/web/icons/`: `Icon-192.png`, `Icon-512.png`,
  `Icon-maskable-192.png`, `Icon-maskable-512.png` (las maskable con el mismo dibujo y un 20% de
  margen de seguridad). Los cuatro nombres deben coincidir EXACTAMENTE con los que ya declaran los
  `manifest.json` de ambas apps, para no romper las rutas.
- idempotente y con `--out` para el directorio base.

Ejecutar `cd app_cliente && dart run tool/gen_branding.dart` y commitear los PNG resultantes de
las dos apps.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; dart run tool/gen_branding.dart &amp;&amp; test -s assets/branding/icon_1024.png &amp;&amp; test -s web/favicon.png &amp;&amp; test -s web/icons/Icon-512.png &amp;&amp; test -s ../panel_admin/web/favicon.png &amp;&amp; test -s ../panel_admin/web/icons/Icon-maskable-512.png &amp;&amp; echo "ASSETS_OK"</automated>
    <automated>cd app_cliente &amp;&amp; flutter analyze</automated>
  </verify>
  <done>Un solo comando regenera todos los assets de marca de las dos apps, sin depender de fuentes ni de descargas.</done>
</task>

<task type="auto">
  <name>Tarea 2: Branding web de las dos apps + shell de carga + gate automatizado</name>
  <files>app_cliente/web/index.html, app_cliente/web/manifest.json, panel_admin/web/index.html, panel_admin/web/manifest.json, scripts/audit_branding.mjs, scripts/package.json</files>
  <action>
Aplicar en los cuatro archivos los valores de la tabla del bloque `<interfaces>`. Respetar que los
fondos difieren a propósito entre apps (`#F5F6F8` panel, `#F7F7F7` cliente): vienen de dos mockups
distintos y así está documentado en los dos `core/theme.dart`.
En ambos `index.html` añadir además `<meta name="theme-color" content="#FF4C05">` y conservar la
estructura del bootstrap de Flutter sin tocarla.

**Shell de carga con marca**, en los dos `index.html`: un `div` centrado con el color de fondo de esa
app, el ícono y un indicador, que se retira cuando Flutter monta. Hoy `main()` espera `bootstrap()`
(inicialización de Firebase) antes de `runApp`, así que la pestaña queda en blanco durante toda esa
espera y se lee como "roto" en vez de "cargando". Retirar el `div` desde el punto de enganche del
bootstrap de Flutter ya presente en el archivo; NO cambiar el orden de inicialización de Firebase.

Crear `scripts/audit_branding.mjs`: gate que recorre `app_cliente/web` y `panel_admin/web`
(`index.html` y `manifest.json` de cada una) y falla con `exit 1` si encuentra cualquiera de:
`A new Flutter project`, `#0175C2`, `gri_cliente` o `gri_panel_admin` como `name`/`short_name`/`title`,
o si `theme_color` no es `#FF4C05` (comparación sin distinguir mayúsculas). Que además compruebe que
los cuatro iconos declarados en cada `manifest.json` existen en disco y no están vacíos.
Salida legible: `archivo → hallazgo`. Añadir `"audit:branding": "node audit_branding.mjs"` a
`scripts/package.json`.
Verificar el gate por construcción: reintroducir temporalmente `#0175C2` en una de las dos apps,
comprobar que falla, y revertir.
  </action>
  <verify>
    <automated>cd scripts &amp;&amp; npm run audit:branding</automated>
    <automated>cd panel_admin &amp;&amp; flutter build web --release</automated>
    <automated>cd app_cliente &amp;&amp; flutter build web --release</automated>
  </verify>
  <done>Cero rastros de los valores por defecto en las dos apps, las dos compilan para web con el `index.html` editado, y hay un gate que caza la regresión.</done>
</task>

<task type="auto">
  <name>Tarea 3: Ícono y splash de la app móvil</name>
  <files>app_cliente/pubspec.yaml, app_cliente/android/app/src/main/res/</files>
  <action>
Añadir `flutter_launcher_icons` y `flutter_native_splash` como `dev_dependencies` de `app_cliente`
(ambos llevan años recomendados en la documentación de stack del proyecto y nunca se añadieron).
Antes de instalar, comprobar el publisher de cada uno con
`curl -s https://pub.dev/api/packages/<paquete>/publisher` y pinear versión exacta.

Configurar en `pubspec.yaml`:
- `flutter_launcher_icons`: `image_path: assets/branding/icon_1024.png`, `android: true`,
  `adaptive_icon_background: "#FF4C05"`, `adaptive_icon_foreground` con el glifo, `web: false`
  (los iconos web ya los genera la Tarea 1 con los nombres que espera el manifest).
- `flutter_native_splash`: `color: "#FF4C05"`, `image: assets/branding/icon_512.png`, bloque
  `android_12` configurado (en Android 12+ el splash lo dibuja el sistema y requiere su propia clave).

Ejecutar ambos generadores y commitear su salida. Verificar que
`android/app/src/main/res/mipmap-hdpi/ic_launcher.png` ya no es el asset de plantilla de 544 bytes y
que `drawable/launch_background.xml` deja de tener el placeholder comentado sin rellenar.

⚠️ Hay un cambio no commiteado en `app_cliente/android/app/src/main/AndroidManifest.xml` y un
`res/xml/` sin seguimiento en el árbol de trabajo. Antes de correr los generadores, revisar
`git diff` de ese archivo para no mezclar cambios ajenos con los del splash, y dejar constancia en el
SUMMARY de qué se conservó.
  </action>
  <verify>
    <automated>test $(stat -c%s app_cliente/android/app/src/main/res/mipmap-hdpi/ic_launcher.png) -gt 1000 &amp;&amp; echo "ICONO_OK"</automated>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
  </verify>
  <done>La app móvil arranca con ícono y splash GRI; ningún asset de plantilla de Flutter sobrevive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → pub.dev | Se añaden 3 dependencias de desarrollo (`image`, `flutter_launcher_icons`, `flutter_native_splash`) |
| `index.html` → arranque de la app | Un shell de carga mal retirado dejaría la app inutilizable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-18-SC | Tampering | dev_dependencies nuevas de pub.dev | mitigate | Verificar el publisher de cada paquete vía la API de pub.dev antes de instalar y pinear versión exacta. Son `dev_dependencies`: no entran en el binario de producción |
| T-11-18-02 | Denial of Service | shell de carga que no se retira | mitigate | Gate `flutter build web --release` en las DOS apps dentro del propio plan, más el runbook manual del plan 11-15 que las arranca en Chrome |
| T-11-18-03 | Information Disclosure | shell de carga en `index.html` | accept | Solo contiene marca y un indicador; ningún dato ni configuración |
| T-11-18-04 | Tampering | regresión a los valores de plantilla | mitigate | `npm run audit:branding` cubre las dos apps y falla si reaparece cualquiera de los cuatro marcadores |
</threat_model>

<verification>
- `cd scripts && npm run audit:branding` → exit 0.
- `cd app_cliente && flutter build web --release` y `cd panel_admin && flutter build web --release` → compilan.
- `cd app_cliente && flutter test && flutter analyze` → verde, 0 issues.
- `grep -rn "A new Flutter project\|#0175C2" app_cliente/web panel_admin/web` → sin resultados.
</verification>

<success_criteria>
- La primera impresión de las DOS apps (pestaña, favicon, instalador PWA, ícono y splash del móvil) es GRI.
- Los assets se regeneran con un comando y no dependen de ningún archivo binario aportado a mano.
- La regresión está cubierta por un gate, no por revisión visual.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-18-SUMMARY.md` al terminar.
</output>
