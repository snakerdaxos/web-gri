---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .firebaserc
  - .gitignore
  - CLAUDE.md
  - firebase.json
  - functions/package.json
  - functions/index.js
  - functions/.env.example
  - functions/.env.demo-gri
  - functions/.gitignore
  - scripts/package.json
  - scripts/run_emulators.mjs
  - scripts/test/rules/_contexts.mjs
  - scripts/test/rules/smoke.test.mjs
  - docs/FIREBASE_SETUP.md
autonomous: true
requirements: [ENV-01, DOC-01]

must_haves:
  truths:
    - "El emulador de Firestore arranca en esta máquina sin que el desarrollador configure Java a mano"
    - "Un test de rules escrito con @firebase/rules-unit-testing corre y reporta resultados"
    - "El emulador de Functions arranca junto al de Firestore desde firebase.json"
    - "CLAUDE.md describe el stack Flutter+Firebase real, no el FastAPI+MySQL archivado"
    - "La configuración que consumen las Cloud Functions en emuladores es determinista y está versionada"
  artifacts:
    - path: ".firebaserc"
      provides: "Alias default (p-gri-b5b40) y demo para emuladores/tests"
      contains: "p-gri-b5b40"
    - path: "scripts/run_emulators.mjs"
      provides: "Wrapper que resuelve Java y lanza firebase emulators:exec"
      min_lines: 40
    - path: "functions/package.json"
      provides: "Codebase Node de Cloud Functions con engines.node = 22"
      contains: "\"node\": \"22\""
    - path: "scripts/test/rules/_contexts.mjs"
      provides: "Helpers de contexto por rol para la suite de rules"
      exports: ["initEnv", "cliente", "adminDemo", "adminOtro", "superAdmin", "anon"]
  key_links:
    - from: "scripts/package.json"
      to: "scripts/run_emulators.mjs"
      via: "npm script test:rules"
      pattern: "run_emulators\\.mjs"
    - from: "firebase.json"
      to: "functions/"
      via: "bloque functions[].source"
      pattern: "\"source\": \"functions\""
---

<objective>
Desbloquear el entorno de test contra emuladores y crear el andamiaje Node de la fase:
Java resuelto, `.firebaserc` creado, codebase `functions/` en blanco pero arrancable, y el arnés
de tests de `firestore.rules` funcionando de punta a punta con un caso de humo.

Purpose: sin esto NO corre ni un solo test de rules ni de Cloud Functions. Es el prerequisito duro
de los planes 03, 04, 06, 07 y 08.
Output: `.firebaserc`, `functions/` (scaffold), `firebase.json` ampliado, `scripts/run_emulators.mjs`,
`scripts/test/rules/_contexts.mjs` + `smoke.test.mjs`, npm scripts, CLAUDE.md corregido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-CONTEXT.md
@firebase.json
@firestore.rules
@scripts/package.json
@docs/FIREBASE_SETUP.md
@CLAUDE.md
</context>

<interfaces>
<!-- Datos verificados por la investigación (11-RESEARCH.md). No re-investigar. -->

Entorno de esta máquina (VERIFICADO):
- Node local: v24.13.1 · npm 10.9.7 · Flutter 3.47.0
- `firebase-tools` 15.27.0 ya instalado en `scripts/node_modules/.bin/firebase` (NO global)
- Java NO está en PATH. Existe y funciona: `C:\Program Files\Android\Android Studio\jbr\bin\java.exe` (OpenJDK 21.0.10)
- `.firebaserc` NO existe. `PROJECT_ID = 'p-gri-b5b40'` está hardcodeado en `scripts/seed_firebase.mjs:41`
- El emulador de Firestore EXIGE Java; el de Functions y el de Auth NO

Versiones aprobadas en la auditoría de legitimidad de paquetes (todas `OK`, ninguna `[SUS]`/`[SLOP]`):
- `@firebase/rules-unit-testing` 5.0.1 (peer: `firebase ^12.0.0`, engines node >=20)
- `firebase` 12.17.1 (JS client SDK)
- `firebase-functions` 7.3.2 · `firebase-admin` 14.2.0 (misma versión que scripts/package.json)
- Runtime de Cloud Functions: Node **22** (20 y 22 soportados; 18 deprecado; 24 NO soportado)

Puertos de emulador (5001 es el default oficial del emulador de Functions):
auth 9099 · functions 5001 · firestore 8080 · ui 4000
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Resolver Java, crear .firebaserc y corregir el stack documentado</name>
  <files>scripts/run_emulators.mjs, .firebaserc, .gitignore, CLAUDE.md, docs/FIREBASE_SETUP.md</files>
  <action>
Crear `scripts/run_emulators.mjs`: wrapper Node (ESM, el `scripts/package.json` ya es `"type": "module"`)
que resuelve Java y lanza el CLI de Firebase con el entorno correcto.

Comportamiento requerido:
1. Resolver el ejecutable de Java en este orden: (a) `JAVA_HOME/bin/java` si `JAVA_HOME` está definido;
   (b) `java` si está en PATH (comprobar con `spawnSync('java', ['-version'])`); (c) las rutas conocidas
   del JBR de Android Studio, empezando por `C:\Program Files\Android\Android Studio\jbr`.
   Si ninguna resuelve, salir con código 1 e imprimir instrucciones accionables (instalar Temurin 21
   o definir `JAVA_HOME`). NO fallar en silencio.
2. Cuando el Java resuelto no venga del PATH, inyectar `JAVA_HOME` y prependear su `bin` al `PATH`
   del proceso hijo únicamente (no tocar el entorno global de la máquina).
3. Reenviar todos los argumentos recibidos a `node_modules/.bin/firebase emulators:exec`, resolviendo
   la ruta del binario relativa a este archivo (`scripts/node_modules/.bin/firebase`, con sufijo `.cmd`
   en win32). Propagar el código de salida del hijo con `process.exit`.
4. Ejecutar siempre con `cwd` en la raíz del repo (un nivel arriba de `scripts/`) para que
   `firebase.json`, `.firebaserc`, `firestore.rules` y `firestore.indexes.json` se resuelvan.
5. Aceptar cero o más `--set-env CLAVE=VALOR` ANTES del resto de argumentos, retirarlos del argv que
   se reenvía al CLI, e inyectarlos en el entorno del proceso hijo. Los tests e2e de los planes 11-07
   y 11-08 lo usan para conocer los valores esperados de `BOOTSTRAP_EMAIL` y `BOOTSTRAP_SECRET` sin
   duplicar constantes.
   ⚠️ **Importante y no obvio:** esta inyección NO alcanza al emulador de Functions. El emulador carga
   la configuración de la función desde `functions/.env` y `functions/.env.{projectId}` **al arrancar**,
   antes de que exista el proceso de test, así que escribir un `.env` desde un `before()` llega tarde.
   La configuración que consume la función se declara en `functions/.env.demo-gri` (Tarea 2), y
   `--set-env` sirve solo para que el test sepa qué valores esperar.

Crear `.firebaserc` en la raíz con dos alias: `default` → `p-gri-b5b40` (el proyecto real) y
`demo` → `demo-gri` (proyecto ficticio para emuladores: el prefijo `demo-` garantiza que el CLI
nunca pida credenciales ni toque un proyecto real).

Ampliar `.gitignore` con: `functions/node_modules/`, `functions/lib/`, `functions/.env`,
`*-debug.log` (hoy `scripts/firestore-debug.log` queda sin ignorar; `firebase-debug.log` ya está
listado pero el patrón genérico lo cubre mejor). OJO: el patrón debe ignorar `functions/.env` pero
**no** `functions/.env.demo-gri` ni `functions/.env.example`, que sí se commitean; verificar con
`git check-ignore -v` los tres casos.

Corregir `CLAUDE.md`: la sección `## Technology Stack` completa describe el backend FastAPI + MySQL
**archivado en la Fase 10** y hará que cualquier agente futuro planifique contra un stack inexistente.
Sustituirla por el stack vivo, conciso:
- Apps: Flutter 3.47 / Dart — `app_cliente` (Android + Web) y `panel_admin` (Flutter Web)
- Estado/DI: `flutter_riverpod` 3.x + `riverpod_generator`; rutas: `go_router` 17.5
- Backend: **Firebase** — Auth (email/password + custom claims `{role, rid}`), Cloud Firestore
  (autorización en `firestore.rules`), Cloud Functions (Node 22, carpeta `functions/`)
- Paquetes Firebase pineados: `firebase_core` 4.13.0, `firebase_auth` 6.5.7, `cloud_firestore` 6.8.0,
  `cloud_functions` 6.3.6 (solo panel)
- Tests: `flutter_test` + `fake_cloud_firestore` (apps) · `node --test` +
  `@firebase/rules-unit-testing` contra emulador (rules y callables)
- Tooling: `firebase-tools` local en `scripts/`, emuladores Auth/Firestore/Functions/UI
- **NO USAR:** `backend/` (FastAPI + MySQL) está ARCHIVADO como referencia; no planificar contra él.
  MySQL, SQLAlchemy, Alembic, asyncmy, PyJWT, uvicorn y todo lo relacionado quedó fuera del sistema vivo.
Mantener intactas las secciones `## Project`, `## Constraints` (marcando que el stack FastAPI/MySQL
de las decisiones originales fue reemplazado por Firebase en la Fase 10), `## Conventions`,
`## Architecture`, `## GSD Workflow Enforcement` y `## Developer Profile`.

Añadir a `docs/FIREBASE_SETUP.md` una sección corta "Java y emuladores" que documente el wrapper
(`npm run test:rules` ya resuelve Java solo) y la ruta del JBR como fallback manual.
  </action>
  <verify>
    <automated>cd scripts &amp;&amp; node run_emulators.mjs --only firestore --project demo-gri -- node -e "console.log('EMULADOR_OK')"</automated>
    <automated>cd scripts &amp;&amp; node run_emulators.mjs --set-env GRI_PRUEBA=ok --only firestore --project demo-gri -- node -e "process.exit(process.env.GRI_PRUEBA==='ok'?0:1)" &amp;&amp; echo "SET_ENV_OK"</automated>
    <automated>node -e "const f=require('./.firebaserc');if(f.projects.default!=='p-gri-b5b40')process.exit(1);console.log('FIREBASERC_OK')"</automated>
    <automated>grep -v '^#' CLAUDE.md | grep -ci "SQLAlchemy\|asyncmy\|uvicorn\|MySQL 8.4" | grep -q '^0$' &amp;&amp; echo "CLAUDE_MD_OK"</automated>
  </verify>
  <done>`node run_emulators.mjs ... -- node -e "..."` imprime `EMULADOR_OK` (el emulador de Firestore arrancó, luego el comando corrió, luego el emulador se apagó). `.firebaserc` existe con ambos alias. CLAUDE.md ya no menciona el stack Python/MySQL como stack vivo.</done>
</task>

<task type="auto">
  <name>Tarea 2: Scaffold del codebase functions/ y bloque functions en firebase.json</name>
  <files>functions/package.json, functions/index.js, functions/.env.example, functions/.gitignore, firebase.json</files>
  <action>
Crear la carpeta `functions/` **a mano**. Está PROHIBIDO ejecutar `firebase init functions`: es un
asistente interactivo que reescribe `firebase.json` completo y puede cambiar puertos, añadir un
bloque `hosting` que nadie pidió o reapuntar `.firebaserc`.

`functions/package.json`:
- `"name": "gri-functions"`, `"private": true`, `"type": "module"`, `"main": "index.js"`
- `"engines": { "node": "22" }` — obligatorio: el Node local es 24 y **24 no es runtime soportado**
  por Cloud Functions. El emulador avisará del desajuste pero funciona; ceñirse a APIs de Node 22.
- dependencies: `"firebase-admin": "14.2.0"` (la misma versión exacta que `scripts/package.json`,
  sin divergencia) y `"firebase-functions": "7.3.2"`
- scripts: `"test": "node --test test/"`
- Instalar con `npm install` dentro de `functions/`.

`functions/index.js`: punto de entrada del codebase. En este plan queda con la inicialización del
Admin SDK y sin funciones exportadas todavía (los planes 06 y 07 añaden los `export`):
- `import { initializeApp } from 'firebase-admin/app';` + `initializeApp();` una sola vez, en el
  módulo raíz. Documentar con un comentario que cada archivo de `src/` obtiene sus handles con
  `getAuth()` / `getFirestore()` y NUNCA vuelve a llamar `initializeApp()`.
- Un comentario-índice que liste las funciones que se exportarán (`bootstrapPlataforma` en 11-07,
  `crearUsuarioStaff` en 11-08) para que el orden de exports sea evidente.

`functions/.env.example`: documentar `BOOTSTRAP_EMAIL=` y `BOOTSTRAP_SECRET=` (los consume la
función del plan 11-07) con un comentario explicando que el archivo real `functions/.env` NO se
commitea porque contiene los valores de producción.

`functions/.env.demo-gri`: este SÍ se commitea. `firebase-tools` carga `functions/.env` y, encima,
`functions/.env.{projectId}` — y todos los scripts de test usan `--project demo-gri`, así que este
archivo es la configuración determinista de las funciones en los emuladores y jamás afecta a
producción. Contenido:
`BOOTSTRAP_EMAIL=fundador@demo.gri.dev` y `BOOTSTRAP_SECRET=secreto-solo-para-emulador-no-usar-en-produccion`.
Comentar en el propio archivo que son valores de emulador y que el proyecto `demo-gri` no existe.

`functions/.gitignore`: `node_modules/`, `.env`, `*-debug.log`.

Editar `firebase.json` **quirúrgicamente** (el diff no debe superar ~10 líneas). Preservar
literalmente el bloque `firestore` existente. Añadir:
- `"functions": [ { "source": "functions", "codebase": "default", "ignore": ["node_modules", ".git", "*-debug.log", "test/**"] } ]`
  (el CLI acepta objeto único o array de codebases; el array deja la puerta abierta a más codebases)
- dentro de `emulators`, `"functions": { "port": 5001 }` conservando `auth` 9099, `firestore` 8080 y `ui` 4000.
  </action>
  <verify>
    <automated>cd functions &amp;&amp; npm install --no-audit --no-fund &amp;&amp; node -e "const p=require('./package.json');if(p.engines.node!=='22')process.exit(1);console.log('ENGINES_OK')"</automated>
    <automated>node -e "const f=require('./firebase.json');if(!f.functions||!f.emulators.functions||f.emulators.functions.port!==5001)process.exit(1);if(!f.firestore.rules)process.exit(1);console.log('FIREBASE_JSON_OK')"</automated>
    <automated>grep -q "BOOTSTRAP_EMAIL=" functions/.env.demo-gri &amp;&amp; grep -q "BOOTSTRAP_SECRET=" functions/.env.demo-gri &amp;&amp; echo "ENV_DEMO_OK"</automated>
    <automated>cd scripts &amp;&amp; node run_emulators.mjs --only functions,firestore --project demo-gri -- node -e "console.log('FUNCTIONS_EMU_OK')"</automated>
  </verify>
  <done>`functions/` instala sin errores, `firebase.json` conserva su bloque `firestore` y declara `functions` + `emulators.functions.port = 5001`, y el emulador de Functions arranca junto al de Firestore.</done>
</task>

<task type="auto">
  <name>Tarea 3: Arnés de tests de firestore.rules con caso de humo</name>
  <files>scripts/package.json, scripts/test/rules/_contexts.mjs, scripts/test/rules/smoke.test.mjs</files>
  <action>
Instalar en `scripts/` (que ya tiene `package.json` y `firebase-tools`):
`npm i -D @firebase/rules-unit-testing@5.0.1 firebase@12.17.1`.

Añadir a `scripts/package.json` los scripts npm (todos pasan por el wrapper de la Tarea 1, que
resuelve Java):
- `"test:rules": "node run_emulators.mjs --only firestore --project demo-gri -- node --test test/rules/"`
- `"test:functions": "node run_emulators.mjs --only auth,functions,firestore --project demo-gri -- node --test test/functions/"`
- `"test": "npm run test:rules && npm run test:functions"`
Mantener el script `seed` existente sin tocar. Como `test/functions/` aún no existe, crear la carpeta
con un `.gitkeep` para que `test:functions` no reviente por ruta inexistente (los tests llegan en 11-07/11-08).

Crear `scripts/test/rules/_contexts.mjs` — fixture COMPARTIDO por toda la suite de rules
(los planes 03, 04 y 08 lo importan; su firma es un contrato):
- `initEnv()`: `initializeTestEnvironment({ projectId: 'demo-gri-rules', firestore: { rules: readFileSync('../firestore.rules','utf8') } })`.
  OJO con la ruta: el proceso corre con cwd en la raíz del repo (lo fija el wrapper), así que la ruta
  correcta es `firestore.rules`; resolverla con `fileURLToPath(import.meta.url)` para que no dependa del cwd.
- Fábricas de contexto que reciben el `testEnv` y devuelven un `firestore()` listo:
  `anon(env)` (no autenticado, `unauthenticatedContext()`),
  `cliente(env, uid='uid-cliente')` (autenticado SIN claims — ausencia de `role` == cliente),
  `adminDemo(env)` (`{role:'admin_restaurante', rid:'demo'}`),
  `adminOtro(env)` (`{role:'admin_restaurante', rid:'otro'}`),
  `mesero(env)` / `cocina(env)` (`rid:'demo'`),
  `superAdmin(env)` (`{role:'super_admin'}` — **sin `rid`**: el super_admin no lleva rid, confirmado
  en `scripts/seed_firebase.mjs:47`).
- `sembrar(env, fn)`: envoltorio de `env.withSecurityRulesDisabled(ctx => fn(ctx.firestore()))` para
  preparar datos saltándose las rules.
Documentar en cabecera que los claims van en el SEGUNDO argumento de `authenticatedContext`.

Crear `scripts/test/rules/smoke.test.mjs` — prueba que el arnés entero funciona (NO es un test de
producto, es el gate del andamiaje). Con `before/after/beforeEach` de `node:test`:
- `restaurantes/{id}` es de lectura pública (`allow read: if true`): un contexto ANÓNIMO hace
  `getDoc` de un restaurante sembrado → `assertSucceeds`.
- Un contexto anónimo intenta `setDoc` de `restaurantes/x` → `assertFails` (solo `isSuper()` crea).
- `env.clearFirestore()` en `beforeEach` y `env.cleanup()` en `after`.
  </action>
  <verify>
    <automated>cd scripts &amp;&amp; npm run test:rules</automated>
  </verify>
  <done>`npm run test:rules` levanta el emulador de Firestore, ejecuta `smoke.test.mjs` con 2 casos en verde y apaga el emulador, devolviendo exit code 0. La suite de los planes siguientes ya tiene dónde apoyarse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → registro npm | Se instalan 2 paquetes nuevos en `scripts/` y 2 en `functions/` |
| repo → filesystem del desarrollador | El wrapper de Java lee rutas del sistema y muta el PATH del proceso hijo |
| `.firebaserc` → proyecto Firebase real | Un alias mal apuntado ejecutaría tests contra `p-gri-b5b40` con datos reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-SC | Tampering | `npm i @firebase/rules-unit-testing`, `firebase`, `firebase-functions`, `firebase-admin` | mitigate | Los 4 paquetes están en la `## Auditoría de Legitimidad de Paquetes` de 11-RESEARCH.md con veredicto `OK`, repo fuente `github.com/firebase/*`, sin scripts `postinstall`. Ninguno es `[ASSUMED]`/`[SUS]`/`[SLOP]` → no requiere checkpoint humano. Instalar con versión EXACTA (sin `^`) |
| T-11-01-02 | Tampering | `.firebaserc` / npm scripts | mitigate | Todos los scripts de test pasan `--project demo-gri` explícito. El prefijo `demo-` hace que el CLI y los SDK rechacen credenciales reales por diseño. Prohibido usar el alias `default` en cualquier script de test |
| T-11-01-03 | Elevation of Privilege | `functions/.env` (BOOTSTRAP_EMAIL) | mitigate | Solo se commitea `.env.example`; `functions/.gitignore` y `.gitignore` raíz excluyen `functions/.env` |
| T-11-01-04 | Information Disclosure | `*-debug.log` de los emuladores | mitigate | Patrón añadido a `.gitignore` (los logs del emulador contienen payloads de documentos) |
</threat_model>

<verification>
- `cd scripts && npm run test:rules` → exit 0, sin necesidad de configurar Java a mano.
- `cd scripts && node run_emulators.mjs --only auth,functions,firestore --project demo-gri -- node -e "console.log(1)"` → arranca los 3 emuladores.
- `git diff firebase.json` → menos de 12 líneas cambiadas, bloque `firestore` intacto.
- `flutter analyze` en ambas apps sigue en 0 issues (este plan no toca código Dart).
</verification>

<success_criteria>
- Cualquier desarrollador de esta máquina corre `npm run test:rules` sin tocar variables de entorno.
- `functions/` existe, instala y arranca en el emulador, con `engines.node = "22"`.
- `_contexts.mjs` expone los 7 contextos de rol que consumirán los planes 03, 04 y 08.
- CLAUDE.md ya no envía a ningún agente futuro contra el stack FastAPI+MySQL archivado.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-01-SUMMARY.md` al terminar.
</output>
