---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app_cliente/test/helpers/firebase_fakes.dart
  - panel_admin/test/helpers/firebase_fakes.dart
  - app_cliente/test/base_vacia_test.dart
  - panel_admin/test/base_vacia_test.dart
  - panel_admin/pubspec.yaml
  - panel_admin/lib/core/firebase_providers.dart
  - panel_admin/lib/core/firebase_bootstrap.dart
  - panel_admin/test/core/functions_provider_test.dart
autonomous: true
requirements: [ENV-01, TEST-02]

must_haves:
  truths:
    - "Existe un fixture de Firestore VACÍO (sin seed) que ambas suites pueden usar"
    - "Ninguna de las dos apps crashea contra una base de datos completamente vacía"
    - "El panel puede invocar Cloud Functions y, con --dart-define=USE_EMULATORS=true, apunta al emulador"
  artifacts:
    - path: "app_cliente/test/helpers/firebase_fakes.dart"
      provides: "buildFakeFirestoreVacio() junto al buildFakeFirestoreConSeed() existente"
      contains: "buildFakeFirestoreVacio"
    - path: "panel_admin/lib/core/firebase_providers.dart"
      provides: "firebaseFunctionsProvider con región explícita us-central1"
      contains: "us-central1"
    - path: "panel_admin/test/base_vacia_test.dart"
      provides: "Regresión de primer arranque en el panel"
      min_lines: 30
  key_links:
    - from: "panel_admin/lib/core/firebase_bootstrap.dart"
      to: "emulador de Functions :5001"
      via: "useFunctionsEmulator dentro del gate useEmulators"
      pattern: "useFunctionsEmulator"
---

<objective>
Cerrar el punto ciego de "base vacía" en las dos suites Flutter y dejar el panel listo para llamar
Cloud Functions.

Purpose: hoy `buildFakeFirestoreConSeed()` SIEMPRE pre-siembra, así que el escenario de primer
arranque —el que el usuario reportó como roto— es el único que nunca se ha probado. Y sin el cliente
de `cloud_functions` cableado, los planes 06 y 08 no pueden existir.
Output: `buildFakeFirestoreVacio()` en ambas apps, dos suites de regresión de base vacía, y
`firebaseFunctionsProvider` + emulador de Functions en el bootstrap del panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@app_cliente/test/helpers/firebase_fakes.dart
@panel_admin/test/helpers/firebase_fakes.dart
@panel_admin/lib/core/firebase_providers.dart
@panel_admin/lib/core/firebase_bootstrap.dart
@app_cliente/lib/features/restaurantes/restaurantes_provider.dart
@panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart
</context>

<interfaces>
<!-- Contratos ya vigentes en el repo. Usarlos tal cual; no explorar el codebase para deducirlos. -->

Patrón de test verificado (TESTING.md) — los tests pumpean pantallas SUELTAS, nunca el AppShell:
```dart
final db = await buildFakeFirestoreConSeed();
final auth = mockAuth(email: 'carlos@demo.gri.dev');
await tester.pumpWidget(ProviderScope(
  overrides: [
    firebaseAuthProvider.overrideWithValue(auth),
    firestoreProvider.overrideWithValue(db),
    claimsProvider.overrideWith((ref) async => (role: 'mesero', rid: 'demo')),
  ],
  child: MaterialApp(home: AlgunaScreen()),
));
```

Providers del panel (`panel_admin/lib/core/firebase_providers.dart`, riverpod_generator):
```dart
@Riverpod(keepAlive: true) FirebaseAuth firebaseAuth(Ref ref) => FirebaseAuth.instance;
@Riverpod(keepAlive: true) FirebaseFirestore firestore(Ref ref) => FirebaseFirestore.instance;
@Riverpod(keepAlive: true) Stream<User?> authStateChanges(Ref ref) => ...;
@Riverpod(keepAlive: true) Future<({String role, String? rid})> claims(Ref ref) async {...}
```

Gate de emuladores (`firebase_bootstrap.dart`), orden crítico initApp → useAuthEmulator → useFirestoreEmulator:
```dart
const useEmulators = bool.fromEnvironment('USE_EMULATORS', defaultValue: false);
if (useEmulators) {
  await FirebaseAuth.instance.useAuthEmulator('127.0.0.1', 9099);
  FirebaseFirestore.instance.useFirestoreEmulator('127.0.0.1', 8080);
}
```

Paquete a añadir (VERIFICADO en pub.dev, publisher verificado firebase.google.com, Flutter Favorite):
`cloud_functions: 6.3.6` — requiere `firebase_core ^4.13.0` (el repo pinea 4.13.0 exacto: compatible)
y Flutter >= 3.27 (repo: 3.47).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Fixture de base vacía y regresión de primer arranque en ambas apps</name>
  <files>app_cliente/test/helpers/firebase_fakes.dart, panel_admin/test/helpers/firebase_fakes.dart, app_cliente/test/base_vacia_test.dart, panel_admin/test/base_vacia_test.dart</files>
  <behavior>
    - `buildFakeFirestoreVacio()` devuelve un `FakeFirebaseFirestore()` SIN un solo documento.
    - app_cliente / `restaurantesList`: con base vacía devuelve una lista vacía, NO lanza.
    - app_cliente / `restaurantesListScreen`: con base vacía renderiza un estado vacío legible y no
      deja ninguna excepción pendiente (`tester.takeException()` es `null`).
    - app_cliente / `restauranteDetalle('lo-que-sea')`: con base vacía lanza `StateError`
      ("Restaurante no encontrado") de forma controlada — ese es el contrato actual del provider.
    - panel_admin / `restaurantesAdmin` como `super_admin`: con base vacía devuelve lista vacía, NO lanza.
    - panel_admin / `ConfiguracionScreen` (tab Restaurantes) como `super_admin` con base vacía:
      renderiza sin excepción pendiente.
    - panel_admin / `staffMenu` con `rid` nulo: emite lista vacía y no lanza.
  </behavior>
  <action>
Añadir `Future<FakeFirebaseFirestore> buildFakeFirestoreVacio() async => FakeFirebaseFirestore();`
a AMBOS `test/helpers/firebase_fakes.dart`, con un comentario de cabecera explicando por qué existe:
es el escenario de primer arranque —base recién creada, antes de que exista ningún restaurante— y
es literalmente el único que nunca se había probado (ver `.planning/codebase/TESTING.md`).
No tocar `buildFakeFirestoreConSeed()` ni `mockAuth()`: son contratos usados por los 175 tests vigentes.

Crear `app_cliente/test/base_vacia_test.dart` y `panel_admin/test/base_vacia_test.dart` cubriendo
los comportamientos de `<behavior>`. Usar el patrón de overrides ya vigente. Para los tests de
provider puro, `ProviderContainer(overrides: [...])` + `addTearDown(container.dispose)`.

Estos tests son DESCRIPTIVOS del estado actual: si una pantalla hoy queda en blanco sin mensaje,
el test debe afirmar únicamente "no hay excepción pendiente" y dejar un `// TODO(11-10)` apuntando
al plan de estados vacíos, que endurecerá la aserción a "muestra el texto guía X". Está PROHIBIDO
arreglar aquí la UI de los estados vacíos: eso es el plan 11-10 y tocar los mismos archivos crearía
conflicto de olas.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter test test/base_vacia_test.dart</automated>
    <automated>cd panel_admin &amp;&amp; flutter test test/base_vacia_test.dart</automated>
  </verify>
  <done>Ambos archivos pasan en verde y `buildFakeFirestoreVacio()` está disponible para los planes 05, 07 y 10. Las suites completas siguen verdes (91 + N y 84 + N).</done>
</task>

<task type="auto">
  <name>Tarea 2: Cliente de Cloud Functions en el panel + emulador en el bootstrap</name>
  <files>panel_admin/pubspec.yaml, panel_admin/lib/core/firebase_providers.dart, panel_admin/lib/core/firebase_bootstrap.dart, panel_admin/test/core/functions_provider_test.dart</files>
  <action>
Antes de añadir el paquete, confirmar en pub.dev que `cloud_functions` sigue publicado por el
publisher **verificado** `firebase.google.com` (`curl -s https://pub.dev/api/packages/cloud_functions/publisher`).
No añadirlo si el publisher no coincide.

`cd panel_admin && flutter pub add cloud_functions:6.3.6` (versión exacta, coherente con el resto de
pines Firebase del repo). `app_cliente` NO lo necesita: la única app que llama callables es el panel.

En `firebase_providers.dart` añadir, siguiendo el estilo de los providers existentes:
```dart
/// Cliente de Cloud Functions. La REGIÓN va declarada explícitamente en los DOS
/// lados (aquí y en el `onCall` de functions/): un desajuste produce un 404 opaco
/// que en Flutter Web se disfraza de error de CORS.
@Riverpod(keepAlive: true)
FirebaseFunctions firebaseFunctions(Ref ref) =>
    FirebaseFunctions.instanceFor(region: 'us-central1');
```
Regenerar con `dart run build_runner build --delete-conflicting-outputs`.

En `firebase_bootstrap.dart`, DENTRO del `if (useEmulators)` ya existente y DESPUÉS de
`useFirestoreEmulator`, añadir:
`FirebaseFunctions.instanceFor(region: 'us-central1').useFunctionsEmulator('127.0.0.1', 5001);`
El mapeo `127.0.0.1` → `10.0.2.2` en Android lo hace el SDK igual que en Auth/Firestore
(`automaticHostMapping`), así que el comentario ya presente sobre los emuladores sigue siendo válido
para las tres; ampliarlo para mencionar Functions y el puerto 5001.

Crear `panel_admin/test/core/functions_provider_test.dart`: test puro que verifica que el provider
está declarado y que la región es `us-central1`. Como `FirebaseFunctions.instanceFor` requiere una
app Firebase inicializada, el test NO debe resolver el provider: debe afirmar sobre el CÓDIGO FUENTE
—leer `lib/core/firebase_providers.dart` y `lib/core/firebase_bootstrap.dart` con `dart:io` y
comprobar que ambos contienen `us-central1` y que el bootstrap contiene `useFunctionsEmulator` con
puerto 5001 dentro del bloque de emuladores—. Es un gate barato contra la Trampa 4 (región
desalineada) y contra que alguien borre el cableado del emulador.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test test/core/functions_provider_test.dart</automated>
    <automated>cd panel_admin &amp;&amp; grep -c "cloud_functions: 6.3.6" pubspec.yaml</automated>
  </verify>
  <done>`flutter analyze` sigue en 0 issues, el provider existe con región explícita, el bootstrap conecta el emulador de Functions en 5001 y el test de contrato de región pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel_admin → Cloud Functions | Se abre un canal nuevo desde el cliente hacia código privilegiado (Admin SDK) |
| repo → pub.dev | Se añade una dependencia nueva a una app en producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-SC | Tampering | `flutter pub add cloud_functions` | mitigate | pub.dev queda fuera del alcance de slopcheck; la mitigación es verificar el publisher **verificado** `firebase.google.com` vía la API de pub.dev antes de instalar, y pinear versión exacta 6.3.6 (sin `^`) |
| T-11-02-02 | Spoofing | `FirebaseFunctions.instanceFor(region:)` | mitigate | Región declarada explícitamente en cliente y servidor; test de contrato que falla si alguien la quita |
| T-11-02-03 | Tampering | `useFunctionsEmulator` filtrándose a producción | mitigate | Va dentro del gate `const bool.fromEnvironment('USE_EMULATORS')`, que es constante de compilación: sin el define, la llamada ni existe en el binario |
| T-11-02-04 | Denial of Service | Fixture de base vacía | accept | Solo afecta a tests; sin superficie de producción |
</threat_model>

<verification>
- `cd app_cliente && flutter test` → 91 + N en verde, `flutter analyze` 0 issues.
- `cd panel_admin && flutter test` → 84 + N en verde, `flutter analyze` 0 issues.
- `grep -rn "buildFakeFirestoreVacio" app_cliente/test panel_admin/test` → definido en ambos helpers y usado en ambos `base_vacia_test.dart`.
</verification>

<success_criteria>
- Existe cobertura automatizada del escenario "base de datos vacía" en las dos apps, que hoy es cero.
- El panel puede llamar callables y en modo emuladores apunta a `127.0.0.1:5001`.
- Cero regresiones: analyze en 0 y las 175 pruebas previas siguen pasando.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-02-SUMMARY.md` al terminar.
</output>
