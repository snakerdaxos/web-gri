---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 09
type: execute
wave: 4
depends_on: ["11-02"]
files_modified:
  - app_cliente/lib/features/shared/empty_state.dart
  - app_cliente/lib/features/pedidos/menu_mesa_screen.dart
  - app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart
  - app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart
  - app_cliente/lib/features/shared/not_found_screen.dart
  - app_cliente/lib/app.dart
  - app_cliente/test/base_vacia_test.dart
  - app_cliente/test/pedidos/menu_vacio_test.dart
  - app_cliente/test/router_404_test.dart
  - panel_admin/lib/features/shared/not_found_screen.dart
  - panel_admin/lib/app.dart
  - panel_admin/test/router_404_test.dart
  - panel_admin/web/manifest.json
  - panel_admin/web/index.html
  - panel_admin/web/favicon.png
  - panel_admin/web/icons/
  - app_cliente/pubspec.yaml
  - app_cliente/tool/gen_branding.dart
  - app_cliente/assets/branding/
autonomous: true
requirements: [UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "Ninguna lista de las dos apps queda en blanco sin explicar qué pasa ni qué hacer"
    - "Escanear el QR de una mesa cuyo restaurante aún no tiene menú muestra una guía, no una pantalla vacía"
    - "El navegador y el instalador PWA muestran identidad GRI, no 'A new Flutter project'"
    - "La app móvil muestra ícono y splash de GRI, no el logo por defecto de Flutter"
    - "Una URL inexistente muestra una pantalla 404 propia en ambas apps"
  artifacts:
    - path: "app_cliente/lib/features/shared/empty_state.dart"
      provides: "Widget de estado vacío con icono, titular, guía y CTA opcional"
      min_lines: 30
    - path: "app_cliente/tool/gen_branding.dart"
      provides: "Generador determinista de los assets de marca"
      min_lines: 50
    - path: "panel_admin/web/manifest.json"
      provides: "Manifiesto con nombre, descripción y colores GRI"
      contains: "#FF4C05"
  key_links:
    - from: "app_cliente/lib/app.dart"
      to: "NotFoundScreen"
      via: "errorBuilder de GoRouter"
      pattern: "errorBuilder"
    - from: "app_cliente/lib/features/pedidos/menu_mesa_screen.dart"
      to: "EmptyState"
      via: "guard de categorias.isEmpty antes del ListView"
      pattern: "isEmpty"
---

<objective>
Cerrar los agujeros de "no es amigable / parece un prototipo": estados vacíos guiados, branding propio
en lugar de los defaults de Flutter, y una pantalla 404 en ambas apps.

Purpose: la auditoría puntuó `app_cliente` con 19/60 y el panel con ~3.8/10, y el hallazgo más
dañino para la percepción de profesionalidad es el más barato de arreglar: el navegador dice
literalmente "A new Flutter project" con el azul de Flutter, y la app móvil arranca con el logo de
Flutter. Además, `menu_mesa_screen` deja al cliente en una pantalla en blanco justo después de
escanear el QR, que es el peor momento posible para parecer roto.
Output: `EmptyState` aplicado, assets de marca generados de forma determinista, manifest/index/favicon
del panel, ícono y splash del móvil, y `errorBuilder` en los dos routers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/audit/UI-REVIEW-app_cliente.md
@.planning/codebase/audit/UI-REVIEW-panel_admin.md
@app_cliente/lib/features/pedidos/menu_mesa_screen.dart
@app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart
@app_cliente/lib/features/reservas/mis_reservas_screen.dart
@app_cliente/lib/app.dart
@panel_admin/lib/app.dart
@panel_admin/web/manifest.json
@panel_admin/web/index.html
@app_cliente/lib/core/theme.dart
</context>

<interfaces>
<!-- Hechos auditados. Ubicaciones exactas. -->

Estado vacío CRÍTICO: `app_cliente/lib/features/pedidos/menu_mesa_screen.dart:116-141` — la rama
`data:` renderiza `ListView(children: [for (cat in detalle.categorias) ExpansionTile(...)])` SIN un
`if (detalle.categorias.isEmpty)`. Con un menú sin categorías el cuerpo queda completamente en blanco.
Es la pantalla a la que se llega DESPUÉS de escanear el QR de la mesa, o sea, a mitad del flujo.
`restaurante_detalle_screen.dart:142-151` ya hace lo correcto para el mismo caso ("Este restaurante
aún no tiene menú").

Patrón de estado vacío BUENO ya presente (usarlo como base del widget): `mis_reservas_screen.dart:62-77`
y `pedido_estado_screen.dart:120-140` — icono + titular + texto de ayuda + botón de acción.
Patrón POBRE a homogeneizar: `restaurantes_list_screen.dart:32-40` y `restaurante_detalle_screen.dart:142-151`
(solo un `Text` gris centrado).

Branding del panel, valores actuales vs. objetivo:
| Archivo | Hoy | Objetivo |
|---|---|---|
| `web/manifest.json` `name`/`short_name` | `gri_panel_admin` | `GRI — Panel Administrativo` / `GRI Panel` |
| `web/manifest.json` `description` | `A new Flutter project.` | descripción real del producto |
| `web/manifest.json` `theme_color`/`background_color` | `#0175C2` (azul demo de Flutter) | `#FF4C05` / `#F5F6F8` |
| `web/index.html:32` `<title>` | `gri_panel_admin` | `GRI — Panel Administrativo` |
| `web/favicon.png` + `web/icons/*` | logo de Flutter | marca GRI |

Marca GRI (del mockup `documentos/index.html`): cuadrado redondeado (`border-radius: 12px` sobre
45×45) con `background: var(--primary)` = `#FF4C05` y el glifo 🍽️ en blanco, junto al wordmark "GRI".
No existe ningún archivo de logo en `documentos/`: hay que generar el asset.

`errorBuilder` está disponible en `GoRouter(...)` de go_router 17.5.0 (verificado en el pub cache).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Widget EmptyState y aplicación a las listas sin guía</name>
  <files>app_cliente/lib/features/shared/empty_state.dart, app_cliente/lib/features/pedidos/menu_mesa_screen.dart, app_cliente/lib/features/restaurantes/restaurantes_list_screen.dart, app_cliente/lib/features/restaurantes/restaurante_detalle_screen.dart, app_cliente/test/pedidos/menu_vacio_test.dart, app_cliente/test/base_vacia_test.dart</files>
  <behavior>
    - `menu_mesa_screen` con un `RestauranteDetalle` de 0 categorías muestra un titular y un texto guía
      (p.ej. "Este restaurante aún no publicó su menú" / "Avísale al mesero para que te atienda"),
      NO un cuerpo vacío.
    - `menu_mesa_screen` con categorías sigue renderizando los `ExpansionTile` como hasta ahora
      (ninguna regresión en los tests de carrito/menú existentes).
    - `restaurantes_list_screen` con 0 restaurantes muestra icono + titular + guía, no solo un `Text` gris.
    - `restaurante_detalle_screen` sin menú muestra el mismo patrón, conservando su copy actual.
    - El texto guía de cada estado vacío es único y localizable por `find.text` en los tests.
  </behavior>
  <action>
Crear `app_cliente/lib/features/shared/empty_state.dart` con
`EmptyState({required String icono, required String titulo, required String guia, Widget? accion})`,
extrayendo la estructura del patrón bueno ya presente en `mis_reservas_screen.dart:62-77` (mismos
tamaños, mismos colores, mismo espaciado). Es EXTRACCIÓN, no rediseño: la decisión bloqueada del
usuario conserva la identidad visual actual.

Aplicarlo en los tres puntos: el guard nuevo de `menu_mesa_screen.dart` (el crítico) y la
homogeneización de `restaurantes_list_screen.dart` y `restaurante_detalle_screen.dart`. No tocar
`mis_reservas_screen` ni `pedido_estado_screen` en este plan salvo para migrarlos al widget si el
cambio es 1:1 y sus tests siguen pasando sin editar aserciones.

Crear `app_cliente/test/pedidos/menu_vacio_test.dart` con el caso crítico y su contrario, y endurecer
en `app_cliente/test/base_vacia_test.dart` las aserciones que el plan 11-02 dejó marcadas con
`// TODO(11-10)` — ahora sí deben afirmar el texto guía concreto en lugar de "no hay excepción".
(El marcador dice 11-10 por la numeración provisional; corresponde a ESTE plan.)
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test test/pedidos/ test/restaurantes/ test/base_vacia_test.dart</automated>
  </verify>
  <done>Ninguna lista de la app cliente queda en blanco; el caso post-escaneo con menú vacío muestra guía y tiene test de regresión.</done>
</task>

<task type="auto">
  <name>Tarea 2: Assets de marca generados y branding web del panel</name>
  <files>app_cliente/tool/gen_branding.dart, app_cliente/pubspec.yaml, app_cliente/assets/branding/, panel_admin/web/manifest.json, panel_admin/web/index.html, panel_admin/web/favicon.png, panel_admin/web/icons/</files>
  <action>
No existe ningún archivo de logo en el repo, así que el asset se GENERA de forma determinista
(nada de descargar imágenes ni de pedirle al usuario un PNG).

Crear `app_cliente/tool/gen_branding.dart`, un script Dart puro que use `package:image` (añadir como
`dev_dependency` de `app_cliente`; confirmar antes en pub.dev que el publisher es el habitual del
paquete y pinear versión exacta) para dibujar el ícono de marca:
- lienzo cuadrado configurable (1024 por defecto), fondo transparente
- cuadrado redondeado que ocupa el 100% del lienzo, radio ≈ 22% del lado, relleno `#FF4C05`
- glifo blanco de "plato con cubiertos" dibujado con primitivas: círculo con borde grueso centrado
  (el plato) y dos barras redondeadas verticales a los lados (cubiertos). Sin fuentes: el rendering
  de texto/emoji no es determinista entre plataformas.
- salidas: `app_cliente/assets/branding/icon_1024.png`, `icon_512.png`, y para el panel
  `panel_admin/web/icons/Icon-192.png`, `Icon-512.png`, `Icon-maskable-192.png`, `Icon-maskable-512.png`
  (las maskable con el mismo dibujo pero con un 20% de margen de seguridad) y `panel_admin/web/favicon.png` (64×64).
- el script debe ser idempotente y aceptar `--out` para el directorio base.

Ejecutarlo (`cd app_cliente && dart run tool/gen_branding.dart`) y commitear los PNG resultantes.

Editar `panel_admin/web/manifest.json` con los valores de la tabla del bloque `<interfaces>`,
manteniendo la estructura de `icons` que Flutter genera (y añadiendo las entradas maskable si no están).
Editar `panel_admin/web/index.html`: `<title>GRI — Panel Administrativo</title>`, `<meta name="description">`
real y `<meta name="theme-color" content="#FF4C05">`.

Además, añadir en `index.html` un **shell de carga con marca**: un `div` centrado con el color de
fondo `#F5F6F8`, el ícono y un indicador, que Flutter reemplaza al montar. Hoy el arranque
(`main.dart` espera `bootstrap()` antes de `runApp`) deja una pestaña en blanco que se lee como
"roto" y no como "cargando". Eliminar el `div` desde el `flutter_bootstrap`/`onEntrypointLoaded`
existente o con un pequeño script inline; no cambiar el orden del bootstrap de Firebase.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; dart run tool/gen_branding.dart &amp;&amp; test -s assets/branding/icon_1024.png &amp;&amp; test -s ../panel_admin/web/favicon.png &amp;&amp; echo "ASSETS_OK"</automated>
    <automated>node -e "const m=require('./panel_admin/web/manifest.json');if(m.theme_color.toUpperCase()!=='#FF4C05')process.exit(1);if(/A new Flutter project/.test(m.description))process.exit(1);if(/gri_panel_admin/.test(m.name))process.exit(1);console.log('MANIFEST_OK')"</automated>
    <automated>grep -q "GRI" panel_admin/web/index.html &amp;&amp; grep -vq "gri_panel_admin</title>" panel_admin/web/index.html &amp;&amp; echo "INDEX_OK"</automated>
  </verify>
  <done>El generador produce todos los assets, el manifiesto y el título del panel muestran identidad GRI con `#FF4C05`, y la pestaña ya no arranca en blanco.</done>
</task>

<task type="auto">
  <name>Tarea 3: Ícono y splash del móvil + pantalla 404 en ambos routers</name>
  <files>app_cliente/pubspec.yaml, app_cliente/lib/features/shared/not_found_screen.dart, app_cliente/lib/app.dart, app_cliente/test/router_404_test.dart, panel_admin/lib/features/shared/not_found_screen.dart, panel_admin/lib/app.dart, panel_admin/test/router_404_test.dart</files>
  <action>
Branding móvil: añadir `flutter_launcher_icons` y `flutter_native_splash` como `dev_dependencies` de
`app_cliente` (ambos están recomendados desde siempre en la documentación de stack del proyecto y
nunca se añadieron). Antes de instalarlos, comprobar su publisher en pub.dev
(`curl -s https://pub.dev/api/packages/<paquete>/publisher`) y pinear versión exacta.
Configurar en `pubspec.yaml`:
- `flutter_launcher_icons`: `image_path: assets/branding/icon_1024.png`, `android: true`,
  `adaptive_icon_background: "#FF4C05"`, `adaptive_icon_foreground` con el glifo, `web: true` si aplica.
- `flutter_native_splash`: `color: "#FF4C05"`, `image: assets/branding/icon_512.png`, `android_12` configurado.
Ejecutar ambos generadores y commitear la salida. Verificar que `android/app/src/main/res/mipmap-hdpi/ic_launcher.png`
ya no es el asset de 544 bytes de la plantilla de Flutter.

404: crear `NotFoundScreen` en cada app (`features/shared/not_found_screen.dart`), con el estilo ya
vigente de cada una: titular "Página no encontrada", la URI solicitada en texto secundario y un botón
que vuelve al inicio (`context.go('/')`). Cablearla con
`errorBuilder: (context, state) => NotFoundScreen(uri: state.uri)` en el `GoRouter` de
`app_cliente/lib/app.dart` y de `panel_admin/lib/app.dart`. En el panel, la ruta 404 debe seguir
respetando el guard de sesión: sin sesión, el redirect a `/login` manda; con sesión, se muestra el 404.

Tests `router_404_test.dart` en ambas apps: construir el `GoRouter` de la app (o uno equivalente con
el mismo `errorBuilder`), navegar a `/ruta-que-no-existe` y afirmar que `NotFoundScreen` se renderiza
y que el botón vuelve a `/`.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test test/router_404_test.dart</automated>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test test/router_404_test.dart</automated>
    <automated>test $(stat -c%s app_cliente/android/app/src/main/res/mipmap-hdpi/ic_launcher.png) -gt 1000 &amp;&amp; echo "ICONO_OK"</automated>
  </verify>
  <done>La app móvil ya no muestra el logo por defecto de Flutter, y una URL inexistente da una pantalla propia en las dos apps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → pub.dev | Se añaden 3 dependencias de desarrollo (`image`, `flutter_launcher_icons`, `flutter_native_splash`) |
| URL del navegador → router del panel | El 404 es alcanzable escribiendo cualquier ruta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-SC | Tampering | dev_dependencies nuevas de pub.dev | mitigate | Verificar el publisher de cada paquete vía la API de pub.dev antes de instalar y pinear versión exacta. Son `dev_dependencies`: no entran en el binario de producción |
| T-11-09-02 | Information Disclosure | `NotFoundScreen` mostrando la URI | mitigate | Se muestra la ruta solicitada, nunca query params ni fragmentos que puedan llevar tokens; recortar a la parte de path |
| T-11-09-03 | Spoofing | 404 alcanzable sin sesión en el panel | mitigate | El `redirect` del guard se evalúa antes que el `errorBuilder`: sin sesión se va a `/login`, no se filtra la existencia de rutas internas |
| T-11-09-04 | Information Disclosure | shell de carga en `index.html` | accept | Solo contiene marca y un spinner; ningún dato ni configuración |
</threat_model>

<verification>
- `cd app_cliente && flutter test && flutter analyze` → verde, 0 issues.
- `cd panel_admin && flutter test && flutter analyze` → verde, 0 issues.
- `cd panel_admin && flutter build web --release` → compila (comprobación de que el `index.html` editado no rompe el arranque).
- Inspección: `panel_admin/web/manifest.json` sin ninguna cadena `A new Flutter project` ni `#0175C2`.
</verification>

<success_criteria>
- Ninguna pantalla de lista se queda muda; el caso post-QR con menú vacío está cubierto y testeado.
- La primera impresión (pestaña, favicon, instalador PWA, ícono y splash del móvil) es GRI, no Flutter.
- Las URLs inválidas tienen respuesta propia en las dos apps.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-09-SUMMARY.md` al terminar.
</output>
