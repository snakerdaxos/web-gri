---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 12
type: execute
wave: 8
depends_on: ["11-11", "11-14"]
files_modified:
  - panel_admin/lib/core/theme.dart
  - panel_admin/lib/features/**
  - panel_admin/lib/features/shared/error_box.dart
  - panel_admin/test/core/sin_hex_crudos_test.dart
autonomous: true
requirements: [DS-01]

must_haves:
  truths:
    - "Ningún color de la UI del panel se declara con un hex crudo fuera del archivo de tema"
    - "El estilo de los botones primarios se declara una sola vez, no en cinco pantallas"
    - "La sombra de las tarjetas se declara una sola vez, no en cuatro"
    - "La apariencia de todas las pantallas es idéntica a la anterior a este plan"
  artifacts:
    - path: "panel_admin/test/core/sin_hex_crudos_test.dart"
      provides: "Gate automatizado contra la reaparición de hex crudos en el panel"
      min_lines: 20
    - path: "panel_admin/lib/features/shared/error_box.dart"
      provides: "Caja de error compartida en lugar de tres copias privadas"
      min_lines: 25
  key_links:
    - from: "panel_admin/lib/features/**"
      to: "GriColors / griTheme"
      via: "constantes con nombre en lugar de Color(0x...)"
      pattern: "GriColors\\."
---

<objective>
Migrar todos los literales de color y los estilos duplicados **del panel** a los tokens y al tema
creados en el plan 11-11, y dejar un gate que impida su reaparición.

(La misma migración para `app_cliente` es el plan 11-19, que corre en la MISMA ola y en paralelo: son
apps distintas y no comparten ni un archivo. Se separan porque el panel tiene 36 hex
crudos más cinco duplicaciones estructurales y el cliente otros 13 más la migración de `TextStyle`:
juntos consumen demasiado contexto para un solo ejecutor y la calidad se degrada justo donde el
trabajo es más mecánico y más fácil de estropear.)

Purpose: `theme.dart` del panel dice ser "fuente única de verdad visual para TODO el panel" y es falso:
36 hex crudos viven fuera, el estilo del botón primario está copiado en 5+ pantallas, la sombra de
tarjeta en 4 y `_ErrorBox` reimplementado en 3. Mientras eso siga así, cualquier cambio futuro de marca
exige editar a mano una docena de archivos y las inconsistencias seguirán acumulándose.
Output: cero hex crudos fuera del tema, cero `styleFrom` duplicados, una `ErrorBox` compartida y dos
tests que fallan si alguien reintroduce el patrón.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/audit/UI-REVIEW-panel_admin.md
@.planning/codebase/audit/UI-REVIEW-app_cliente.md
@panel_admin/lib/core/theme.dart
@app_cliente/lib/core/theme.dart
@panel_admin/lib/core/design_tokens.dart
@app_cliente/lib/core/design_tokens.dart
</context>

<interfaces>
<!-- REGLA DURA DE ESTE PLAN: es una migración 1:1, mecánica. -->
Cada hex crudo se sustituye por una constante con nombre **del mismo valor exacto**.
Está PROHIBIDO "aprovechar y ajustar" ningún color, redondeo, sombra o espaciado. Si algo se ve mal,
se anota como hallazgo — no se corrige aquí. El único cambio de color permitido en toda la fase es el
de contraste del texto secundario, y pertenece al plan 11-14.

Inventario auditado (panel, 36 hex crudos — muestra):
`app_shell.dart:94` `Color(0xFFEEEEEE)` (divider) · `app_shell.dart:201` `Color(0xFFAAAAAA)` ·
`app_shell.dart:253` `Color(0xFFCCCCCC)` · `menu_screen.dart:146/199/202`
`Color(0xFFE65100)/Color(0xFFFF8F00)/Color(0xFF9E9E9E)` (badges) · `reportes_screen.dart:382` `Color(0xFFE8F0FE)`.

El inventario equivalente de `app_cliente` (ámbar `#F5A623` con 9 usos, gradiente naranja con 4)
pertenece al plan 11-19; NO tocar `app_cliente/` desde aquí.

Duplicaciones estructurales a eliminar:
- Estilo del botón primario copiado en `login_screen.dart:156-166`, `dashboard_screen.dart:137-143` y
  `:231-234`, FAB de `mesas_screen.dart`, `cocina_screen.dart:311-315` → ahora vive en
  `griTheme.elevatedButtonTheme` (plan 11-11).
- `BoxShadow(color: Color(0x0D000000), blurRadius: 12, offset: Offset(0,3))` en `stat_card.dart:38-44`,
  `dashboard_screen.dart:109-115`, `pedido_card.dart:46-52`, `reportes_screen.dart:350-356` →
  `griCardDecoration` (plan 11-11).
- `_ErrorBox` reimplementado en `dashboard_screen.dart:210-242`, `mesas_screen.dart:115-147` y el
  patrón equivalente en `cocina_screen.dart:294-323`.

Paddings de página divergentes sin motivo: `EdgeInsets.all(30)` (dashboard, cocina) vs `all(24)`
(reservas, clientes, reportes) vs `fromLTRB(24,16,24,4)` (menú). Unificar **solo** los que ya coinciden
entre sí a `GriSpacing`; donde el valor difiera (30 vs 24), NO cambiarlo: nombrarlo
(`GriSpacing.pagina`) con el valor que ya tiene cada pantalla o dejarlo tal cual y anotarlo. Cambiar
un 30 por un 24 es un cambio visual.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Migrar los hex crudos del panel a GriColors</name>
  <files>panel_admin/lib/core/theme.dart, panel_admin/lib/features/**</files>
  <action>
Inventariar primero: `grep -rn "Color(0x" panel_admin/lib --include=*.dart | grep -v "core/theme.dart"`.
El inventario DEBE incluir el código nuevo de esta misma fase, que no puede quedar fuera del sistema
de diseño: `features/equipo/` (plan 11-10), `features/bootstrap/` (plan 11-07),
`features/shared/responsive_page.dart` y `core/gri_icons.dart` (plan 11-21).
**Orden nuevo, deliberado:** este plan corre DESPUÉS del trabajo visual (11-21) y de accesibilidad
(11-14), no antes. El motivo lo decidió el usuario: lo visible va primero y la limpieza interna
después. El efecto secundario está aceptado — el plan 11-21 escribirá algún literal a mano que aquí se
migra—, y a cambio se migra código ya asentado en vez de código que va a reescribirse.
Guardar la lista completa en el SUMMARY del plan como evidencia del antes.

Para cada literal, añadir a `GriColors` una constante con nombre semántico (qué ES, no qué color es:
`divider`, `iconMuted`, `badgeAgotadoFg`, `badgeNuevoBg`, `chartTint`…) con el **mismo valor hex**, y
sustituir el uso. Agrupar las constantes nuevas por sección con comentarios, siguiendo la organización
que ya tiene el archivo.

Reemplazar también las sombras duplicadas por `griCardDecoration` y los `styleFrom` de botón por el
estilo del tema (borrando el `style:` local cuando sea idéntico; si algún botón difiere a propósito,
dejarlo y comentar por qué).

Crear `panel_admin/lib/features/shared/error_box.dart` con la caja de error compartida, tomando como
base la implementación de `dashboard_screen.dart:210-242`, y sustituir las tres copias privadas.

No tocar la sección de colores de mesa de `theme.dart:35-85`: la auditoría la señala como la única
parte del sistema de diseño bien ejecutada.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
    <automated>cd panel_admin &amp;&amp; test $(grep -rn "Color(0x" lib --include=*.dart | grep -v "core/theme.dart" | grep -v "core/design_tokens.dart" | wc -l) -eq 0 &amp;&amp; echo "SIN_HEX_CRUDOS"</automated>
  </verify>
  <done>Cero `Color(0x...)` fuera de `core/theme.dart`, cero sombras y estilos de botón duplicados, una sola `ErrorBox`, y la suite del panel verde sin cambiar ningún valor esperado.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Gate automatizado contra la reaparición de literales en el panel</name>
  <files>panel_admin/test/core/sin_hex_crudos_test.dart</files>
  <behavior>
    - El test recorre `lib/**/*.dart` excluyendo `core/theme.dart` y `core/design_tokens.dart`.
    - Si encuentra un `Color(0x...)` fuera de esos archivos, falla e imprime archivo, línea y el literal.
    - Una línea con el comentario `// TOKEN-IGNORE: motivo` en la misma línea o en la anterior queda exenta.
    - El test pasa con el estado final de la Tarea 1.
    - Verificado por construcción: introducir un `Color(0xFF123456)` temporal en una pantalla hace fallar el test.
  </behavior>
  <action>
Crear el test en `panel_admin` (test puro con `dart:io`, sin widget tree). El plan 11-19 crea su
gemelo en `app_cliente`: dejar en la cabecera un comentario de sincronización nombrándolo. Debe ignorar líneas
comentadas: filtrar las que empiecen por `//` tras recortar espacios, para que la propia documentación
del archivo no se autoinvalide. Incluir en el mensaje de fallo la instrucción de qué hacer
("promueve el color a `GriColors` con un nombre semántico y el MISMO valor").

Comprobar el gate manualmente inyectando un literal temporal, confirmando el fallo y revirtiéndolo;
dejar constancia en el SUMMARY.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter test test/core/sin_hex_crudos_test.dart</automated>
  </verify>
  <done>El panel tiene un gate que falla si vuelve a aparecer un color suelto, con escape documentado para los casos legítimos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna | Refactor de presentación; no toca autorización, datos ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-12-01 | Tampering | deriva visual durante la migración | mitigate | Migración 1:1 obligatoria, prohibición explícita de "aprovechar y ajustar", y las 175+ pruebas existentes (incluidas las de color de mesa y de chip) como red de seguridad sin editar valores esperados |
| T-11-12-02 | Tampering | reaparición de literales tras la fase | mitigate | Gate `sin_hex_crudos_test.dart` en ambas apps, con escape explícito y comentado |
</threat_model>

<verification>
- `cd panel_admin && flutter test && flutter analyze` → verde, 0 issues.
- `git diff --stat` revisado: los cambios son sustituciones, no reescrituras de layout.
- Revisión manual del diff buscando cualquier hex cuyo VALOR cambie → debe ser cero.
</verification>

<success_criteria>
- `panel_admin/lib/core/theme.dart` es de verdad la fuente única de verdad visual que su comentario afirma ser.
- Un cambio futuro de marca en el panel se hace en un archivo, no en una docena.
- La apariencia del producto no ha cambiado.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-12-SUMMARY.md` al terminar.
</output>
