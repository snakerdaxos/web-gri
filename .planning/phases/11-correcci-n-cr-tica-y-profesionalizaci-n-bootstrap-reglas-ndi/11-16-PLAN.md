---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 16
type: execute
wave: 10
depends_on: ["11-15"]
files_modified:
  - docs/DEPLOY-FASE-11.md
autonomous: false
requirements: [E2E-02]

must_haves:
  truths:
    - "Las rules y los índices de la fase están desplegados en el proyecto real"
    - "El índice compuesto de categorias está construido y el menú del panel carga contra el proyecto real"
    - "El menú del cliente carga contra el proyecto real con un producto agotado"
    - "Esta verificación NO depende de que el usuario acepte subir a Blaze"
  artifacts:
    - path: "docs/DEPLOY-FASE-11.md"
      provides: "Runbook de despliegue completo, con las dos fases separadas por requisito de facturación"
      min_lines: 70
  key_links:
    - from: "firestore.indexes.json"
      to: "índices construidos en p-gri-b5b40"
      via: "firebase deploy --only firestore:indexes"
      pattern: "firestore:indexes"
---

<objective>
Desplegar al proyecto Firebase real la parte de la fase que NO requiere Blaze —rules e índices— y
validar contra él lo que ningún emulador puede validar: que el índice compuesto existe y que los dos
bugs P0 del menú están realmente cerrados en producción.

Purpose: el emulador de Firestore **no valida índices compuestos**, así que este checkpoint es la
ÚNICA prueba posible del bug del índice. Va separado del despliegue de Cloud Functions (plan 11-20)
precisamente para que no quede rehén de la decisión de facturación: si el usuario decide no subir a
Blaze, esta verificación se hace igual.
Output: runbook de despliegue completo y el sellado humano de la parte Firestore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/SMOKE-E2E-v2.md
@docs/FIREBASE_SETUP.md
@.planning/STATE.md
@firebase.json
@firestore.indexes.json
@firestore.rules
</context>

<interfaces>
<!-- Hechos verificados que condicionan este plan. -->
- "You can emulate functions in any Firebase project, but to deploy functions, your project must be on
  the Blaze pricing plan." → Blaze condiciona SOLO el despliegue de funciones. `firestore:rules` y
  `firestore:indexes` se despliegan en el plan Spark sin ningún problema.
- Los índices nuevos **tardan minutos en construirse** tras el deploy. Hay que esperar a estado
  `READY` antes de dar por bueno el menú del panel; si no, el síntoma es idéntico al del bug.
- El estado real de rules e índices en `p-gri-b5b40` es DESCONOCIDO: el deploy de la Fase 10 quedó
  como checkpoint humano pendiente. Verificar con `firebase firestore:indexes` antes de asumir nada.
- El emulador de Firestore no valida índices compuestos: "The Firestore emulator does not track
  composite indexes and will instead execute any valid query." Por eso este checkpoint existe.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Runbook de despliegue de la fase</name>
  <files>docs/DEPLOY-FASE-11.md</files>
  <action>
Escribir `docs/DEPLOY-FASE-11.md` con el procedimiento exacto y los comandos literales (todos desde
`scripts/`, que es donde vive `firebase-tools`), **organizado en dos fases separadas por su requisito
de facturación**, porque son independientes:

**FASE A — Firestore (NO requiere Blaze).** Es la de este plan.
1. Pre-vuelo: `npm run gates` en verde; `firebase login`; `firebase use default`;
   `firebase firestore:indexes` para registrar el estado ACTUAL del proyecto (puede que el deploy de
   la Fase 10 nunca se hiciera).
2. `firebase deploy --only firestore:rules` y verificar en la consola que la fecha de publicación cambió.
3. `firebase deploy --only firestore:indexes`, y luego esperar: repetir `firebase firestore:indexes`
   hasta que el índice `categorias(restauranteId ASC, orden ASC)` aparezca construido. Documentar que
   mientras se construye, el síntoma en la app es EXACTAMENTE el mismo que el del bug
   (`FAILED_PRECONDITION` / "The query requires an index"), así que no hay que sacar conclusiones antes.
4. Verificación contra el proyecto real (lo que ningún emulador demuestra):
   - menú del panel carga sin `FAILED_PRECONDITION` → prueba del índice
   - menú del cliente carga con un producto marcado como agotado y una categoría inactiva → prueba del
     bug de query vs rules
   - un usuario staff de un restaurante NO ve datos de otro (comprobación rápida de aislamiento)
5. Reversión de la Fase A: `git checkout` de la versión anterior de `firestore.rules` y redeploy.
   Los índices NO se borran automáticamente al revertir; documentarlo (borrar un índice es manual en
   la consola y no es urgente: un índice de más no rompe nada).

**FASE B — Cloud Functions (REQUIERE Blaze).** Es el plan 11-20; aquí solo se documenta el
procedimiento para que el runbook esté completo: subir a Blaze con presupuesto de alerta, crear
`functions/.env` con `BOOTSTRAP_EMAIL` y `BOOTSTRAP_SECRET`, `firebase deploy --only functions`
(el primer despliegue puede pedir habilitar Cloud Build, Artifact Registry, Cloud Run y Eventarc: el
CLI las habilita solo, pero puede fallar por permisos), y verificación de `/bootstrap` y del alta de
staff. Reversión: `firebase functions:delete` por función; lo NO reversible son los claims ya
asignados y el centinela de bootstrap una vez creado.

Cerrar con "Qué queda como deuda conocida": App Check no habilitado en las callables, sin flujo de
invitación por correo para el staff, sin baja/desactivación de staff desde el panel, y la huella SHA-1
de release pendiente de registrar para Google Sign-In cuando se firme un APK de producción.
  </action>
  <verify>
    <automated>grep -q "FASE A" docs/DEPLOY-FASE-11.md &amp;&amp; grep -q "FASE B" docs/DEPLOY-FASE-11.md &amp;&amp; grep -q "firestore:indexes" docs/DEPLOY-FASE-11.md &amp;&amp; grep -q "BOOTSTRAP_SECRET" docs/DEPLOY-FASE-11.md &amp;&amp; grep -q "READY" docs/DEPLOY-FASE-11.md &amp;&amp; test $(wc -l &lt; docs/DEPLOY-FASE-11.md) -ge 70 &amp;&amp; echo "RUNBOOK_DEPLOY_OK"</automated>
    <automated>cd scripts &amp;&amp; npm run gates</automated>
  </verify>
  <done>El runbook separa explícitamente lo que necesita Blaze de lo que no, cubre pre-vuelo, deploy ordenado, espera de índices, verificación real y reversión de cada fase; y todos los gates automatizados están en verde antes de pedir nada al humano.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarea 2: Checkpoint humano A — deploy de rules e índices y prueba real del bug del índice</name>
  <files>docs/DEPLOY-FASE-11.md (solo lectura durante el checkpoint)</files>
  <action>
Pausar y presentar el checkpoint. NO intentar `firebase login` ni `firebase deploy` de forma autónoma:
requieren credenciales interactivas. Presentar `<what-built>`, las instrucciones de `<how-to-verify>` y
esperar el resume-signal. Si el usuario reporta fallos, recogerlos literalmente (paso y mensaje exacto)
para alimentar un plan de cierre de brechas.

Este checkpoint **no menciona Blaze ni facturación**: todo lo que pide se puede hacer en el plan Spark
actual. El de Cloud Functions es el plan 11-20, aparte y a propósito.
  </action>
  <what-built>
Los dos bugs P0 de Firestore están corregidos y cubiertos por tests automatizados:
- El menú del cliente ya filtra `activo` y `disponible` en la query, replicando lo que exige la regla
  (antes la consulta se denegaba SIEMPRE, no solo con productos agotados).
- El índice compuesto `categorias(restauranteId, orden)` está declarado en `firestore.indexes.json`.
- `firestore.rules` tiene suite automatizada en las 9 colecciones, más la regla nueva de lectura de
  equipo acotada al `rid` y el blindaje del centinela de bootstrap.
- `npm run audit:indexes` verifica que ninguna query se quede sin índice y que ninguna pierda un
  filtro que la regla exige.

Pero hay un límite duro: **el emulador de Firestore no valida índices compuestos**. Ninguna prueba
local puede demostrar que el bug del índice está cerrado — solo el proyecto real puede. Eso es lo que
verifica este checkpoint. No requiere Blaze ni ningún cambio de facturación.
  </what-built>
  <how-to-verify>
Sigue la **FASE A** de `docs/DEPLOY-FASE-11.md`. En resumen:

1. Desde `scripts/`: `firebase login` y `firebase use default`.
2. Registra el estado de partida: `firebase firestore:indexes` (guarda la salida; puede que el deploy
   de la Fase 10 nunca llegara a hacerse).
3. `firebase deploy --only firestore:rules`
4. `firebase deploy --only firestore:indexes`
5. **Espera** y repite `firebase firestore:indexes` hasta que aparezca construido el índice de
   `categorias` sobre `restauranteId` + `orden`. Tarda minutos. Mientras se construye, el error en la
   app es idéntico al del bug, así que no saques conclusiones antes de que esté listo.
6. Verifica contra el proyecto real (con las apps apuntando a producción, es decir SIN
   `--dart-define=USE_EMULATORS=true`):
   a. Abre el menú del panel (Configuración → Menú): debe cargar sin "The query requires an index".
   b. Marca un producto como agotado y desactiva una categoría desde el panel.
   c. Abre ese restaurante en la app cliente: el menú debe cargar igualmente, sin mostrar ni el
      producto agotado ni la categoría inactiva. Antes de esta fase, esta pantalla fallaba siempre.
   d. Inicia sesión con un staff de un restaurante y comprueba que no ve datos de otro.

Si algo falla, anota el paso y el mensaje exacto.
  </how-to-verify>
  <verify>
    <automated>cd scripts &amp;&amp; npm run gates</automated>
    <human-check>Rules e índices desplegados en p-gri-b5b40, índice de categorias construido, y los cuatro puntos del paso 6 comprobados</human-check>
  </verify>
  <resume-signal>Escribe "approved" si el deploy y las cuatro verificaciones pasaron, o describe qué falló y en qué paso</resume-signal>
  <done>El usuario confirma que las rules y los índices están desplegados y que el menú del panel y el del cliente funcionan contra el proyecto real, o reporta las fallas para un plan de cierre de brechas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → proyecto Firebase de producción | El despliegue de rules cambia la capa de autorización de un sistema vivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16-01 | Denial of Service | despliegue de rules que rompa el sistema vivo | mitigate | Deploy por partes con verificación entre cada una y sección de reversión en el runbook; la suite completa de rules corrió en verde antes |
| T-11-16-02 | Denial of Service | menú roto por índices aún en construcción | mitigate | Paso explícito de esperar a `READY` y aviso de que el síntoma intermedio es idéntico al del bug |
| T-11-16-03 | Elevation of Privilege | regla nueva de lectura de `usuarios` en producción | mitigate | Verificación de aislamiento entre tenants incluida en el paso 6.d, además de los tests de rules ya en verde |
| T-11-16-04 | Repudiation | dar el bug del índice por cerrado sin prueba | mitigate | Este checkpoint es la prueba, y se ha separado del de facturación para que no pueda quedar sin ejecutarse |
</threat_model>

<verification>
- `cd scripts && npm run gates` → exit 0 antes de pedir nada al humano.
- Confirmación humana de los cuatro puntos de verificación contra `p-gri-b5b40`.
- `firebase firestore:indexes` muestra el índice de `categorias` construido.
</verification>

<success_criteria>
- Los dos bugs P0 de Firestore están comprobados contra el proyecto real, no solo contra emuladores.
- Esta comprobación se ha podido hacer sin tocar la facturación.
- El checkpoint pendiente de la Fase 10 sobre rules e índices queda absorbido y cerrado.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-16-SUMMARY.md` al terminar.
</output>
