---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 16
type: execute
wave: 10
depends_on: ["11-15"]
files_modified:
  - docs/DEPLOY-FASE-11.md
  - docs/SMOKE-E2E-v2.md
autonomous: false
requirements: [E2E-02]
user_setup:
  - service: firebase-blaze
    why: "Cloud Functions solo se pueden DESPLEGAR con el plan Blaze (emularlas funciona en cualquier plan)"
    dashboard_config:
      - task: "Subir el proyecto p-gri-b5b40 al plan Blaze (pago por uso) y fijar un presupuesto de alerta"
        location: "Firebase Console → ⚙ Configuración del proyecto → Uso y facturación → Detalles y configuración"
      - task: "Definir BOOTSTRAP_EMAIL en functions/.env antes de desplegar"
        location: "Repositorio local, archivo functions/.env (no se commitea)"
      - task: "Habilitar el proveedor Email/Password si aún no lo está"
        location: "Firebase Console → Authentication → Sign-in method"

must_haves:
  truths:
    - "Las rules, los índices y las dos Cloud Functions están desplegados en el proyecto real"
    - "Los índices compuestos terminaron de construirse y el menú del panel carga contra el proyecto real"
    - "El flujo completo funciona contra el proyecto real, no solo contra emuladores"
  artifacts:
    - path: "docs/DEPLOY-FASE-11.md"
      provides: "Runbook de despliegue con comandos exactos y orden"
      min_lines: 60
  key_links:
    - from: "firestore.indexes.json"
      to: "índices construidos en p-gri-b5b40"
      via: "firebase deploy --only firestore:indexes"
      pattern: "firestore:indexes"
---

<objective>
Desplegar la fase al proyecto Firebase real y validar contra él lo que ningún emulador puede validar:
los índices compuestos y el comportamiento de las Cloud Functions desplegadas.

Purpose: el plan Blaze solo hace falta para DESPLEGAR funciones, no para emularlas — por eso toda la
fase se ha podido desarrollar y probar sin tocar la facturación. Este plan aísla ese único paso que
exige decisión humana y acceso a la consola, igual que hizo la Fase 10 con su checkpoint de deploy.
Output: runbook de despliegue y el sellado humano de la fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/SMOKE-E2E-v2.md
@docs/FIREBASE_SETUP.md
@.planning/STATE.md
@firebase.json
@firestore.indexes.json
</context>

<interfaces>
<!-- Hechos verificados que condicionan este plan. -->
- "You can emulate functions in any Firebase project, but to deploy functions, your project must be on
  the Blaze pricing plan." → Blaze es requisito SOLO del despliegue.
- Cloud Functions v2 requiere habilitar Cloud Build, Artifact Registry, Cloud Run y Eventarc. El CLI
  las habilita en el primer despliegue, pero puede fallar por permisos: preverlo en el runbook.
- Los índices nuevos **tardan minutos en construirse** tras el deploy. Hay que esperar a que su estado
  sea `READY` antes de dar por bueno el menú del panel.
- El estado real de rules e índices en `p-gri-b5b40` es DESCONOCIDO: el deploy de la Fase 10 quedó como
  checkpoint humano pendiente. Verificar con `firebase firestore:indexes` antes de asumir nada.
- Node local es 24 y el runtime desplegado es 22: el CLI avisará del desajuste. Es esperado.
- Región de las funciones: `us-central1` en el servidor y en el cliente. Un desajuste da un 404 opaco
  que en Flutter Web se disfraza de error de CORS.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Runbook de despliegue de la fase</name>
  <files>docs/DEPLOY-FASE-11.md</files>
  <action>
Escribir `docs/DEPLOY-FASE-11.md` con el procedimiento exacto, en este orden y con los comandos
literales (todos desde `scripts/`, que es donde vive `firebase-tools`):

1. **Pre-vuelo (antes de tocar la consola):** `npm run gates` en verde; `firebase login`;
   `firebase use default`; `firebase firestore:indexes` para registrar el estado ACTUAL del proyecto
   (puede que el deploy de la Fase 10 nunca se hiciera).
2. **Blaze:** pasos de consola para subir el plan y fijar un presupuesto de alerta. Indicar el orden
   de magnitud esperado del coste para este uso (dos callables invocadas unas pocas veces al día, sin
   triggers de Firestore) y que sin tráfico el coste tiende a cero.
3. **Configuración de la función:** crear `functions/.env` con `BOOTSTRAP_EMAIL` (el correo real de
   quien va a inicializar la plataforma). Advertir de que si no está, la función falla cerrada a propósito.
4. **Deploy por partes, en este orden y verificando cada una:**
   `firebase deploy --only firestore:rules` → `firebase deploy --only firestore:indexes` →
   esperar a `READY` (`firebase firestore:indexes` hasta que el índice de `categorias` aparezca
   construido) → `firebase deploy --only functions`.
   Documentar que el primer deploy de functions puede pedir habilitar APIs de GCP y qué hacer si falla
   por permisos.
5. **Verificación contra el proyecto real** (lo que los emuladores NO pueden demostrar):
   - abrir el menú del panel → debe cargar sin `FAILED_PRECONDITION` (prueba del índice)
   - abrir el menú del cliente con un producto agotado → debe cargar (prueba del bug de query vs rules)
   - `/bootstrap` con el correo configurado → crea el super_admin; una segunda llamada debe fallar
   - alta de staff desde `/equipo` → el nuevo usuario inicia sesión y ve su restaurante
6. **Reversión:** cómo volver atrás (redeploy de las rules anteriores desde git, `firebase functions:delete`
   de cada función) y qué NO es reversible (los claims ya asignados, el centinela de bootstrap una vez creado).

Añadir al final una sección "Qué queda como deuda conocida": App Check no habilitado en las callables,
sin flujo de invitación por correo para el staff, sin baja/desactivación de staff desde el panel.
  </action>
  <verify>
    <automated>grep -q "firestore:indexes" docs/DEPLOY-FASE-11.md &amp;&amp; grep -q "BOOTSTRAP_EMAIL" docs/DEPLOY-FASE-11.md &amp;&amp; grep -q "READY" docs/DEPLOY-FASE-11.md &amp;&amp; test $(wc -l &lt; docs/DEPLOY-FASE-11.md) -ge 60 &amp;&amp; echo "RUNBOOK_DEPLOY_OK"</automated>
    <automated>cd scripts &amp;&amp; npm run gates</automated>
  </verify>
  <done>El runbook existe, cubre pre-vuelo, Blaze, configuración, deploy ordenado, verificación real y reversión; y todos los gates automatizados de la fase están en verde antes de pedir nada al humano.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Tarea 2: Checkpoint humano — Blaze, despliegue real y smoke contra el proyecto</name>
  <files>docs/DEPLOY-FASE-11.md (solo lectura durante el checkpoint), functions/.env (lo crea el usuario, no se commitea)</files>
  <action>
Pausar la ejecucion y presentar al usuario el checkpoint de despliegue. NO intentar `firebase login`,
`firebase deploy` ni la subida a Blaze de forma autonoma: requieren credenciales interactivas y una
decision de facturacion que solo el usuario puede tomar. Presentar el bloque <what-built>, las
instrucciones de <how-to-verify> y esperar el resume-signal. Si el usuario reporta fallos, recogerlos
literalmente (paso y mensaje exacto) para que alimenten un plan de cierre de brechas.
  </action>
  <what-built>
Toda la fase 11 está implementada y verificada contra emuladores:
- Bootstrap del primer `super_admin` desde `/bootstrap` (función de un solo uso, con guarda atómica y tests de concurrencia)
- Alta de restaurante con slug y alta de staff con la matriz de roles, validada en la Cloud Function
- Bug del menú del cliente (query vs rules) corregido, con suite de rules que lo demuestra
- Índice compuesto de `categorias` declarado + audit estático que impide futuras queries sin índice
- Suite completa de `firestore.rules` (9 colecciones), tests de base vacía, y gates de accesibilidad
- Quick wins de UI, branding GRI, 404 y sistema de diseño con responsive real

Lo único que NO se ha hecho automáticamente es lo que exige decisión humana y acceso a la consola:
subir a Blaze y desplegar. El plan Blaze **solo hace falta para desplegar** funciones; emularlas
funciona en cualquier plan, y por eso toda la fase se ha desarrollado sin tocar la facturación.
  </what-built>
  <how-to-verify>
Seguir `docs/DEPLOY-FASE-11.md` de principio a fin. Resumen de lo que hay que hacer:

1. Decidir si aceptas subir el proyecto `p-gri-b5b40` al plan **Blaze** (pago por uso). Sin esto, las
   Cloud Functions no se pueden desplegar y el bootstrap y el alta de staff solo funcionan en emuladores.
   Fija un presupuesto de alerta en la consola.
2. Crear `functions/.env` con `BOOTSTRAP_EMAIL=<tu correo>`. Sin esa variable la función se niega a
   inicializar nada (es la mitigación de escalada de privilegios que pediste).
3. Desde `scripts/`: `firebase login`, `firebase use default`, y desplegar EN ESTE ORDEN:
   `firebase deploy --only firestore:rules`
   `firebase deploy --only firestore:indexes` → esperar a que el índice de `categorias` esté `READY`
   `firebase deploy --only functions`
4. Verificar contra el proyecto REAL (esto es lo que ningún emulador puede demostrar):
   - El menú del panel carga (si falla con "The query requires an index", el índice aún se está construyendo: esperar)
   - El menú del cliente carga con un producto marcado como agotado
   - `/bootstrap` crea tu super_admin; volver a intentarlo falla
   - Desde `/equipo` das de alta un mesero y ese mesero puede iniciar sesión
5. Ejecutar el runbook `docs/SMOKE-E2E-v2.md` completo, ya sea contra emuladores o contra el proyecto real.

Si algo falla, anota el paso y el mensaje exacto: se convertirá en el plan de cierre de brechas.
  </how-to-verify>
  <verify>
    <automated>cd scripts &amp;&amp; npm run gates</automated>
    <human-check>Deploy real completado y los 4 puntos de verificación del paso 4 comprobados contra p-gri-b5b40</human-check>
  </verify>
  <resume-signal>Escribe "approved" si el deploy y las verificaciones pasaron, o describe qué falló y en qué paso</resume-signal>
  <done>El usuario confirma que rules, índices y funciones están desplegados y que las cuatro verificaciones contra el proyecto real pasan, o reporta las fallas para un plan de cierre de brechas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → proyecto Firebase de producción | El despliegue cambia la capa de autorización de un sistema vivo |
| internet → callables desplegadas | A partir del deploy, las funciones son invocables desde fuera |
| operador → facturación | Blaze abre un coste variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16-01 | Elevation of Privilege | ventana entre el deploy de functions y el bootstrap | mitigate | `BOOTSTRAP_EMAIL` se configura ANTES del deploy y la función falla cerrada sin ella: no existe ventana en la que un desconocido pueda hacerse super_admin |
| T-11-16-02 | Denial of Service | despliegue de rules que rompa el sistema vivo | mitigate | Orden de deploy por partes con verificación entre cada una, y sección de reversión en el runbook. La suite completa de rules corrió en verde antes |
| T-11-16-03 | Denial of Service | menú roto por índices aún en construcción | mitigate | Paso explícito de esperar a `READY` antes de dar el deploy por bueno; el síntoma esperado está documentado |
| T-11-16-04 | Denial of Service | coste inesperado en Blaze | mitigate | Presupuesto de alerta en la consola como paso obligatorio del runbook; sin tráfico el coste tiende a cero |
| T-11-16-05 | Spoofing | callables invocadas por bots | accept | App Check queda DIFERIDO y documentado como deuda de seguridad conocida; mitigación parcial vía `maxInstances` y la matriz de autorización |
</threat_model>

<verification>
- `cd scripts && npm run gates` → exit 0 antes de pedir nada al humano.
- Confirmación humana de los 4 puntos de verificación contra `p-gri-b5b40`.
- `firebase firestore:indexes` muestra el índice de `categorias` construido.
</verification>

<success_criteria>
- La fase está desplegada y comprobada contra el proyecto real, no solo contra emuladores.
- El único paso que exigía decisión humana (facturación) queda aislado y documentado.
- El checkpoint pendiente de la Fase 10 queda absorbido y cerrado.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-16-SUMMARY.md` al terminar.
</output>
