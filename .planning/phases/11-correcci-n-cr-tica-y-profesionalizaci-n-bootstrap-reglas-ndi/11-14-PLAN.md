---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 14
type: execute
wave: 7
depends_on: ["11-13", "11-21"]
files_modified:
  - app_cliente/lib/features/restaurantes/home_screen.dart
  - app_cliente/lib/features/pedidos/menu_mesa_screen.dart
  - app_cliente/lib/features/pagos/calificacion_sheet.dart
  - app_cliente/lib/core/theme.dart
  - app_cliente/test/a11y/a11y_test.dart
  - panel_admin/lib/features/shared/app_shell.dart
  - panel_admin/lib/features/mesas/mesa_actions_sheet.dart
  - panel_admin/lib/features/mesas/mesas_screen.dart
  - panel_admin/lib/features/menu/menu_screen.dart
  - panel_admin/lib/features/dashboard/dashboard_screen.dart
  - panel_admin/lib/features/dashboard/widgets/mesa_tile.dart
  - panel_admin/lib/features/equipo/equipo_screen.dart
  - panel_admin/lib/core/theme.dart
  - panel_admin/test/a11y/a11y_test.dart
autonomous: true
requirements: [DS-03]

must_haves:
  truths:
    - "Todo control táctil de las pantallas del camino crítico mide al menos 48x48 y tiene etiqueta"
    - "Un lector de pantalla puede identificar los botones de icono y la navegación del panel"
    - "El texto secundario cumple el contraste mínimo AA sobre el fondo en que se usa"
    - "Las guías de accesibilidad se comprueban con tests, no con revisión manual"
  artifacts:
    - path: "app_cliente/test/a11y/a11y_test.dart"
      provides: "Gates meetsGuideline del camino crítico del cliente"
      min_lines: 40
    - path: "panel_admin/test/a11y/a11y_test.dart"
      provides: "Gates meetsGuideline del camino crítico del panel"
      min_lines: 40
  key_links:
    - from: "app_cliente/lib/features/restaurantes/home_screen.dart"
      to: "androidTapTargetGuideline"
      via: "botón QR ampliado a 48dp"
      pattern: "48"
---

<objective>
Cerrar la brecha de accesibilidad de las dos apps —cero `Semantics` en 68 + 40 archivos, botones de
icono sin etiqueta, un CTA principal por debajo del mínimo táctil y texto secundario por debajo del
contraste AA— y convertir esos hallazgos en gates automatizados.

Purpose: la auditoría puntuó accesibilidad con 1/10 en el cliente y 3/10 en el panel. Es la mitad de
"no es amigable el interfaz". `flutter_test` trae las guías integradas, así que cada arreglo puede
quedar blindado con tres líneas de test.
Output: etiquetas y tooltips, tamaños táctiles corregidos, contraste del texto secundario ajustado y
dos suites `a11y` que fallan si se rompe cualquiera de las tres cosas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/audit/UI-REVIEW-app_cliente.md
@.planning/codebase/audit/UI-REVIEW-panel_admin.md
@app_cliente/lib/features/restaurantes/home_screen.dart
@app_cliente/lib/features/pagos/calificacion_sheet.dart
@app_cliente/lib/features/pedidos/menu_mesa_screen.dart
@panel_admin/lib/features/shared/app_shell.dart
@panel_admin/lib/features/mesas/mesa_actions_sheet.dart
@app_cliente/lib/core/theme.dart
@panel_admin/lib/core/theme.dart
</context>

<interfaces>
<!-- Guías integradas en flutter_test. Tres líneas por test. -->
```dart
testWidgets('login cumple tap targets Android (48dp)', (tester) async {
  final handle = tester.ensureSemantics();
  await tester.pumpWidget(_login());
  await expectLater(tester, meetsGuideline(androidTapTargetGuideline));
  await expectLater(tester, meetsGuideline(labeledTapTargetGuideline));
  handle.dispose();
});
```
`androidTapTargetGuideline` (mínimo 48×48) · `iOSTapTargetGuideline` (44×44) ·
`textContrastGuideline` (ratio WCAG) · `labeledTapTargetGuideline` (todo nodo con acción de tap
debe tener label).

Hallazgos que estos gates convierten en automáticos:
- `app_cliente/lib/features/restaurantes/home_screen.dart:295-317` (`_QrButton`):
  `SizedBox(width: 45, height: 45)` — es el CTA principal "escanear mesa" y está por debajo de 48.
- De 6 `IconButton` en el cliente, solo 1 tiene `tooltip` (`menu_mesa_screen.dart:233`). Sin etiqueta:
  los de cantidad +/− del carrito (`menu_mesa_screen.dart:238-256`) y las 5 estrellas
  (`calificacion_sheet.dart:211-223`).
- En el panel: 0 `Semantics` y 1 solo `tooltip` (`menu_screen.dart:156`). Sin etiqueta: los
  `ListTile` de acciones de mesa (`mesa_actions_sheet.dart:122-132`) y el sidebar colapsado a 70px
  (`app_shell.dart:265-276`), que es navegación solo-icono sin hover.
- `GriColors.gray = #777777` sobre blanco ≈ 4.48:1, justo por debajo del 4.5:1 de WCAG AA para texto
  normal a 12-13px. ⚠️ El token NO se modifica (decisión bloqueada: la paleta se conserva): la
  corrección se aplica en el rol tipográfico de texto secundario. Ver Tarea 2.

⚠️ `textContrastGuideline` es el gate más ruidoso. Si al aplicarlo aparecen decenas de fallos, se
aplica SOLO a las pantallas del camino crítico (login, registro, menú, carrito) y el resto se anota
en el SUMMARY. No bloquear la fase por él.

✅ **Los emojis YA son `Icon`s cuando este plan corre.** El usuario desbloqueó la sustitución
(11-CONTEXT.md, "Emojis como iconos") y la ejecutan los planes **11-13** (app cliente) y **11-21**
(panel), de los que este depende. Eso
simplifica el trabajo de aquí: un `Icon` acepta `semanticLabel` de forma nativa y un `IconButton`
acepta `tooltip`, así que ya no hace falta el envoltorio
`Semantics(...) + ExcludeSemantics(<glifo>)` que se había previsto para los emojis.
Si al llegar aquí quedara algún emoji suelto en un control interactivo, **no** se envuelve: se reporta
como hueco de 11-13 / 11-21 y se sustituye por su `Icon`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Etiquetas, tooltips y tamaños táctiles en app_cliente</name>
  <files>app_cliente/lib/features/restaurantes/home_screen.dart, app_cliente/lib/features/pedidos/menu_mesa_screen.dart, app_cliente/lib/features/pagos/calificacion_sheet.dart</files>
  <behavior>
    - El botón de escanear QR de la home mide al menos 48×48 y expone la etiqueta "Escanear QR de la mesa".
    - Los botones +/− del carrito tienen `tooltip` ("Quitar una unidad" / "Agregar una unidad") y miden al menos 48×48.
    - Las 5 estrellas de calificación exponen etiqueta individual ("Calificar con N estrellas").
    - Todo control interactivo con icono expone etiqueta mediante `semanticLabel` del `Icon` o `tooltip`
      del `IconButton` (los emojis ya no existen: los sustituyeron los planes 11-13 y 11-21).
    - Ninguna de estas pantallas cambia de aspecto salvo el crecimiento del botón QR de 45 a 48.
  </behavior>
  <action>
Ampliar `_QrButton` (`home_screen.dart:295-317`) de 45×45 a 48×48 usando `GriSpacing`/constantes donde
aplique, conservando el tint, el radio y el glifo. Es el único cambio dimensional permitido en el plan
y está justificado por el mínimo táctil de Android.

Añadir `tooltip` a los `IconButton` sin etiqueta y `constraints: BoxConstraints(minWidth: 48, minHeight: 48)`
donde el tamaño quede corto. Como los planes 11-13 y 11-21 ya convirtieron los emojis en `Icon`s, la etiqueta se pone
con `semanticLabel` en el propio `Icon` o con `tooltip` en el `IconButton` — sin envoltorios
`Semantics`/`ExcludeSemantics`, que solo hacían falta mientras el contenido era un carácter de texto.

No tocar tipografía, colores ni layout más allá de lo anterior.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
  </verify>
  <done>Los 6 controles señalados por la auditoría tienen etiqueta y tamaño mínimo, sin más cambios visuales que el botón QR.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: Etiquetas del panel y contraste del texto secundario</name>
  <files>panel_admin/lib/features/shared/app_shell.dart, panel_admin/lib/features/mesas/mesa_actions_sheet.dart, panel_admin/lib/features/mesas/mesas_screen.dart, panel_admin/lib/features/menu/menu_screen.dart, panel_admin/lib/features/dashboard/dashboard_screen.dart, panel_admin/lib/features/dashboard/widgets/mesa_tile.dart, panel_admin/lib/features/equipo/equipo_screen.dart, panel_admin/lib/core/theme.dart, app_cliente/lib/core/theme.dart</files>
  <behavior>
    - Cada ítem del sidebar colapsado (70px) muestra un `Tooltip` con su nombre al pasar el ratón y expone etiqueta semántica.
    - Los `ListTile` de acciones de mesa exponen etiqueta descriptiva de la acción.
    - El color aplicado al texto secundario pequeño (`bodySmall` / `labelSmall` del `textTheme`) cumple >= 4.5:1 sobre blanco.
    - `GriColors.gray` conserva EXACTAMENTE su valor `#777777` en las dos apps (test que lo afirma).
    - El texto principal, la marca y los colores de estado siguen idénticos.
    - El `InkWell` del mapa de mesas (`dashboard/widgets/mesa_tile.dart:28-30`) expone etiqueta semántica con el número y el estado de la mesa.
    - Los controles de `mesas_screen` y de `equipo_screen` que hoy no tienen etiqueta la tienen.
  </behavior>
  <action>
Añadir `Tooltip(message: label, child: ...)` a cada `_MenuItem` cuando `collapsed == true`
(`app_shell.dart:265-276`) y `Semantics(label: ...)` a los `ListTile` con emoji de
`mesa_actions_sheet.dart:122-132`.

Etiquetar además los controles tactiles que hoy no anuncian nada y que las guias del test de la
Tarea 3 van a exigir en dashboard, mesas y equipo:
- el `InkWell` de `dashboard/widgets/mesa_tile.dart:28-30`, que es el control principal del mapa de
  mesas y no expone nada: envolverlo en `Semantics(button: true, label: 'Mesa {numero}, {estado}')`;
- los iconos de las stat-cards del dashboard (ya convertidos por el plan 11-21): darles `semanticLabel`;
- los controles sin etiqueta de `mesas_screen.dart` y de `equipo_screen.dart` (esta ultima creada en
  el plan 11-10, por eso figura en `files_modified`).
Anadir etiqueta y tooltip. Los emojis ya son `Icon`s (planes 11-13 y 11-21): usar su `semanticLabel`.

Contraste — **sin tocar el token de marca**. `GriColors.gray` (`#777777`) es parte de la paleta del
mockup, y la decisión bloqueada del usuario dice que la paleta no se cambia. La corrección se aplica
en el ROL tipográfico, no en el token:
- Añadir a `GriColors` un color NUEVO, `textoSecundarioAccesible` (`#6E6E6E`, ≈5.0:1 sobre blanco),
  documentado como "variante accesible de `gray` para texto pequeño; `gray` se conserva intacto para
  bordes, iconos y decoración, donde el umbral de WCAG para texto no aplica".
- Apuntar a él **solo** los slots del `textTheme` que se usan para texto secundario a 12-13px
  (`bodySmall` / `labelSmall`, definidos en el plan 11-11), en las dos apps.
- NO cambiar el valor de `GriColors.gray` ni tocar sus usos decorativos.
Así el contraste cumple donde WCAG lo exige, la paleta de marca queda intacta y la decisión bloqueada
se respeta sin excepciones. Calcular el ratio con la fórmula WCAG (luminancia relativa) en el propio
test, para no depender de la estimación de la auditoría.

Si aparece texto secundario que NO pasa por el `textTheme` (un estilo inline suelto), migrar ESE punto
al slot correspondiente — nunca cambiar el token. Ojo al orden nuevo: las migraciones masivas a tokens
(planes 11-12 y 11-19) corren **después** de este plan, así que aquí todavía quedan estilos inline por
todas partes; migrar solo los puntos concretos de texto secundario que haga falta tocar, sin
adelantarse al trabajo de aquellos.

  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
  </verify>
  <done>La navegación solo-icono y el mapa de mesas del panel son usables con lector de pantalla, y el texto secundario cumple AA en las dos apps sin que ningún token de la paleta de marca cambie de valor.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Suites a11y con meetsGuideline en el camino crítico</name>
  <files>app_cliente/test/a11y/a11y_test.dart, panel_admin/test/a11y/a11y_test.dart</files>
  <behavior>
    - app_cliente: login, registro, home, menú de mesa y carrito cumplen `androidTapTargetGuideline` y `labeledTapTargetGuideline`.
    - panel_admin: login, dashboard, mesas y equipo cumplen `androidTapTargetGuideline` y
      `labeledTapTargetGuideline` (posible gracias a las etiquetas que añade la Tarea 2 en
      `mesa_tile.dart`, `mesas_screen.dart` y `equipo_screen.dart`).
    - Se incluye un test de contraste (`textContrastGuideline`) sobre login y registro de ambas apps.
    - Un test unitario calcula el ratio de contraste del color de texto secundario del `textTheme`
      sobre blanco y exige >= 4.5; otro afirma que `GriColors.gray` sigue valiendo exactamente `#777777`.
    - Si `textContrastGuideline` falla en pantallas fuera del camino crítico, esas pantallas NO se
      incluyen en la suite y quedan listadas en el SUMMARY como deuda conocida.
  </behavior>
  <action>
Crear `test/a11y/a11y_test.dart` en ambas apps siguiendo el patrón de tres líneas del bloque
`<interfaces>`, reutilizando los helpers de fake/mocks ya existentes para montar cada pantalla.
Recordar `tester.ensureSemantics()` antes y `handle.dispose()` después, o las guías no ven nada.

Implementar el cálculo de ratio WCAG (luminancia relativa) como función auxiliar del test para
verificar el token de contraste con un número, no con una estimación.

Empezar por `androidTapTargetGuideline` y `labeledTapTargetGuideline` en todo el camino crítico; añadir
`textContrastGuideline` pantalla a pantalla y quedarse solo con las que pasan tras las Tareas 1 y 2.
Documentar en el SUMMARY qué pantallas quedaron fuera y por qué — es información que necesita la
verificación de la fase.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter test test/a11y/</automated>
    <automated>cd panel_admin &amp;&amp; flutter test test/a11y/</automated>
    <automated>cd app_cliente &amp;&amp; flutter test &amp;&amp; flutter analyze</automated>
    <automated>cd panel_admin &amp;&amp; flutter test &amp;&amp; flutter analyze</automated>
  </verify>
  <done>Las dos apps tienen gates de accesibilidad automatizados sobre su camino crítico y las suites completas siguen verdes con analyze en 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna | Plan de presentación y accesibilidad; no toca autorización, datos ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-14-01 | Tampering | identidad visual (decisión bloqueada) al corregir el contraste | mitigate | El token de marca `GriColors.gray` NO se toca: se añade un color nuevo aplicado solo a los slots de texto secundario del `textTheme`. Dos tests lo blindan: uno calcula el ratio del rol tipográfico y otro afirma que `gray` sigue valiendo `#777777` |
| T-11-14-02 | Information Disclosure | etiquetas de accesibilidad | mitigate | Las etiquetas describen la acción ("Agregar una unidad"), nunca datos del usuario ni identificadores internos |
| T-11-14-03 | Denial of Service | `textContrastGuideline` bloqueando la fase | mitigate | Se acota al camino crítico; el resto se documenta como deuda en vez de detener la entrega |
</threat_model>

<verification>
- `cd app_cliente && flutter test && flutter analyze` → verde, 0 issues.
- `cd panel_admin && flutter test && flutter analyze` → verde, 0 issues.
- `grep -rn "Semantics(\|tooltip:" app_cliente/lib panel_admin/lib | wc -l` → claramente por encima de los 1 + 1 de partida.
</verification>

<success_criteria>
- Los tres hallazgos de accesibilidad con evidencia file:line (target de 45dp, iconos sin etiqueta, contraste 4.48:1) están cerrados sin tocar la paleta de marca.
- Cada arreglo tiene un gate automatizado que impide la regresión.
- La identidad visual sigue intacta: ningún token de marca cambia de valor.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-14-SUMMARY.md` al terminar.
</output>
