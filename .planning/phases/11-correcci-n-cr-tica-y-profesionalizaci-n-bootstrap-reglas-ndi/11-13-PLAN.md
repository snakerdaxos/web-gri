---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 13
type: execute
wave: 7
depends_on: ["11-12", "11-19"]
files_modified:
  - app_cliente/lib/features/shared/app_shell.dart
  - app_cliente/test/shared/app_shell_responsive_test.dart
  - app_cliente/lib/features/reservas/reserva_wizard_screen.dart
  - app_cliente/test/reservas/wizard_form_test.dart
  - panel_admin/lib/features/shared/responsive_page.dart
  - panel_admin/lib/features/shared/app_shell.dart
  - panel_admin/lib/features/dashboard/widgets/stat_card.dart
  - panel_admin/test/bootstrap/bootstrap_router_test.dart
  - panel_admin/lib/features/menu/menu_screen.dart
  - panel_admin/lib/features/reservas/reservas_screen.dart
  - panel_admin/lib/features/cocina/cocina_screen.dart
  - panel_admin/lib/features/clientes/clientes_screen.dart
  - panel_admin/lib/features/reportes/reportes_screen.dart
  - panel_admin/lib/features/configuracion/configuracion_screen.dart
  - panel_admin/lib/features/equipo/equipo_screen.dart
  - panel_admin/lib/features/dashboard/dashboard_screen.dart
  - panel_admin/lib/features/mesas/mesas_screen.dart
  - panel_admin/test/shared/responsive_test.dart
  - panel_admin/test/reservas/reservas_screen_test.dart
autonomous: true
requirements: [DS-02]

must_haves:
  truths:
    - "El contenido de la app cliente no queda debajo de la barra de estado ni del notch"
    - "La app cliente se adapta al ancho disponible en vez de quedar encajada en una columna fija de 480"
    - "TODAS las pantallas del panel se adaptan al viewport, no solo 3 — incluida la de equipo creada en esta misma fase"
    - "El contenido del panel no se estira de borde a borde en monitores anchos"
    - "No hay RenderFlex overflow en ninguno de los cuatro puntos conocidos: wizard de reservas, lista de reservas, StatCard a 4 columnas y sidebar del panel"
    - "Ningún test del repo sigue filtrando excepciones de overflow para poder pasar"
  artifacts:
    - path: "panel_admin/lib/features/shared/responsive_page.dart"
      provides: "Envoltorio de página con ancho máximo y breakpoints compartidos"
      min_lines: 30
    - path: "app_cliente/test/shared/app_shell_responsive_test.dart"
      provides: "Red de seguridad del único cambio estructural del shell"
      min_lines: 30
  key_links:
    - from: "panel_admin/lib/features/**"
      to: "GriBreakpoints"
      via: "LayoutBuilder con los breakpoints nombrados"
      pattern: "GriBreakpoints"
---

<objective>
Hacer responsive de verdad las dos apps: liberar la app cliente de la jaula de 480px sin romperla,
adaptar las 6 pantallas del panel que hoy no adaptan nada, y eliminar los dos overflows conocidos.

Purpose: "no hay responsive, se ve todo muy desordenado" es una de las quejas literales del usuario y
las dos auditorías la confirman: `app_cliente` tiene CERO `LayoutBuilder` en 68 archivos, y el panel
solo 3 de 9 pantallas adaptan. En monitores anchos las tarjetas se estiran de borde a borde, que es
justo lo que se percibe como "desordenado".
Output: shell adaptativo del cliente con test de 3 anchos, `ResponsivePage` aplicado a las 6 pantallas
del panel, grids fluidos y los overflows corregidos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/audit/UI-REVIEW-app_cliente.md
@.planning/codebase/audit/UI-REVIEW-panel_admin.md
@app_cliente/lib/features/shared/app_shell.dart
@app_cliente/lib/core/design_tokens.dart
@panel_admin/lib/core/design_tokens.dart
@panel_admin/lib/features/dashboard/dashboard_screen.dart
@panel_admin/lib/features/reservas/reservas_screen.dart
@app_cliente/lib/features/reservas/reserva_wizard_screen.dart
</context>

<interfaces>
<!-- Riesgo principal de este plan: producir deriva visual y violar la decisión bloqueada.
     Mitigación: de 0 a 840 px el comportamiento del cliente es IDÉNTICO al de hoy. -->

**BUG del espacio superior en la app cliente (reportado por el usuario).** Síntoma literal: *"no deja
espacio en la parte superior haciendo que quede todo muy pegado, se oculta el logo y demás"*.
Causa verificada: `app_cliente/lib/features/shared/app_shell.dart` monta el contenido como
`Scaffold(body: Center(ConstrainedBox(maxWidth: 480, child: navigationShell)))` **sin `SafeArea`**, así
que el contenido empieza en y=0, debajo de la barra de estado y del notch. Se arregla en el MISMO
archivo y en la misma tarea que el `maxWidth`, porque es exactamente el mismo widget.
Datos verificados del árbol: solo 3 de 9 pantallas usan `SafeArea` (`menu_mesa_screen`,
`pedido_estado_screen`, `reserva_wizard_screen`) y 6 de las 9 tienen `AppBar` propio. Es decir: hay que
evitar el **doble padding** en las pantallas que ya se protegen por su cuenta.

**Los cuatro desbordamientos conocidos** (los dos últimos aparecieron DURANTE la ejecución de la fase
y están documentados en `deferred-items.md`):

| # | Dónde | Cuánto | Cuándo |
|---|---|---|---|
| 1 | `app_cliente/.../reserva_wizard_screen.dart:340-361` (`_ResumenRow`) | variable | con nombres largos |
| 2 | `panel_admin/.../reservas_screen.dart:181-227` (`_ReservaCard`) | variable | ~650px de contenido |
| 3 | `panel_admin/.../widgets/stat_card.dart:50` | **31px abajo** | viewport >= 1100px (grid a 4 columnas, `childAspectRatio: 2.6` deja 59.8px para ~91px de contenido) |
| 4 | `panel_admin/.../shared/app_shell.dart:171` (logo del sidebar) | **85px a la derecha** | **a CUALQUIER ancho** — el sidebar es `Container(width: 220)` con 15px de padding = 190px útiles, y el `Row` del logo pide ~248px. Un segundo `Row` en `:264` (item de menú) desborda 33px por lo mismo |

El #4 es visible en producción HOY y no depende del viewport. Lo detectó `bootstrap_router_test.dart`
(plan 11-07), el primer test del repo que renderiza el `AppShell` completo — los tests del dashboard
pumpean la pantalla suelta, así que nunca se había ejercitado.

**Deuda de test que este plan DEBE saldar** (si no, esos filtros seguirán ocultando regresiones):
- `panel_admin/test/bootstrap/bootstrap_router_test.dart:53` filtra las excepciones
  `A RenderFlex overflowed` con el motivo documentado en el propio archivo. **Retirar el filtro** al
  arreglar el sidebar; el test debe seguir pasando sin él.
- Los tests de dashboard del plan 11-02 evitan el problema pumpeando a **800px** (camino de 1 columna)
  en lugar de a 1200px, con un comentario que lo explica. **Añadir un caso a 1200px**, que es el que
  reproduce el desbordamiento de `StatCard`.

Patrón para el shell del cliente (sustituye al `ConstrainedBox(maxWidth: 480)` fijo de
`app_shell.dart:31-35`). El error a evitar es BORRAR el `ConstrainedBox`: eso estira la app a todo el
ancho del navegador y empeora el desorden. Se convierte la jaula en un techo con adaptación:
```dart
body: LayoutBuilder(
  builder: (context, c) {
    // < 480: móvil real → sin restricción (hoy también lo hace)
    // 480–840: web/tablet estrecho → columna centrada de 480 (comportamiento ACTUAL)
    // >= 840: tablet ancho / desktop → columna más generosa (720), no 480
    final maxW = c.maxWidth < GriBreakpoints.contenidoMax
        ? double.infinity
        : (c.maxWidth < 840 ? GriBreakpoints.contenidoMax : 720);
    return Center(child: ConstrainedBox(
      constraints: BoxConstraints(maxWidth: maxW), child: navigationShell));
  },
),
```
VERIFICADO: ninguno de los 175 tests actuales pumpea `AppShell` (los tests montan pantallas sueltas
dentro de `MaterialApp(home: ...)`), así que este cambio no puede romperlos — y por eso mismo NO
tiene ninguna red de seguridad hoy. El test de 3 anchos es obligatorio.

Breakpoints del panel: usar los ya nombrados en `GriBreakpoints` (750 y 1100, valores hoy vigentes
en `app_shell.dart:72` y `dashboard_screen.dart:37`). NO migrar a los 600/840/1200 de Material 3:
desplazaría el punto de colapso del sidebar y del grid, que es un cambio visual prohibido.

Overflows confirmados:
1. `app_cliente/lib/features/reservas/reserva_wizard_screen.dart:340-361` — `_ResumenRow` con `Text`
   de valor sin `Expanded`/ellipsis: overflow con cualquier nombre de restaurante largo.
2. `panel_admin/lib/features/reservas/reservas_screen.dart:181-227` — `_ReservaCard`: un único `Row`
   con hora (fontSize 20 bold) + `Expanded` + chip + DOS botones sin restringir. A ~650px de ancho de
   contenido (tablet de 768 menos sidebar y padding) desborda.

Ancho máximo de contenido en el panel: hoy `maxWidth` solo aparece en `login_screen.dart:83` y en un
diálogo. En un monitor de 1920 quedan ~1650px de contenido y las tarjetas se estiran enteras.
`ConstrainedBox(maxWidth: 1200)` centrado es el remedio estándar.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarea 1: Zona segura superior y shell adaptativo de app_cliente, más el overflow del wizard</name>
  <files>app_cliente/lib/features/shared/app_shell.dart, app_cliente/test/shared/app_shell_responsive_test.dart, app_cliente/lib/features/reservas/reserva_wizard_screen.dart, app_cliente/test/reservas/wizard_form_test.dart</files>
  <behavior>
    - Con un `MediaQuery` que declara `padding.top` (barra de estado / notch, p.ej. 44px), el contenido
      del shell NO se dibuja bajo esa franja: su origen vertical es >= el `padding.top` declarado.
    - Con `padding.top` a 0 el layout es idéntico al actual (ningún hueco añadido de más).
    - Las pantallas que YA usan `SafeArea` (`menu_mesa_screen`, `pedido_estado_screen`,
      `reserva_wizard_screen`) NO acumulan doble margen superior tras el cambio.
    - La `BottomNavigationBar` sigue respetando el área segura inferior (gesto de navegación).
    - A 360px de ancho: el contenido ocupa todo el ancho disponible (sin columna centrada).
    - A 600px: el contenido queda limitado a 480px y centrado (comportamiento idéntico al actual).
    - A 1200px: el contenido queda limitado a 720px y centrado (ya no a 480).
    - La `BottomNavigationBar` sigue funcionando y conserva el índice activo en los tres anchos.
    - `_ResumenRow` con un nombre de restaurante de 60 caracteres a 320px de ancho no produce overflow
      (`tester.takeException()` es null y no hay `FlutterError` de RenderFlex).
  </behavior>
  <action>
Aplicar en `app_shell.dart` el patrón del bloque `<interfaces>` y, **en el mismo widget**, arreglar la
zona segura superior: envolver el cuerpo en `SafeArea` (con `bottom: false`, porque la
`BottomNavigationBar` del `Scaffold` ya gestiona el área segura inferior por su cuenta y duplicarlo
añadiría un hueco).
Orden: `Scaffold(body: SafeArea(bottom: false, child: LayoutBuilder(... Center(ConstrainedBox(...)))))`.
Evitar el doble padding: `SafeArea` es idempotente por consumo de `MediaQuery` —una `SafeArea` anidada
dentro de otra no vuelve a aplicar el mismo inset—, así que las tres pantallas que ya la usan no se
tocan. Verificarlo con el caso de test correspondiente en lugar de asumirlo. Las 6 pantallas con
`AppBar` tampoco necesitan cambio: el `AppBar` ya consume el inset superior.
No tocar la `BottomNavigationBar`, ni el `goBranch`, ni la lista de tabs.

Crear `app_cliente/test/shared/app_shell_responsive_test.dart`: pumpear el `AppShell` dentro de un
`MediaQuery`/`SizedBox` de anchos 360, 600 y 1200 y afirmar el `maxWidth` efectivo del `ConstrainedBox`
resultante (`tester.widget<ConstrainedBox>(...).constraints.maxWidth`).
**Añadir el test de zona segura**, que es el que impide que esto vuelva en la primera refactorización:
pumpear con `MediaQuery(data: MediaQueryData(padding: EdgeInsets.only(top: 44)), ...)` y afirmar que el
`globalToLocal`/`localToGlobal` del primer descendiente del shell arranca en `dy >= 44`
(`tester.getTopLeft(find.byType(...))`), y un caso con `padding.top: 0` que afirme que no se añade
hueco. Añadir también el caso de no-duplicación pumpeando una pantalla que ya trae `SafeArea`. Es la ÚNICA red de seguridad
de este cambio: sin ella, nadie detectaría una regresión del layout del cliente.

Corregir `_ResumenRow` (`reserva_wizard_screen.dart:340-361`) envolviendo el `Text` del valor en
`Expanded` con `overflow: TextOverflow.ellipsis` y `textAlign` a la derecha, sin cambiar tipografía ni
espaciado. Añadir a `wizard_form_test.dart` el caso de texto largo a 320px.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test test/shared/app_shell_responsive_test.dart test/reservas/</automated>
  </verify>
  <done>El contenido de la app cliente ya no queda bajo la barra de estado (con test que lo demuestra y sin doble margen donde ya había `SafeArea`), el shell adapta a tres anchos, el comportamiento de 0 a 840px es idéntico al anterior, y el overflow del wizard está cerrado con regresión.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 2: ResponsivePage y las 6 pantallas del panel sin adaptación</name>
  <files>panel_admin/lib/features/shared/responsive_page.dart, panel_admin/lib/features/menu/menu_screen.dart, panel_admin/lib/features/cocina/cocina_screen.dart, panel_admin/lib/features/clientes/clientes_screen.dart, panel_admin/lib/features/reportes/reportes_screen.dart, panel_admin/lib/features/configuracion/configuracion_screen.dart, panel_admin/test/shared/responsive_test.dart</files>
  <behavior>
    - `ResponsivePage` limita su hijo a 1200px de ancho y lo centra cuando hay más espacio.
    - A 1920px de ancho de contenido, cada pantalla de la lista real (incluida `equipo_screen`) renderiza su contenido dentro de un ancho <= 1200.
    - A 900px, el contenido ocupa el ancho disponible sin recortes.
    - `ResponsivePage` expone el `LayoutBuilder` (o el ancho medido) para que la pantalla decida su
      densidad, y sus consumidores no vuelven a llamar `MediaQuery.of(context).size`.
    - Ninguna pantalla cambia su padding vertical ni su tipografía respecto al estado anterior.
  </behavior>
  <action>
Crear `panel_admin/lib/features/shared/responsive_page.dart` con
`ResponsivePage({required Widget Function(BuildContext, double ancho) builder, double maxWidth = 1200, EdgeInsets? padding})`
que envuelva en `LayoutBuilder` + `Center` + `ConstrainedBox`, usando `GriBreakpoints` para exponer
banderas útiles (`esCompacto`, `esExpandido`). Documentar por qué `LayoutBuilder` y no `MediaQuery`:
`MediaQuery` da el tamaño de la VENTANA, y dentro de un shell con sidebar de 250px eso es el número
equivocado.

Aplicarlo a TODAS las pantallas que no adaptan. Al iniciar la tarea, obtener la lista real con
`ls panel_admin/lib/features/**/*_screen.dart` en lugar de fiarse de una cuenta fija: además de las 6
que detectó la auditoría (`menu_screen`, `reservas_screen` —ver Tarea 3—, `cocina_screen`,
`clientes_screen`, `reportes_screen`, `configuracion_screen`), esta misma fase añadió
`equipo_screen.dart` (plan 11-10) y `bootstrap_screen.dart` (plan 11-07, que ya nace con
`ConstrainedBox(maxWidth: 400)` propio y por tanto puede quedar exenta si se documenta).
Envolver el cuerpo de cada una.
Conservar el padding exacto que cada una usa hoy (30 vs 24 vs `fromLTRB(24,16,24,4)`): homogeneizarlos
sería un cambio visual y está fuera de alcance.

En `dashboard_screen.dart` y `mesas_screen.dart`, sustituir los `GridView.count` de tope 4 columnas
por `GridView.builder` con `SliverGridDelegateWithMaxCrossAxisExtent(maxCrossAxisExtent: 260)` para
que en pantallas ultraanchas aparezca una quinta columna en vez de tarjetas gigantes. Verificar que a
los anchos habituales (1280, 1440) el número de columnas resultante coincide con el actual; si no
coincide, ajustar `maxCrossAxisExtent` hasta que coincida — el objetivo es "igual en los anchos de
hoy, mejor en los anchos grandes".

Crear `panel_admin/test/shared/responsive_test.dart` con los casos de `<behavior>`, pumpeando cada
pantalla a 900 y 1920 px con los providers ya fakeados del repo.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test test/shared/responsive_test.dart</automated>
    <automated>cd panel_admin &amp;&amp; TOT=$(ls lib/features/*/*_screen.dart | wc -l); ADAPT=$(grep -rln "LayoutBuilder\|ResponsivePage" lib/features --include=*_screen.dart | wc -l); echo "adaptan $ADAPT de $TOT"; test "$ADAPT" -eq "$TOT"</automated>
  </verify>
  <done>El gate compara contra el número REAL de pantallas del árbol (no contra una constante que se queda corta cuando la fase añade pantallas nuevas) y sale en verde: todas adaptan. El contenido tiene techo de 1200px en monitores anchos y los grids son fluidos sin cambiar el número de columnas a los anchos habituales.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Los tres desbordamientos del panel y retirada de los filtros de test</name>
  <files>panel_admin/lib/features/reservas/reservas_screen.dart, panel_admin/lib/features/dashboard/widgets/stat_card.dart, panel_admin/lib/features/shared/app_shell.dart, panel_admin/test/reservas/reservas_screen_test.dart, panel_admin/test/dashboard/stats_render_test.dart, panel_admin/test/bootstrap/bootstrap_router_test.dart</files>
  <behavior>
    - `_ReservaCard` con nombre de mesa largo, chip de estado y los dos botones, a 650px de ancho de
      contenido: sin excepción de RenderFlex.
    - A 650px, los botones de acción pasan a una segunda fila (o el cluster se envuelve) en lugar de desbordar.
    - A 1200px, la tarjeta conserva exactamente la disposición actual de una sola fila.
    - Los tests existentes de `reservas_screen` siguen pasando sin modificar sus aserciones.
    - El dashboard pumpeado a **1200px** de ancho (grid a 4 columnas) no produce ninguna excepción de
      RenderFlex: `StatCard` cabe en el alto que le da `childAspectRatio`.
    - `AppShell` del panel renderizado con sesión, a 800px y a 1920px, no produce ninguna excepción de
      RenderFlex: ni el logo del sidebar ni los items del menú desbordan.
    - `bootstrap_router_test.dart` pasa **sin** el filtro de excepciones de overflow.
    - `grep -rn "RenderFlex overflowed" panel_admin/test app_cliente/test` no devuelve ningún filtro activo.
  </behavior>
  <action>
En `_ReservaCard` (`reservas_screen.dart:181-227`) envolver el grupo chip + botones en un
`Wrap(spacing: GriSpacing.sm, runSpacing: GriSpacing.sm)` o, si eso alterase el aspecto en anchos
grandes, usar el `LayoutBuilder` de `ResponsivePage` para bajar los botones a una segunda fila por
debajo de 900px. Elegir la opción que conserve EXACTAMENTE el aspecto actual a anchos de escritorio;
comprobarlo con el test de 1200px.

Envolver además el cuerpo de la pantalla en `ResponsivePage` (queda pendiente de la Tarea 2).

**Desbordamiento #3 — `StatCard` (31px, viewport >= 1100px).** Con `maxWidth >= 1100` el grid pasa a 4
columnas (`dashboard_screen.dart:37-39`) y `childAspectRatio: 2.6` deja 59.8px de alto para ~91px de
contenido en el `Column` de `stat_card.dart:50`. Arreglarlo por el lado del contenido o del ratio, lo
que menos altere el aspecto a los anchos habituales: bajar el `childAspectRatio` para el tramo de 4
columnas, o hacer que el `Column` de la card se ajuste (`mainAxisSize`, `Flexible`, tipografía del
`textTheme` en vez de tamaños fijos). PROHIBIDO cambiar colores o el orden de los elementos.

**Desbordamiento #4 — sidebar del panel (85px + 33px, a cualquier ancho).** El sidebar es un
`Container(width: 220)` con `padding` horizontal de 15 → 190px útiles, y el `Row` del logo
(`app_shell.dart:171`) mete un badge de 45px + 12px de hueco + un `Column` con `GRI` y
`Gestión de Restaurante`, que a 14px pide ~248px sin restricción. Arreglo: envolver el `Column` de
textos en `Expanded` (y el `Text(label)` del item de menú de `:264` igual), con
`overflow: TextOverflow.ellipsis` donde haga falta. Es un defecto real visible en producción hoy, no un
artefacto de test.

**Retirada de los filtros — parte de la definición de hecho, no opcional.**
1. Quitar de `panel_admin/test/bootstrap/bootstrap_router_test.dart:53` el filtro de excepciones
   `A RenderFlex overflowed` (y el comentario que lo justificaba), y comprobar que el test sigue en verde.
2. Añadir a `panel_admin/test/dashboard/stats_render_test.dart` un caso a **1200px** de ancho — hoy los
   tests pumpean a 800px justamente para esquivar el desbordamiento de `StatCard`—, y actualizar el
   comentario que explicaba por qué se evitaba.
Si al retirar el filtro aparece un desbordamiento distinto de los dos arreglados aquí, NO volver a
poner el filtro: arreglarlo o, si queda fuera de alcance, anotarlo en `deferred-items.md` con la misma
disciplina y dejar el test fallando de forma visible en el SUMMARY.

Ampliar `panel_admin/test/reservas/reservas_screen_test.dart` con los casos de `<behavior>`, montando
la pantalla dentro de un `SizedBox` de ancho controlado.
  </action>
  <verify>
    <automated>cd panel_admin &amp;&amp; flutter analyze &amp;&amp; flutter test test/reservas/</automated>
    <automated>cd panel_admin &amp;&amp; flutter test</automated>
    <automated>cd app_cliente &amp;&amp; flutter test</automated>
  </verify>
  <done>Los tres desbordamientos del panel están cerrados con regresión, ningún test del repo filtra ya excepciones de overflow, el aspecto de escritorio no cambia y las dos suites completas siguen verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna | Plan de layout; no toca autorización, datos ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13-01 | Tampering | deriva visual al liberar el `maxWidth: 480` | mitigate | El comportamiento solo se relaja POR ENCIMA de 840px; de 0 a 840 es idéntico. Test de 3 anchos como red de seguridad del único cambio estructural del shell |
| T-11-13-02 | Tampering | cambio del punto de colapso del sidebar / columnas del grid | mitigate | Se reutilizan los breakpoints 750/1100 ya vigentes; el test comprueba que el número de columnas a 1280 y 1440 no cambia |
| T-11-13-03 | Denial of Service | RenderFlex overflow visible al usuario | mitigate | Los CUATRO desbordamientos conocidos quedan cerrados con test de regresión (anchos estrechos, texto largo, 1200px para el grid y el shell completo con sesión) |
| T-11-13-04 | Repudiation | filtros de excepciones que ocultan regresiones futuras | mitigate | Retirar el filtro de `bootstrap_router_test.dart` y añadir el caso a 1200px forma parte de la definición de hecho; un `grep` en la verificación comprueba que no queda ninguno |
| T-11-13-05 | Denial of Service | contenido bajo la barra de estado / notch | mitigate | `SafeArea(bottom: false)` en el shell del cliente, con test de `MediaQuery.padding.top` y caso de no-duplicación en las pantallas que ya la usaban |
</threat_model>

<verification>
- `cd app_cliente && flutter test && flutter analyze` → verde, 0 issues.
- `cd panel_admin && flutter test && flutter analyze` → verde, 0 issues.
- `grep -rn "RenderFlex overflowed" panel_admin/test app_cliente/test` → sin resultados (ningún filtro activo).
- El gate de cobertura responsive compara `ADAPT` contra `TOT` calculados del árbol, no contra un número escrito a mano.
- Comprobación manual (opcional, para el runbook del plan 11-15): `flutter run -d chrome` redimensionando la ventana entre 360 y 1920 sin ver bandas de overflow.
</verification>

<success_criteria>
- La queja "no hay responsive, se ve desordenado" queda cerrada con evidencia: TODAS las pantallas del panel adaptan (contadas del árbol real) y el cliente deja de estar encajado.
- Ningún ancho produce overflow en los cuatro puntos conocidos, y la app cliente respeta la zona segura superior.
- Los filtros de test que tapaban desbordamientos han desaparecido en lugar de quedarse como deuda oculta.
- La identidad visual y el layout a los anchos de uso habitual son indistinguibles de los anteriores.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-13-SUMMARY.md` al terminar.
</output>
