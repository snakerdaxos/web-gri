---
phase: 11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi
plan: 19
type: execute
wave: 6
depends_on: ["11-09", "11-11", "11-17"]
files_modified:
  - app_cliente/lib/core/theme.dart
  - app_cliente/lib/features/**
  - app_cliente/test/core/sin_hex_crudos_test.dart
autonomous: true
requirements: [DS-01]

must_haves:
  truths:
    - "Ningún color de la UI de la app cliente se declara con un hex crudo fuera del archivo de tema"
    - "Los TextStyle inline que encajan en la escala tipográfica usan el textTheme"
    - "La apariencia de la app cliente es idéntica a la anterior a este plan"
  artifacts:
    - path: "app_cliente/test/core/sin_hex_crudos_test.dart"
      provides: "Gate automatizado contra la reaparición de hex crudos en la app cliente"
      min_lines: 20
  key_links:
    - from: "app_cliente/lib/features/**"
      to: "GriColors / griTheme"
      via: "constantes con nombre en lugar de Color(0x...)"
      pattern: "GriColors\\."
---

<objective>
Migrar los literales de color y los `TextStyle` inline de la app cliente a los tokens y a la escala
tipográfica creados en el plan 11-11, y dejar un gate que impida su reaparición.

Purpose: gemelo del plan 11-12 para la otra app. Se separan porque juntos consumen demasiado contexto
para un solo ejecutor, y este es justo el tipo de trabajo mecánico donde la degradación por contexto
produce cambios de color no intencionados.
Output: cero hex crudos fuera del tema en `app_cliente`, `TextStyle` migrados donde encajan, y
`sin_hex_crudos_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/audit/UI-REVIEW-app_cliente.md
@app_cliente/lib/core/theme.dart
@app_cliente/lib/core/design_tokens.dart
@app_cliente/lib/models/pedido.dart
</context>

<interfaces>
<!-- REGLA DURA: migración 1:1, mecánica. -->
Cada hex crudo se sustituye por una constante con nombre **del mismo valor exacto**.
Está PROHIBIDO "aprovechar y ajustar" ningún color, redondeo, sombra o espaciado. Si algo se ve mal,
se anota como hallazgo — no se corrige aquí. El único cambio de color permitido en toda la fase es el
del rol de texto secundario por contraste, y pertenece al plan 11-14.

Inventario auditado de `app_cliente`:
- `Color(0xFFF5A623)` (ámbar de estrellas/calificación): **9 usos** en `home_screen.dart`,
  `restaurantes_list_screen.dart`, `restaurante_detalle_screen.dart`, `pedido_estado_screen.dart`,
  `calificacion_sheet.dart`.
- Gradiente `Color(0xFFFF6B35)` / `Color(0xFFFF9B5A)`: 4 usos cada uno en `home_screen.dart`,
  `restaurantes_list_screen.dart`, `restaurante_detalle_screen.dart`.
- Los colores de estado de pedido de `models/pedido.dart:78-95` YA los movió el plan 11-11 al
  `ThemeExtension`: no volver a tocarlos.

El inventario debe incluir además el código nuevo de esta misma fase, que por eso figura entre las
dependencias: `features/shared/password_field.dart` y las pantallas de auth (plan 11-06),
`features/shared/empty_state.dart` y `not_found_screen.dart` (plan 11-09), y
`features/shared/google_boton.dart` (plan 11-17, que además dejó un `// TODO(11-19)` explícito
apuntando a esta migración — resolverlo aquí).

Tipografía: la escala vive en `griTheme.textTheme` (plan 11-11). Criterio de corte: migrar solo los
`TextStyle` cuyo tamaño Y peso coincidan EXACTAMENTE con un slot. Los que no coincidan se dejan y se
anotan; forzar una migración aproximada cambiaría la apariencia.
</interfaces>

<tasks>

<task type="auto">
  <name>Tarea 1: Migrar los hex crudos de app_cliente a GriColors</name>
  <files>app_cliente/lib/core/theme.dart, app_cliente/lib/features/**</files>
  <action>
Inventariar primero y guardar la lista en el SUMMARY como evidencia del antes:
`grep -rn "Color(0x" app_cliente/lib --include=*.dart | grep -v "core/theme.dart"`.

Promover cada literal a `GriColors` con nombre semántico (qué ES, no qué color es) y el MISMO valor:
`amber` / `calificacionEstrella` para `0xFFF5A623`, `gradienteInicio` para `0xFFFF6B35`,
`gradienteFin` para `0xFFFF9B5A`, más los que aparezcan en el inventario. Agrupar las constantes
nuevas por sección con comentarios, siguiendo la organización que ya tiene el archivo.

Resolver el `// TODO(11-19)` que dejó `features/shared/google_boton.dart` (plan 11-17): sustituir sus
valores literales por `GriSpacing`/`GriRadius`/`GriColors` conservando exactamente el mismo aspecto.

No tocar `models/pedido.dart` (ya migrado en el plan 11-11) ni los helpers de chip de reserva de
`theme.dart`, que ya están centralizados.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
    <automated>cd app_cliente &amp;&amp; test $(grep -rn "Color(0x" lib --include=*.dart | grep -v "core/theme.dart" | grep -v "core/design_tokens.dart" | wc -l) -eq 0 &amp;&amp; echo "SIN_HEX_CRUDOS"</automated>
  </verify>
  <done>Cero `Color(0x...)` fuera de `core/theme.dart` en la app cliente y la suite completa verde sin cambiar ningún valor esperado.</done>
</task>

<task type="auto">
  <name>Tarea 2: Migrar los TextStyle inline y el espaciado que encaja en la escala</name>
  <files>app_cliente/lib/features/**</files>
  <action>
Migrar los `TextStyle` inline a `Theme.of(context).textTheme.*` **solo** cuando tamaño y peso
coincidan exactamente con un slot de la escala del plan 11-11. Registrar en el SUMMARY la lista de los
que NO se migraron y por qué (tamaño fuera de escala), para que quede constancia de que la decisión
fue deliberada y no un olvido.

Sustituir los literales de espaciado por `GriSpacing` **solo** cuando el valor coincida exactamente
con un peldaño (4/8/16/24/32/48). Un `SizedBox(height: 18)` NO se convierte en `md` ni en `lg`: se
deja y se anota. Cambiar un 18 por un 16 es un cambio visual y está prohibido.

Verificar el resultado con los tests existentes SIN editar sus aserciones: si un test de layout
empieza a fallar, la migración cambió algo que no debía.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter analyze &amp;&amp; flutter test</automated>
    <automated>cd app_cliente &amp;&amp; test $(grep -rn "textTheme" lib --include=*.dart | wc -l) -gt 0 &amp;&amp; echo "TEXTTHEME_EN_USO"</automated>
  </verify>
  <done>La app cliente usa la escala tipográfica donde encaja, el espaciado nombrado donde coincide, y ningún test existente necesitó tocarse.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarea 3: Gate anti-regresión de la app cliente</name>
  <files>app_cliente/test/core/sin_hex_crudos_test.dart</files>
  <behavior>
    - El test recorre `lib/**/*.dart` excluyendo `core/theme.dart` y `core/design_tokens.dart`.
    - Si encuentra un `Color(0x...)` fuera de esos archivos, falla e imprime archivo, línea y el literal.
    - Una línea con el comentario `// TOKEN-IGNORE: motivo` en la misma línea o en la anterior queda exenta.
    - Las líneas comentadas se ignoran (la documentación del propio archivo no puede autoinvalidarlo).
    - El test pasa con el estado final de las Tareas 1 y 2.
    - Verificado por construcción: introducir un `Color(0xFF123456)` temporal en una pantalla hace fallar el test.
  </behavior>
  <action>
Crear el gemelo del gate que el plan 11-12 creó en `panel_admin`, con la MISMA lógica y el mismo
mensaje de fallo ("promueve el color a `GriColors` con un nombre semántico y el MISMO valor").
Cabecera con comentario de sincronización nombrando `panel_admin/test/core/sin_hex_crudos_test.dart`.

Comprobar el gate manualmente inyectando un literal temporal, confirmando el fallo y revirtiéndolo;
dejar constancia en el SUMMARY.
  </action>
  <verify>
    <automated>cd app_cliente &amp;&amp; flutter test test/core/sin_hex_crudos_test.dart</automated>
    <automated>cd app_cliente &amp;&amp; flutter test &amp;&amp; flutter analyze</automated>
  </verify>
  <done>La app cliente tiene un gate que falla si vuelve a aparecer un color suelto, con escape documentado para los casos legítimos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ninguna | Refactor de presentación; no toca autorización, datos ni red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-19-01 | Tampering | deriva visual durante la migración | mitigate | Migración 1:1 obligatoria, prohibición explícita de "aprovechar y ajustar", y las pruebas existentes de la app como red de seguridad sin editar valores esperados |
| T-11-19-02 | Tampering | reaparición de literales tras la fase | mitigate | Gate `sin_hex_crudos_test.dart`, con escape explícito y comentado |
</threat_model>

<verification>
- `cd app_cliente && flutter test && flutter analyze` → verde, 0 issues.
- `git diff` revisado a mano: ningún literal de color cambia de valor; solo cambia dónde vive.
- `grep -rn "TODO(11-19)" app_cliente/lib` → sin resultados (el marcador del plan 11-17 quedó resuelto).
</verification>

<success_criteria>
- `app_cliente/lib/core/theme.dart` es la fuente única de verdad visual de la app cliente.
- Todo el código nuevo de la fase (auth, estados vacíos, 404, botón de Google) queda dentro del sistema de diseño.
- La apariencia de la app no ha cambiado.
</success_criteria>

<output>
Crear `.planning/phases/11-correcci-n-cr-tica-y-profesionalizaci-n-bootstrap-reglas-ndi/11-19-SUMMARY.md` al terminar.
</output>
