---
phase: 01-fundaci-n-e-infraestructura
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/app/core/db.py
  - backend/app/api/__init__.py
  - backend/app/api/health.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_db_config.py
  - backend/Dockerfile
  - backend/.dockerignore
  - backend/scripts/verify_infra.sh
  - docker-compose.yml
  - .gitignore
  - .env.example
  - .env
  - .planning/phases/01-fundaci-n-e-infraestructura/SKELETON.md
autonomous: true
requirements: [INFR-01, INFR-02]

must_haves:
  truths:
    - "docker compose up levanta MySQL y la API; GET /health responde 200 con database: connected"
    - "Los datos de MySQL sobreviven al reinicio del contenedor (volumen persistente gri_mysql_data)"
    - "La BD usa charset utf8mb4, collation utf8mb4_unicode_ci y timezone -05:00 (America/Bogota)"
    - "Acentos y emojis se guardan y se leen intactos (round-trip de 'Açaí 🍜 café Bogotá')"
    - "Cambiar DB_NAME/DB_HOST en .env cambia la conexión sin tocar código (mismatch -> /health 503)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Orquestación mysql:8.4-lts + api con healthcheck, depends_on service_healthy y volumen nombrado"
      contains: "gri_mysql_data"
    - path: "backend/Dockerfile"
      provides: "Imagen API uv from GHCR sobre python:3.12-slim, sin build-essential, sin alembic en CMD"
      contains: "ghcr.io/astral-sh/uv"
    - path: "backend/app/core/config.py"
      provides: "Settings parts-based (DB_HOST/PORT/USER/PASSWORD/NAME + DEMO_MODE inerte) con database_url property"
      exports: ["settings", "Settings"]
    - path: "backend/app/core/db.py"
      provides: "Async engine (pool_pre_ping, expire_on_commit=False) + async_sessionmaker + get_session dep"
      exports: ["engine", "async_session_maker", "get_session"]
    - path: "backend/app/api/health.py"
      provides: "GET /health que ejecuta SELECT 1 -> 200 {database: connected} o 503"
      exports: ["router"]
    - path: "backend/app/main.py"
      provides: "FastAPI app con lifespan (no on_event) que dispone el engine al shutdown"
      exports: ["app"]
    - path: "backend/tests/conftest.py"
      provides: "Fixture httpx AsyncClient apuntando a http://localhost:8000"
    - path: "backend/tests/test_health.py"
      provides: "Smoke test INFR-02: GET /health -> 200 + database connected"
    - path: "backend/tests/test_db_config.py"
      provides: "Integration test INFR-01: charset utf8mb4, tz -05:00, round-trip emoji"
    - path: "backend/scripts/verify_infra.sh"
      provides: "Script de aceptación manual: charset/TZ, persistencia tras restart, env-swap"
    - path: ".env.example"
      provides: "Template committed con placeholders MYSQL_ROOT_PASSWORD/MYSQL_APP_PASSWORD/DEMO_MODE"
    - path: ".planning/phases/01-fundaci-n-e-infraestructura/SKELETON.md"
      provides: "Walking Skeleton record — decisiones arquitectónicas para fases posteriores"
  key_links:
    - from: "docker-compose.yml (servicio api)"
      to: "docker-compose.yml (servicio mysql)"
      via: "depends_on.mysql.condition: service_healthy (gatea arranque hasta mysqladmin ping OK)"
      pattern: "condition: service_healthy"
    - from: "docker-compose.yml api.environment.DB_HOST=mysql"
      to: "backend/app/core/config.py Settings.DB_HOST"
      via: "env var inyectada -> BaseSettings -> database_url property"
      pattern: "DB_HOST"
    - from: "backend/app/core/config.py database_url"
      to: "backend/app/core/db.py create_async_engine"
      via: "URL mysql+asyncmy://...?charset=utf8mb4"
      pattern: "mysql\\+asyncmy://"
    - from: "backend/app/api/health.py health()"
      to: "backend/app/core/db.py get_session"
      via: "Depends(get_session) -> session.execute(text('SELECT 1'))"
      pattern: "SELECT 1"
---

<objective>
Levantar el Walking Skeleton de GRI: el stack Docker (MySQL 8.4 + FastAPI/uvicorn) corre end-to-end y se verifica con `GET /health` que ejecuta un `SELECT 1` real contra la BD. Demuestra las 3 Success Criteria del ROADMAP: arranque limpio, persistencia + charset/TZ correctos, y conexión 100% por variables de entorno.

Purpose: Es la primera fase del proyecto; establece la base arquitectónica (composición Docker, config parts-based, SQLAlchemy 2 async, layout `backend/app/`) sobre la que todas las fases siguientes se construyen. El SKELETON.md registra estas decisiones como contrato.

Output: Una app Python mínima (~6 archivos en `backend/app/`), su Dockerfile uv-based, un `docker-compose.yml` en la raíz, los tests Wave 0 (conftest + test_health + test_db_config), un script de aceptación manual (`verify_infra.sh`), `.env.example`, y `SKELETON.md`.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundaci-n-e-infraestructura/01-RESEARCH.md
@.planning/phases/01-fundaci-n-e-infraestructura/01-VALIDATION.md

# El RESEARCH prescribe los archivos EXACTOS y corrige STACK.md. Seguir sus Patterns 1-5 y Code Examples literalmente.
# Correcciones críticas del RESEARCH a aplicar:
#   - MySQL 8.4: NO pasar --default-authentication-plugin (removido en 8.4; crashea el contenedor)
#   - asyncmy 0.2.14 trae wheels cp312 manylinux -> Dockerfile SIN build-essential/default-libmysqlclient-dev
#   - Alembic se DIFIERE a Phase 3 -> CMD del Dockerfile sin `alembic upgrade head`
#   - DEMO_MODE existe en config pero es inerte (Phase 3 lo lee para sembrar demo)
#   - TZ: env TZ=America/Bogota + command --default-time-zone=-05:00 (offset, NO named zone)
#   - Healthcheck mysql: mysqladmin ping con escape $$ en compose
#   - API: depends_on condition: service_healthy (NO plain depends_on)

<interfaces>
<!-- Contratos que este plan crea y que fases posteriores (2, 3) consumirán -->
<!-- El executor debe implementar exactamente estas firmas -->

backend/app/core/config.py:
```python
class Settings(BaseSettings):
    DB_HOST: str
    DB_PORT: int = 3306
    DB_USER: str
    DB_PASSWORD: str
    DB_NAME: str
    ENVIRONMENT: str = "development"
    DEMO_MODE: bool = False   # inerte esta fase
    @property
    def database_url(self) -> str: ...   # mysql+asyncmy://...?charset=utf8mb4

settings = Settings()
```

backend/app/core/db.py:
```python
engine: AsyncEngine            # create_async_engine(settings.database_url, pool_pre_ping=True, pool_size=10, max_overflow=20, pool_recycle=3600)
async_session_maker: async_sessionmaker[AsyncSession]   # expire_on_commit=False
async def get_session() -> AsyncGenerator[AsyncSession, None]: ...
```

backend/app/api/health.py:
```python
router: APIRouter   # tags=["health"]
# GET /health -> JSONResponse 200 {"status":"ok","database":"connected"} | 503 {"status":"error","database":"unreachable"}
```

backend/app/main.py:
```python
app: FastAPI   # title="GRI API", version="0.1.0", lifespan que hace engine.dispose() al shutdown
# GET / -> {"app":"GRI API","environment": settings.ENVIRONMENT}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App Python skeleton + tests Wave 0 (sin Docker aún)</name>
  <files>
backend/pyproject.toml,
backend/app/__init__.py,
backend/app/main.py,
backend/app/core/__init__.py,
backend/app/core/config.py,
backend/app/core/db.py,
backend/app/api/__init__.py,
backend/app/api/health.py,
backend/tests/__init__.py,
backend/tests/conftest.py,
backend/tests/test_health.py,
backend/tests/test_db_config.py,
.gitignore,
.env.example
  </files>

  <read_first>
- .planning/phases/01-fundaci-n-e-infraestructura/01-RESEARCH.md (secciones: "Recommended Project Structure", Pattern 3 config.py, Pattern 4 db.py, Pattern 5 main.py+health.py, "Installation" pyproject.toml, ".env.example", Pitfalls 6/7/8)
- .planning/phases/01-fundaci-n-e-infraestructura/01-VALIDATION.md (sección "Wave 0 Requirements" — define qué deben contener conftest/test_health/test_db_config)
  </read_first>

  <behavior>
- Settings carga DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME desde env y arma database_url con esquema mysql+asyncmy://...?charset=utf8mb4
- create_async_engine usa pool_pre_ping=True y async_sessionmaker usa expire_on_commit=False (anti-MissingGreenlet)
- main.py define lifespan (no @app.on_event) que await engine.dispose() al shutdown; incluye router de health y un GET / mínimo
- GET /health ejecuta SELECT 1 y devuelve 200 {status:ok, database:connected} o 503 {status:error, database:unreachable}
- test_health.py aserta status 200 y body contiene "database":"connected"
- test_db_config.py aserta @@character_set_database='utf8mb4', @@global.time_zone='-05:00', y round-trip de 'Açaí 🍜 café Bogotá' intacto
- conftest.py provee fixture async_client (httpx.AsyncClient base_url http://localhost:8000)
- .gitignore excluye .env, backend/.venv/, __pycache__/, .pytest_cache/, .ruff_cache/
  </behavior>

  <action>
Trabajar dentro de `backend/`. Inicializar el proyecto con uv: `uv init --python 3.12 --name gri-backend` (si `pyproject.toml` no quedó bien, escribirlo a mano según Pattern del RESEARCH). Luego `uv add "fastapi[standard]>=0.141.0" "uvicorn[standard]>=0.52.0" "sqlalchemy[asyncio]>=2.0.52" "asyncmy>=0.2.14" "pydantic-settings>=2.0"` y `uv add --dev pytest pytest-asyncio httpx ruff`. Esto genera `uv.lock` (commitearlo).

Configurar `pyproject.toml` con: `[project]` name=gri-backend, version=0.1.0, requires-python=">=3.12", dependencies como arriba; `[tool.ruff]` line-length=100, target-version=py312, `[tool.ruff.lint]` select=["E","F","I","B","UP","ASYNC","S","T20"], ignore=["S101"]; `[tool.pytest.ini_options]` asyncio_mode="auto".

Crear `backend/app/__init__.py` vacío y los paquetes `app/core/` y `app/api/` con sus `__init__.py`.

Escribir `app/core/config.py` siguiendo el Pattern 3 del RESEARCH: clase `Settings(BaseSettings)` con `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")`, campos DB_HOST/DB_PORT(default 3306)/DB_USER/DB_PASSWORD/DB_NAME(default "gri")/ENVIRONMENT(default "development")/DEMO_MODE(default False), y `@property database_url` que retorna `f"mysql+asyncmy://{DB_USER}:{DB_PASSWORD}@{DB_HOST}:{DB_PORT}/{DB_NAME}?charset=utf8mb4"`. Instancia `settings = Settings()` a nivel módulo.

Escribir `app/core/db.py` siguiendo el Pattern 4: `engine = create_async_engine(settings.database_url, pool_pre_ping=True, pool_size=10, max_overflow=20, pool_recycle=3600, future=True)`, `async_session_maker = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)` (CRÍTICO: expire_on_commit=False), y `async def get_session() -> AsyncGenerator[AsyncSession, None]:` con `async with async_session_maker() as session: yield session`.

Escribir `app/api/health.py` siguiendo Pattern 5: `router = APIRouter(tags=["health"])`, endpoint `async def health(session: AsyncSession = Depends(get_session)) -> JSONResponse:` que hace `result = await session.execute(text("SELECT 1")); result.scalar_one()` dentro de try/except; en éxito devuelve `JSONResponse(status_code=200, content={"status":"ok","database":"connected"})`, en excepción `JSONResponse(status_code=503, content={"status":"error","database":"unreachable","detail": str(exc)})`. Usar `from sqlalchemy import text`.

Escribir `app/main.py` siguiendo Pattern 5: import `asynccontextmanager`, `AsyncIterator` de `collections.abc`, `FastAPI`, `health` router, `settings`, `engine`. Decorador `@asynccontextmanager async def lifespan(app): yield; await engine.dispose()`. Crear `app = FastAPI(title="GRI API", version="0.1.0", lifespan=lifespan)`, `app.include_router(health.router)`, y un `@app.get("/")` que retorne `{"app":"GRI API","environment": settings.ENVIRONMENT}`.

Wave 0 — `backend/tests/__init__.py` vacío. `conftest.py`: fixture asíncrona `async_client` que yields un `httpx.AsyncClient(base_url="http://localhost:8000")` dentro de `async with` (asume stack corriendo). `test_health.py`: `async def test_health_returns_connected(async_client):` hace `resp = await async_client.get("/health")`, assert `resp.status_code == 200` y `"database" in resp.json()` y `resp.json()["database"] == "connected"`. `test_db_config.py`: `async def test_db_charset_tz_and_unicode(async_client):` — para no duplicar conexión, hacer las queries vía un endpoint o vía la engine importada de `app.core.db`; la forma simple es importar `engine` de `app.core.db`, abrir `async with engine.connect() as conn:` y ejecutar `SELECT @@character_set_database, @@collation_database, @@global.time_zone`, luego `CREATE TABLE IF NOT EXISTS infra_probe(v VARCHAR(50) CHARACTER SET utf8mb4)`, `TRUNCATE`, `INSERT VALUES ('Açaí 🍜 café Bogotá')`, `SELECT v`. Assert charset=='utf8mb4', tz=='-05:00', y el string seleccionado == original.

Crear `.env.example` en la raíz del repo (NO dentro de backend/) con: `MYSQL_ROOT_PASSWORD=replace-me-root`, `MYSQL_APP_PASSWORD=replace-me-app`, `DEMO_MODE=false`, y comentario `# DB_HOST/DB_PORT/DB_USER/DB_NAME se setean en docker-compose.yml para el contenedor (DB_HOST=mysql). Para dev local sin Docker: DB_HOST=127.0.0.1`.

Crear `.gitignore` en la raíz con: `.env`, `backend/.venv/`, `**/__pycache__/`, `**/.pytest_cache/`, `**/.ruff_cache/`, `*.pyc`, `.idea/`, `.vscode/`.

IMPORTANTE — los tests NO se ejecutan en esta tarea (requieren stack Docker corriendo que se levanta en Task 2). El verify de esta tarea es que el código importe y `uv sync` instale todo desde wheels.
  </action>

  <verify>
    <automated>cd backend; uv sync --quiet; if ($?) { uv run python -c "from app.main import app; from app.core.config import settings; from app.core.db import engine, get_session; from app.api.health import router; assert app.title == 'GRI API'; assert settings.database_url.startswith('mysql+asyncmy://'); print('IMPORTS OK')"; if ($?) { uv run python -m pytest --collect-only -q tests/ } }</automated>
  </verify>

  <acceptance_criteria>
- `backend/pyproject.toml` existe y declara requires-python=">=3.12" y las 5 dependencies runtime + 4 dev (pytest, pytest-asyncio, httpx, ruff) SIN alembic, pyjwt, bcrypt, httpx runtime, passlib (deferidos a fases 2-3)
- `backend/uv.lock` existe (generado por uv) y está commiteado
- `backend/app/core/config.py` contiene la cadena literal `mysql+asyncmy://` y el campo `DEMO_MODE: bool = False` y NO contiene `os.getenv` directo
- `backend/app/core/db.py` contiene `expire_on_commit=False` y `pool_pre_ping=True` (anti MissingGreenlet y anti stale conn)
- `backend/app/main.py` contiene `lifespan` y `await engine.dispose()` y NO contiene `@app.on_event` (deprecado)
- `backend/app/api/health.py` contiene `text("SELECT 1")` y devuelve status_code 200 o 503 (no raise)
- `backend/tests/conftest.py` define fixture `async_client` con base_url `http://localhost:8000`
- `backend/tests/test_health.py` aserta status_code == 200 y json()["database"] == "connected"
- `backend/tests/test_db_config.py` aserta charset=='utf8mb4', tz=='-05:00', round-trip de 'Açaí 🍜 café Bogotá'
- `cd backend && uv sync` termina exitosamente SIN compilar nada (asyncmy/greenlet instalan desde wheels; no aparece gcc/build en output)
- `uv run python -c "from app.main import app"` imprime IMPORTS OK sin error
- `uv run python -m pytest --collect-only tests/` colecciona los tests sin error de import
- `.gitignore` incluye `.env` y `backend/.venv/`
- `.env.example` existe en la raíz con MYSQL_ROOT_PASSWORD, MYSQL_APP_PASSWORD, DEMO_MODE
  </acceptance_criteria>

  <done>El código Python de la app existe, importa correctamente, y `uv sync` instala las dependencias desde wheels (sin toolchain C). Los tests Wave 0 están escritos con asserts concretos pero NO se corren todavía (esperan al stack Docker de Task 2). .env.example y .gitignore committed.</done>
</task>

<task type="auto" depends_on_tasks="1">
  <name>Task 2: docker-compose.yml + Dockerfile — levanta el stack end-to-end</name>
  <files>
docker-compose.yml,
backend/Dockerfile,
backend/.dockerignore,
.env
  </files>

  <read_first>
- .planning/phases/01-fundaci-n-e-infraestructura/01-RESEARCH.md (Pattern 1 docker-compose.yml, Pattern 2 Dockerfile, Pitfalls 1-5 — auth-plugin removal, asyncmy wheels, .venv leak, $$ escape, depends_on race, .dockerignore)
- backend/pyproject.toml (generado en Task 1 — el Dockerfile hace bind-mount de pyproject.toml y uv.lock)
- backend/uv.lock (debe existir de Task 1 — el Dockerfile lo bind-mounta)
  </read_first>

  <behavior>
- `docker compose up -d` arranca mysql y api; el api NO arranca hasta que mysql responde `mysqladmin ping` (condition: service_healthy)
- El contenedor mysql:8.4-lts arranca SIN el flag --default-authentication-plugin (removido en 8.4)
- El volumen nombrado gri_mysql_data persiste datos entre `docker compose down` y `up`
- La imagen API no incluye build-essential (asyncmy instala desde wheel cp312 manylinux)
- El CMD del Dockerfile es sólo `uvicorn app.main:app` (NO `alembic upgrade head` — deferido a Phase 3)
- `GET /health` responde 200 {"status":"ok","database":"connected"} tras el arranque
  </behavior>

  <action>
Crear `docker-compose.yml` en la RAÍZ del repo (no en backend/) siguiendo Pattern 1 del RESEARCH literalmente:
- Servicio `mysql`: image `mysql:8.4-lts`, container_name `gri-mysql`, restart `unless-stopped`, environment con `MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}`, `MYSQL_DATABASE: gri`, `MYSQL_USER: gri_app`, `MYSQL_PASSWORD: ${MYSQL_APP_PASSWORD}`, `TZ: America/Bogota`; ports `3306:3306` (comentario: DEV ONLY); volumes `- gri_mysql_data:/var/lib/mysql`; `command:` lista con `--character-set-server=utf8mb4`, `--collation-server=utf8mb4_unicode_ci`, `--default-time-zone=-05:00` (NO incluir `--default-authentication-plugin` — removido en 8.4); `healthcheck:` con `test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u root -p$$MYSQL_ROOT_PASSWORD --silent"]` (notar el `$$`), interval `10s`, timeout `5s`, retries `10`, start_period `30s`.
- Servicio `api`: `build: ./backend`, container_name `gri-api`, restart `unless-stopped`, `depends_on: mysql: condition: service_healthy`, environment con `DB_HOST: mysql`, `DB_PORT: "3306"`, `DB_USER: gri_app`, `DB_PASSWORD: ${MYSQL_APP_PASSWORD}`, `DB_NAME: gri`, `DEMO_MODE: ${DEMO_MODE:-false}`, `ENVIRONMENT: development`; ports `8000:8000`.
- Sección `volumes:` con `gri_mysql_data:` (named volume, sin driver explícito → local default).

Crear `backend/Dockerfile` siguiendo Pattern 2 del RESEARCH:
- `FROM python:3.12-slim`
- `COPY --from=ghcr.io/astral-sh/uv:0.12 /uv /uvx /bin/`
- `ENV UV_COMPILE_BYTECODE=1 UV_LINK_MODE=copy UV_PROJECT_ENVIRONMENT=/app/.venv`
- `WORKDIR /app`
- Layer 1: `RUN --mount=type=cache,target=/root/.cache/uv --mount=type=bind,source=uv.lock,target=uv.lock --mount=type=bind,source=pyproject.toml,target=pyproject.toml uv sync --locked --no-install-project --no-dev`
- Layer 2: `COPY . /app` y `RUN --mount=type=cache,target=/root/.cache/uv uv sync --locked --no-dev`
- Non-root user: `RUN useradd --create-home --uid 1000 appuser && chown -R appuser /app` y `USER appuser`
- `ENV PATH="/app/.venv/bin:$PATH"`
- `EXPOSE 8000`
- `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]` (SIN alembic upgrade head — deferido Phase 3)

Crear `backend/.dockerignore` con: `.venv/`, `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`, `.env`, `tests/`, `*.pyc`, `.git/`. (Crítico: `.venv/` evita que la venv Windows-host se copie al Linux image — Pitfall 3.)

Crear `.env` en la raíz copiando de `.env.example` y reemplazando placeholders con valores dev reales: `MYSQL_ROOT_PASSWORD=dev-root-secret`, `MYSQL_APP_PASSWORD=dev-app-secret`, `DEMO_MODE=false`. (`.env` está en .gitignore; nunca se commitea.)

Levantar el stack: `docker compose up -d --build`. Esperar a que mysql esté healthy (Compose respeta el condition gate). Verificar con `docker compose ps` que ambos servicios estén Up/healthy.

Si `gri-mysql` falla con "unknown variable 'default-authentication-plugin'" → confirmar que NO está en command (Pitfall 1). Si el build copia `.venv/` → confirmar que `.dockerignore` lo excluye (Pitfall 3).
  </action>

  <verify>
    <automated>docker compose up -d --build; Start-Sleep -Seconds 35; docker compose ps; curl -s http://localhost:8000/health; cd backend; uv run pytest tests/ -v</automated>
  </verify>

  <acceptance_criteria>
- `docker-compose.yml` existe en la raíz del repo (NO en backend/) y contiene `mysql:8.4-lts`, `gri_mysql_data`, `--default-time-zone=-05:00`, `condition: service_healthy` y NO contiene la cadena `default-authentication-plugin`
- `docker-compose.yml` usa `$$MYSQL_ROOT_PASSWORD` (doble `$`) en el healthcheck.test (escape Compose)
- `backend/Dockerfile` contiene `ghcr.io/astral-sh/uv:0.12`, `UV_COMPILE_BYTECODE`, `uv sync --locked`, `USER appuser`, y CMD es sólo `uvicorn app.main:app` (NO contiene `alembic`)
- `backend/Dockerfile` NO contiene `build-essential`, `default-libmysqlclient-dev`, ni `pkg-config` (asyncmy usa wheel)
- `backend/.dockerignore` contiene `.venv/`, `__pycache__/`, `.env`
- `docker compose ps` muestra `gri-mysql` y `gri-api` con estado Up y mysql "healthy"
- `docker compose logs gri-api` NO muestra "Can't connect to MySQL" en bucle (el condition gate funcionó)
- `curl -s http://localhost:8000/health` devuelve exactamente `{"status":"ok","database":"connected"}` con status HTTP 200
- `cd backend && uv run pytest tests/ -v` pasa los tests test_health y test_db_config (3 tests verde)
- `docker exec gri-mysql mysql -ugri_app -pdev-app-secret gri -e "SELECT @@character_set_database, @@global.time_zone"` devuelve `utf8mb4` y `-05:00`
  </acceptance_criteria>

  <done>El stack Docker corre end-to-end: `docker compose up -d` levanta MySQL y la API con gate de healthcheck. `GET /health` responde 200 con database connected (SELECT 1 OK). Los tests Wave 0 pasan. Esto prueba el Walking Skeleton: la capability "GET /health verifica conexión a BD" funciona.</done>
</task>

<task type="auto" depends_on_tasks="2">
  <name>Task 3: Script de aceptación manual (persistencia + env-swap) y SKELETON.md</name>
  <files>
backend/scripts/verify_infra.sh,
.planning/phases/01-fundaci-n-e-infraestructura/SKELETON.md
  </files>

  <read_first>
- .planning/phases/01-fundaci-n-e-infraestructura/01-RESEARCH.md (sección "Code Examples — Verify INFR-01 + INFR-02 (the acceptance script)", y ".env.example")
- C:\Users\valle\.claude\get-shit-done\references\skeleton-template.md (estructura exacta del SKELETON.md)
- .planning/ROADMAP.md (Phase 1 Success Criteria — las 3 verdades que el script debe demostrar)
- docker-compose.yml (creado en Task 2 — confirmar nombres de container, user, password)
  </read_first>

  <behavior>
- verify_infra.sh ejecuta 4 chequeos: (1) curl /health, (2) query charset/TZ + round-trip accented+emoji, (3) restart mysql y verificar persistencia de la fila, (4) documentación del env-swap test
- Los 3 Success Criteria del ROADMAP son demostrables ejecutando el script
- SKELETON.md registra: capability probada, decisiones arquitectónicas (Framework, Data layer, Config, Deployment, Layout), stack touched checklist, out of scope (deferido a fases 2-9), subsequent slice plan
  </behavior>

  <action>
Crear `backend/scripts/` (directorio nuevo). Escribir `backend/scripts/verify_infra.sh` (script bash; en Windows se corre vía WSL/git-bash o `sh scripts/verify_infra.sh` desde el contenedor) siguiendo la sección "Code Examples — Verify INFR-01 + INFR-02" del RESEARCH:

Paso 1 (INFR-02 health): `curl -s http://localhost:8000/health | grep '"database":"connected"'` — si no matchea, exit 1 con mensaje.

Paso 2 (INFR-01 charset/TZ/unicode): `docker exec gri-mysql mysql -ugri_app -p"$MYSQL_APP_PASSWORD" gri` con heredoc SQL que: `SELECT @@character_set_database AS charset, @@collation_database AS collation, @@global.time_zone AS tz_global, NOW() AS now_local;`, `CREATE TABLE IF NOT EXISTS infra_probe (v VARCHAR(50) CHARACTER SET utf8mb4);`, `TRUNCATE infra_probe;`, `INSERT INTO infra_probe VALUES ('Açaí 🍜 café Bogotá');`, `SELECT v FROM infra_probe;`. El script debe imprimir las salidas y grepear `utf8mb4`, `-05:00` y `Açaí 🍜 café Bogotá` (en su defecto, al menos que el INSERT no tire error 1366).

Paso 3 (INFR-01 persistencia): `docker compose restart mysql`, `sleep 15` (esperar healthy), luego `docker exec gri-mysql mysql -ugri_app -p"$MYSQL_APP_PASSWORD" gri -e "SELECT v FROM infra_probe;"` — verificar que la fila con el emoji sigue ahí.

Paso 4 (INFR-02 env-swap, documentado no ejecutado automáticamente): imprimir instrucciones — "Para verificar INFR-02: editar .env cambiando MYSQL_APP_PASSWORD o editar docker-compose.yml cambiando DB_NAME a 'gri_inexistente', correr `docker compose up -d api`, entonces `curl http://localhost:8000/health` debe devolver 503 con database:unreachable. Revertir después."

El script debe usar `set -euo pipefail`, leer `MYSQL_APP_PASSWORD` de `.env` o del entorno, y hacer echo de cada paso con prefijo `[CHECK N/4]`. Termina con `echo "ALL CHECKS PASSED"` si todo verde.

Crear `.planning/phases/01-fundaci-n-e-infraestructura/SKELETON.md` siguiendo el skeleton-template EXACTO:

Encabezado: `# Walking Skeleton — GRI`. `Phase: 1`. `Generated: 2026-08-13`.

**Capability Proven End-to-End:** "Tras `docker compose up`, `GET /health` responde 200 con `{database: connected}`, demostrando que la API FastAPI en un contenedor Docker alcanza MySQL 8.4 en otro contenedor, conectados por variables de entorno, con charset utf8mb4 y timezone America/Bogota."

**Architectural Decisions** (tabla): Framework = FastAPI 0.141 + uvicorn[standard] (async nativo, WebSocket-ready para Phase 7); Data layer = MySQL 8.4 LTS + SQLAlchemy 2.0 async + asyncmy 0.2.14 (wheels cp312, sin toolchain C); Config = pydantic-settings parts-based (DB_HOST/PORT/USER/PASSWORD/NAME) con .env (INFR-02 contract); Deployment target = Docker Compose en Ubuntu Server (dev ahora, prod en Phase 9 añade nginx+TLS); Directory layout = `backend/app/{core,api}/` con `core/` para config+db y `api/` para routers; Dependency mgmt = uv con uv.lock commiteado; Test framework = pytest+pytest-asyncio+httpx (smoke contra stack corriendo).

**Stack Touched in Phase 1** (checklist): scaffold Python ✓, routing real (GET /health + GET /) ✓, database read real (SELECT 1) ✓, deployment local (docker compose up) ✓. Marcar "UI" como N/A (esta fase no tiene UI; la app cliente llega en Phase 5 y el panel en Phase 4).

**Out of Scope (Deferred):** Auth/JWT/roles/multi-tenant → Phase 2; Alembic migrations + modelos de dominio + seed demo → Phase 3; Panel admin UI → Phase 4; App cliente → Phase 5/6; WebSockets → Phase 7; Pagos → Phase 9; nginx/TLS prod → Phase 9; Redis Pub/Sub → v2 (PLT2-02).

**Subsequent Slice Plan:** Phase 2 = auth multi-rol + multi-tenant sobre este skeleton; Phase 3 = modelo de dominio + Alembic (el CMD del Dockerfile se modifica aquí para añadir `alembic upgrade head`); Phase 4 = panel admin; ... Phase 9 = deploy Ubuntu Server.
  </action>

  <verify>
    <automated>sh backend/scripts/verify_infra.sh; if (Test-Path ".planning/phases/01-fundaci-n-e-infraestructura/SKELETON.md") { Write-Output "SKELETON exists" }</automated>
  </verify>

  <acceptance_criteria>
- `backend/scripts/verify_infra.sh` existe, tiene shebang `#!/usr/bin/env bash` y `set -euo pipefail`
- El script tiene 4 pasos etiquetados [CHECK 1/4] a [CHECK 4/4]
- [CHECK 1] curl /health y grep `"database":"connected"`
- [CHECK 2] docker exec mysql con SQL que verifica charset, tz, y round-trip de 'Açaí 🍜 café Bogotá'
- [CHECK 3] docker compose restart mysql + verificar que la fila persiste
- [CHECK 4] documento (echo) del procedimiento env-swap que lleva a /health 503
- Al ejecutar `sh backend/scripts/verify_infra.sh` el resultado termina con "ALL CHECKS PASSED" (o muestra qué check falló con exit != 0)
- `SKELETON.md` existe en `.planning/phases/01-fundaci-n-e-infraestructura/` con las secciones: Capability Proven End-to-End, Architectural Decisions (tabla), Stack Touched in Phase 1, Out of Scope, Subsequent Slice Plan
- SKELETON.md Architectural Decisions nombra FastAPI, MySQL 8.4, SQLAlchemy 2.0 async, asyncmy, pydantic-settings, uv, Docker Compose
- Las 3 Success Criteria del ROADMAP (arranque+health, persistencia+charset/TZ, env-driven) son demostrables con el script
  </acceptance_criteria>

  <done>Script verify_infra.sh ejecuta los 4 chequeos y termina "ALL CHECKS PASSED". SKELETON.md registra las decisiones arquitectónicas como contrato para fases 2-9. Las 3 Success Criteria de INFR-01/INFR-02 están demostradas end-to-end. Walking Skeleton completo.</done>
</task>

</tasks>

<verification>
**Phase gate (antes de /gsd-verify-work):**

1. `docker compose down -v && docker compose up -d --build` — arranca desde cero limpio
2. Esperar ~35s, `docker compose ps` muestra ambos healthy
3. `curl -s http://localhost:8000/health` → `{"status":"ok","database":"connected"}` (200)
4. `cd backend && uv run pytest tests/ -v` → 3 tests verde (test_health, test_db_config × casos)
5. `sh backend/scripts/verify_infra.sh` → ALL CHECKS PASSED
6. Revisar SKELETON.md — decisiones capturadas

**Anti-regresión (cosas que NO deben aparecer):**
- `--default-authentication-plugin` en docker-compose.yml (crashea MySQL 8.4)
- `@app.on_event` en main.py (deprecado)
- `expire_on_commit=True` (default) en async_sessionmaker (causa MissingGreenlet)
- `build-essential`/`default-libmysqlclient-dev` en Dockerfile (innecesario, wheels existen)
- `alembic upgrade head` en CMD del Dockerfile (no hay migraciones, crashea)
- `.venv/` copiado al image (Pitfall 3 — verificar .dockerignore)
- Port 3306 expuesto sin comentario "DEV ONLY" (se quita en Phase 9)
</verification>

<success_criteria>
1. ✅ `docker compose up` levanta MySQL y la API; `/health` responde 200 con `database: connected` (INFR-02)
2. ✅ Datos sobreviven `docker compose restart mysql`; charset=utf8mb4, collation=utf8mb4_unicode_ci, tz=-05:00; 'Açaí 🍜 café Bogotá' round-trips intacto (INFR-01)
3. ✅ Cambiar DB_NAME en compose → `/health` devuelve 503 (INFR-02 env-driven demostrado)
4. ✅ Tests Wave 0 pasan (`uv run pytest tests/ -v`)
5. ✅ SKELETON.md creado con decisiones arquitectónicas
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundaci-n-e-infraestructura/01-01-SUMMARY.md` documenting:
- Files created (con línea de rol de cada uno)
- Cómo levantar el stack (`docker compose up -d`)
- Cómo verificar (`curl /health`, `uv run pytest`, `sh scripts/verify_infra.sh`)
- Confirmación de que las correcciones al STACK.md se aplicaron (no auth-plugin, no build-essential, no alembic en CMD)
- Decisiones registradas en SKELETON.md
- Lo que queda para Phase 2 (auth/JWT sobre este skeleton)
</output>
