---
phase: 08-panel-admin-gesti-n-completa-y-reportes
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - panel_admin/pubspec.yaml
  - panel_admin/lib/core/api_client.dart
  - panel_admin/lib/app.dart
  - panel_admin/lib/features/shared/app_shell.dart
  - panel_admin/lib/features/dashboard/dashboard_screen.dart
  - panel_admin/lib/features/dashboard/widgets/mesa_tile.dart
  - panel_admin/lib/features/mesas/mesas_screen.dart
  - panel_admin/lib/features/mesas/mesa_form_dialog.dart
  - panel_admin/lib/features/mesas/mesa_actions_sheet.dart
  - panel_admin/lib/features/mesas/qr_dialog.dart
  - panel_admin/test/mesas/qr_dialog_test.dart
  - panel_admin/test/mesas/mesas_screen_test.dart
  - panel_admin/test/dashboard/mesa_actions_test.dart
autonomous: true
requirements: [MESA-01, MESA-03, ADMN-04]
must_haves:
  truths:
    - "El admin abre /mesas desde el sidebar y ve el grid de mesas en vivo con FAB 'Nueva mesa'; puede crear y editar mesas (número, capacidad) y el grid se refresca solo (WS kick)"
    - "Al crear/editar con número nuevo, el form advierte que el código QR se regenera (el impreso anterior queda obsoleto)"
    - "El admin puede ver el QR de cada mesa (QrImageView grande + código como texto seleccionable) e imprimirlo (window.print)"
    - "Desde el mapa del dashboard, tocar una mesa ofrece SOLO las transiciones válidas para su estado (mirror de MESA_TRANSITIONS); una carrera perdida (409) muestra SnackBar y la lista se refresca sola"
  artifacts:
    - path: "panel_admin/lib/features/mesas/qr_dialog.dart"
      provides: "Dialog QR imprimible (MESA-03)"
      min_lines: 30
    - path: "panel_admin/lib/features/mesas/mesa_actions_sheet.dart"
      provides: "Bottom sheet con kMesaTransitions + labels de negocio + 409 handling"
      min_lines: 50
    - path: "panel_admin/lib/features/mesas/mesa_form_dialog.dart"
      provides: "Form crear/editar mesa con warning de regeneración de QR"
      min_lines: 40
    - path: "panel_admin/test/dashboard/mesa_actions_test.dart"
      provides: "Suite ADMN-04 (solo transiciones válidas + 409)"
      min_lines: 60
  key_links:
    - from: "panel_admin/lib/core/api_client.dart"
      to: "POST/PATCH /staff/mesas + POST /staff/mesas/{id}/estado"
      via: "createMesa/updateMesa/setMesaEstado con queryRid solo super_admin (patrón getMesas)"
      pattern: "createMesa|updateMesa|setMesaEstado"
    - from: "panel_admin/lib/features/mesas/*"
      to: "mesasProvider (Stream WS kick)"
      via: "watch del stream existente; tras mutación NUNCA se muta estado local — el evento mesa.estado del backend refresca"
      pattern: "mesasProvider"
    - from: "panel_admin/lib/app_shell.dart"
      to: "/mesas"
      via: "_routes[1] = '/mesas' + TopBar con títulos derivados de location"
      pattern: "_routes"
---

<objective>
Panel: gestión completa de mesas — pantalla /mesas (crear/editar), QR dialog imprimible (MESA-03) y mapa del dashboard interactivo (ADMN-04) con solo transiciones válidas.

Purpose: Primera pantalla de gestión del panel (hasta ahora solo lectura); activa el ítem Mesas del sidebar y el botón 'Nueva mesa' del dashboard.
Output: feature features/mesas/ completa, sidebar parcialmente activo, TopBar dinámico, 3 deps nuevas del panel (qr_flutter, data_table_2, web), suite panel 28 → ~40.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-RESEARCH.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-02-SUMMARY.md

@panel_admin/lib/app.dart
@panel_admin/lib/features/shared/app_shell.dart
@panel_admin/lib/core/api_client.dart
@panel_admin/lib/features/dashboard/dashboard_screen.dart
@panel_admin/lib/features/dashboard/widgets/mesa_tile.dart
@panel_admin/lib/features/dashboard/mesas_provider.dart
@panel_admin/lib/models/mesa.dart
@panel_admin/lib/core/format.dart
@panel_admin/test/cocina/cola_test.dart
@panel_admin/pubspec.yaml

<interfaces>
<!-- Contratos existentes del panel. Extraídos del codebase. -->

From panel_admin/lib/core/api_client.dart (patrón de método con queryRid):
```dart
Future<List<Mesa>> getMesas({int? restauranteId}) async {
  final r = await _dio.get<List<dynamic>>('/staff/mesas',
      queryParameters: restauranteId == null ? null : {'restaurante_id': restauranteId});
  ...  // restauranteId SOLO lo pasa super_admin (derivado de user.isSuperAdmin por el caller)
}
```

From panel_admin/lib/models/mesa.dart (freezed existente):
```dart
enum EstadoMesa { disponible, ocupada, reservada, limpieza }
class Mesa { int id; int numero; int capacidad; @JsonKey(name:'codigo_qr') String codigoQr;
             @JsonKey(fromJson: estadoMesaFromJson) EstadoMesa estado; }
```

From panel_admin/lib/features/dashboard/mesas_provider.dart (07-02 COMPLETO en el codebase):
```dart
@riverpod
Stream<List<Mesa>> mesas(Ref ref) async* { ... }
// WS kick-to-refetch en 'mesa.estado' + wsResync + safety net 60s. El backend (08-02)
// emite mesa.estado también en create/update de mesa → las mesas nuevas aparecen solas.
```

From backend (08-02, contrato ya implementado):
```
POST /staff/mesas {numero>0, capacidad>0}          → 201 MesaRead | 409 dup | 400 super_admin sin param
PATCH /staff/mesas/{id} {numero?, capacidad?}      → 200 MesaRead | 404 | 409 | 422 vacío
POST /staff/mesas/{id}/estado {estado}             → 200 MesaRead | 404 | 409 inválida (existe desde F5)
```

Mirror client-side de MESA_TRANSITIONS (backend app/core/state_machines.py — única fuente de verdad es el server):
```dart
const kMesaTransitions = <String, Set<String>>{
  'disponible': {'reservada', 'ocupada'},
  'reservada':  {'ocupada', 'disponible'},
  'ocupada':    {'limpieza'},
  'limpieza':   {'disponible'},
};
// La UI ofrece SOLO las válidas; el 409 del server sigue siendo la autoridad
// (carrera entre dos staff: SnackBar + el evento mesa.estado refresca la lista).
```

Entorno Flutter (gotchas STATE):
- Flutter SDK en C:\src\flutter\bin — cada comando: $env:Path += ";C:\src\flutter\bin"
- build_runner SIN --delete-conflicting-outputs (removido en el runner nuevo)
- Screens bombeadas sin Scaffold/Material ancestor → envolver en Material; ListView lazy → tester.view.physicalSize
- Riverpod 3.4: AsyncValue.value ya es nullable; en widget tests inlinear ProviderScope por test (patrón cola_test.dart)
</interfaces>
</context>

<threat_model>
- **La autoridad es el server**: la UI ofrece solo transiciones válidas del mirror, pero el 409 del backend SIEMPRE manejado (DioException 409 → SnackBar "la mesa cambió de estado" — cubre la carrera entre dos staff). El mirror kMesaTransitions es UX, no seguridad.
- **Sin drift de estado local**: tras create/update/cambiar estado, JAMÁS se muta la lista local — el refresh autoritativo es el evento mesa.estado (kick-to-refetch). Evita dobles renders y divergencia (anti-pattern del research).
- **QR**: el dialog muestra el codigo_qr textual como fallback (SelectableText — el staff puede tipearlo); QrImageView con backgroundColor blanco para contraste de escáner/impresión.
- **Regeneración por cambio de número**: el form advierte ANTES de guardar (texto de warning cuando numero != original) — Pitfall 6 del research.
- **window.print**: web-only (package:web); los tests NO tapan el botón Imprimir (lanzaría en VM). Fallback aceptado: texto "usa Ctrl+P" si hubiera fricción — no bloquear.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Deps + ApiClient (createMesa/updateMesa/setMesaEstado) + ruta /mesas + sidebar + TopBar dinámico</name>
  <files>panel_admin/pubspec.yaml, panel_admin/lib/core/api_client.dart, panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart, panel_admin/lib/features/dashboard/dashboard_screen.dart</files>
  <read_first>panel_admin/pubspec.yaml, panel_admin/lib/core/api_client.dart, panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart, panel_admin/lib/features/dashboard/dashboard_screen.dart</read_first>
  <action>
    1. Con $env:Path += ";C:\src\flutter\bin" en panel_admin: flutter pub add qr_flutter:^4.1.0 data_table_2:^2.8.0 web:^1.1.1 (versiones EXACTAS del research; fl_chart y cached_network_image PROHIBIDOS en v1).
    2. ApiClient — 3 métodos siguiendo el patrón getMesas (queryParameters restaurante_id solo si no-null): createMesa(int numero, int capacidad, {int? restauranteId}) → POST /staff/mesas con data {'numero': numero, 'capacidad': capacidad} → Mesa.fromJson; updateMesa(int mesaId, {int? numero, int? capacidad, int? restauranteId}) → PATCH /staff/mesas/$mesaId con data de solo los campos no-null (excepto restauranteId que es query param) → Mesa; setMesaEstado(int mesaId, String estado, {int? restauranteId}) → POST /staff/mesas/$mesaId/estado data {'estado': estado} → Mesa.
    3. app.dart — importar MesasScreen y agregar GoRoute path '/mesas' dentro del ShellRoute (reemplazar el comentario "Phase 8: /mesas, ..." dejando el resto del comentario para las rutas faltantes).
    4. app_shell.dart — _routes: índice 1 (Mesas) pasa de null a '/mesas'. Refactor _TopBar: eliminar el título 'Dashboard' hardcodeado — agregar un mapa estático path → (título, subtítulo) con los 7 paths finales: '/' → ('Dashboard', 'Resumen de tu restaurante'), '/mesas' → ('Mesas', 'Gestión de mesas y códigos QR'), '/cocina' → ('Pedidos', 'Cola de pedidos en vivo'), '/reservas' → ('Reservas', 'Reservas del día'), '/clientes' → ('Clientes', 'Clientes del restaurante'), '/reportes' → ('Reportes', 'Ventas y platos más vendidos'), '/configuracion' → ('Configuración', 'Menú y administración'); _TopBar recibe location (o el par derivado) desde _AppShellState que ya tiene widget.location; default del mapa = Dashboard.
    5. dashboard_screen.dart — el botón 'Nueva mesa' (hoy onPressed: null, línea ~125) → onPressed: () => context.go('/mesas') (import go_router). El alta vive en la pantalla de gestión.
    6. flutter analyze 0 issues + flutter test sin regresiones.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter analyze; flutter test (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contiene qr_flutter 4.1.0, data_table_2 2.8.0, web 1.1.1 (flutter pub outdated -i qr_flutter los confirma); NO contiene fl_chart ni cached_network_image.
    - flutter analyze 0 issues; suite existente (28) verde sin cambios.
    - Sidebar índice 1 navega a /mesas; TopBar muestra 'Mesas' + subtítulo cuando location == '/mesas'.
  </acceptance_criteria>
  <done>Infraestructura del plan: deps, 3 métodos API, ruta navegable, sidebar Mesas activo, TopBar dinámico.</done>
</task>

<task type="auto">
  <name>Task 2: Feature mesas — screen + form (warning QR) + actions sheet (solo transiciones válidas) + QR dialog</name>
  <files>panel_admin/lib/features/mesas/mesas_screen.dart, panel_admin/lib/features/mesas/mesa_form_dialog.dart, panel_admin/lib/features/mesas/mesa_actions_sheet.dart, panel_admin/lib/features/mesas/qr_dialog.dart, panel_admin/test/mesas/mesas_screen_test.dart, panel_admin/test/mesas/qr_dialog_test.dart</files>
  <read_first>panel_admin/lib/features/dashboard/dashboard_screen.dart (patrón AsyncValue + grid responsive), panel_admin/lib/features/dashboard/widgets/mesa_tile.dart, panel_admin/lib/features/cocina/cocina_screen.dart (patrón SnackBars 409/403 + llamadas a mutación), panel_admin/test/cocina/cola_test.dart (patrón fake ApiClient + espías), panel_admin/lib/core/format.dart</read_first>
  <action>
    1. mesa_form_dialog.dart — MesaFormDialog({Mesa? mesa}): AlertDialog con 2 TextFormField (numero, capacidad; validators int > 0). Modo crear (mesa null) → createMesa; modo editar → updateMesa con solo los campos modificados. Cuando mesa != null y el numero tipeado != mesa.numero → Text naranja visible: '⚠️ Cambiar el número regenera el código QR de la mesa — el QR impreso anterior quedará obsoleto'. Catch DioException: 409 → SnackBar 'Ya existe una mesa con ese número'; otros → SnackBar genérico. Tras éxito: pop + SnackBar de confirmación; el refresh del grid llega por el evento WS (NO invalidar ni mutar lista local).
    2. qr_dialog.dart — showQrDialog(BuildContext context, Mesa mesa): AlertDialog título 'Mesa {numero} — Código QR' con QrImageView(data: mesa.codigoQr, version: QrVersions.auto, size: 280, gapless: false, backgroundColor: Colors.white) + SizedBox(12) + SelectableText(mesa.codigoQr) + actions [TextButton '🖨️ Imprimir' onPressed: web.window.print()] con import 'package:web/web.dart' as web. Sin Scaffold en tests → envolver según necesite.
    3. mesa_actions_sheet.dart — const kMesaTransitions (mapa del research, espejo literal) + const kMesaActionLabels mapa (origen, destino) → label: ('ocupada','limpieza') → 'Marcar en limpieza', ('limpieza','disponible') → 'Liberar', ('disponible','ocupada') → 'Marcar ocupada', ('reservada','ocupada') → 'Marcar ocupada', ('disponible','reservada') → 'Marcar reservada', ('reservada','disponible') → 'Liberar reserva'. showMesaActionsSheet(BuildContext context, WidgetRef ref, Mesa mesa, {bool showEdit = true}): showModalBottomSheet con header 'Mesa {numero} — {estado}', lista de ListTiles SOLO para los destinos válidos del estado actual (orden estable: el del Set o lista fija filtrada), luego Divider, ListTile '📷 Ver código QR' → pop + showQrDialog, y si showEdit: ListTile '✏️ Editar mesa' → pop + MesaFormDialog(mesa: mesa). Acción de estado: setMesaEstado(mesa.id, destino, restauranteId: queryRid) → pop + SnackBar 'Mesa {numero} → {destino}'. Catch DioException 409 → SnackBar 'La mesa cambió de estado (otro usuario la actualizó) — se refrescó la lista' (el WS kick ya refresca). queryRid derivado como en cocina: ref.read(authStateProvider).value?.isSuperAdmin == true ? ridSeleccionado : null.
    4. mesas_screen.dart — MesasScreen ConsumerWidget: watch mesasProvider (el Stream vivo del dashboard — NO crear provider nuevo); AsyncValue.when con loading/error (botón Reintentar → ref.invalidate(mesasProvider))/vacío 'Sin mesas configuradas'; grid responsive LayoutBuilder (>=1100: 4, >=750: 3, si no 2) de MesaTile(mesa: m, onTap: () => showMesaActionsSheet(context, ref, m, showEdit: true)) — el onTap param se agrega en Task 3; mientras tanto invocar directamente el sheet desde un InkWell local si Task 3 no ha tocado mesa_tile aún (orden sugerido: hacer Task 3 antes de correr los tests de este task, o agregar el param onTap aquí mismo — decidir el ejecutor manteniendo mesa_tile.dart como único dueño del widget). Header con título 'Mesas' + FAB.extended 'Nueva mesa' → MesaFormDialog().
    5. Tests: test/mesas/qr_dialog_test.dart — pump MaterialApp con showQrDialog para Mesa fixture codigoQr 'GRI-MESA-R1-005' → expect QrImageView (find.byType) + SelectableText con el código exacto; NO tap Imprimir. test/mesas/mesas_screen_test.dart — ProviderScope con apiClientProvider override por fake (getMesas → 2 fixtures; createMesa/updateMesa espías) + mesasProvider override por Stream.value(fixtures): render 2 tiles; abrir form vía FAB, llenar numero 9 + capacidad 4, guardar → verify createMesa(9, 4) llamado; en modo edición (mesa numero 5) cambiar numero a 6 → expect find.textContaining('regenera') visible.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/mesas/ -j 1; flutter analyze (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - QR dialog renderiza QrImageView con data == mesa.codigoQr y el código como texto seleccionable (test verde).
    - El sheet para cada estado ofrece exactamente los destinos de kMesaTransitions (ninguno más).
    - Form crea/edita vía ApiClient con espías verificados; warning de regeneración visible solo al cambiar numero en edición.
    - flutter analyze 0 issues.
  </acceptance_criteria>
  <done>Feature mesas completa: gestión + QR + acciones de estado reutilizables.</done>
</task>

<task type="auto">
  <name>Task 3: ADMN-04 — mapa del dashboard interactivo (MesaTile.onTap → actions sheet) + tests</name>
  <files>panel_admin/lib/features/dashboard/widgets/mesa_tile.dart, panel_admin/lib/features/dashboard/dashboard_screen.dart, panel_admin/test/dashboard/mesa_actions_test.dart</files>
  <read_first>panel_admin/lib/features/dashboard/widgets/mesa_tile.dart, panel_admin/lib/features/dashboard/dashboard_screen.dart (grid línea ~173), panel_admin/lib/features/mesas/mesa_actions_sheet.dart (Task 2), panel_admin/test/dashboard/mesa_tile_color_test.dart (patrón de test del tile)</read_first>
  <action>
    1. mesa_tile.dart — agregar campo final VoidCallback? onTap (default null mantiene comportamiento actual); envolver el Container existente en InkWell(onTap: onTap, borderRadius: 15) manteniendo el MouseRegion de afuera. MesaTileColorTest existente debe seguir verde sin cambios.
    2. dashboard_screen.dart — en el grid del mapa: MesaTile(mesa: m, onTap: () => showMesaActionsSheet(context, ref, m, showEdit: false)) — en el mapa operacional solo transiciones + Ver QR (la edición vive en /mesas). Import de features/mesas/mesa_actions_sheet.dart.
    3. test/dashboard/mesa_actions_test.dart — casos: (a) sheet con mesa disponible → exactamente 2 acciones de estado ('Marcar reservada', 'Marcar ocupada') + 'Ver código QR' + SIN 'Editar mesa' (showEdit false); (b) mesa limpieza → solo 'Liberar'; (c) mesa ocupada → solo 'Marcar en limpieza'; (d) tap en 'Marcar en limpieza' con mesa ocupada id 7 → fake setMesaEstado(7, 'limpieza') llamado (espía) + sheet cerrado; (e) fake setMesaEstado que lanza DioException 409 → SnackBar con texto 'cambió de estado' visible; (f) tap en un MesaTile del dashboard → abre el sheet (pump del grid con fixtures vía override de mesasProvider por Stream.value). Fakes con el patrón de cola_test.dart (ProviderScope inlineado por test).
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/dashboard/mesa_actions_test.dart test/dashboard/mesa_tile_color_test.dart; flutter analyze (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Para cada uno de los 4 estados, el sheet ofrece SOLO los destinos válidos de kMesaTransitions (tests a-d verdes).
    - 409 → SnackBar y sin crash (test e); el tap del tile abre el sheet (test f).
    - mesa_tile_color_test.dart sin regresiones (el cambio es aditivo).
    - flutter analyze 0 + flutter test completo del panel verde.
  </acceptance_criteria>
  <done>ADMN-04 completo: el admin cambia el estado de una mesa desde el mapa con solo transiciones válidas y manejo de carrera.</done>
</task>

</tasks>

<verification>
- flutter test (suite completa del panel: 28 previos + ~12 nuevos) verde; flutter analyze 0 issues.
- flutter build web --release OK (fase gate parcial — confirma qr_flutter/web compilan en el target web).
- Flujo manual documentado para UAT: login admin@demo.gri.dev → Mesas → crear mesa → QR visible/imprimible → dashboard → tap mesa ocupada → 'Marcar en limpieza' → color cambia en vivo.
</verification>

<success_criteria>
- MESA-01 (UI): crear/editar mesas desde /mesas con QR autogenerado visible en el grid vivo.
- MESA-03: QR dialog con QrImageView + código seleccionable + imprimir (window.print).
- ADMN-04: mapa del dashboard con acciones solo-válidas por estado, 409 → SnackBar, refresh por WS.
</success_criteria>

<output>
After completion, create `.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-03-SUMMARY.md`
</output>
