---
phase: 08-panel-admin-gesti-n-completa-y-reportes
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - panel_admin/lib/models/reporte.dart
  - panel_admin/lib/models/reserva.dart
  - panel_admin/lib/core/api_client.dart
  - panel_admin/lib/features/reportes/reportes_screen.dart
  - panel_admin/lib/features/reservas/reservas_provider.dart
  - panel_admin/lib/features/reservas/reservas_screen.dart
  - panel_admin/lib/features/configuracion/configuracion_screen.dart
  - panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart
  - panel_admin/lib/app.dart
  - panel_admin/lib/features/shared/app_shell.dart
  - panel_admin/test/reportes/reportes_screen_test.dart
  - panel_admin/test/reservas/reservas_screen_test.dart
  - panel_admin/test/configuracion/restaurantes_test.dart
autonomous: true
requirements: [REPO-01, REPO-02, PLAT-05]
must_haves:
  truths:
    - "El admin consulta ventas por rango (o el default últimos 7 días del server) y ve total, número de pedidos, tabla por día y top 10 de platos más vendidos"
    - "El admin ve las reservas del día (selector de fecha) y puede marcar la mesa ocupada al llegar el cliente; un 409 muestra SnackBar"
    - "El super-admin ve la lista de restaurantes (incluye inactivos) en Configuración → Restaurantes y puede desactivar/reactivar con un switch"
    - "Los 7 ítems del sidebar navegan a pantallas reales (ningún 'Próximamente' restante)"
  artifacts:
    - path: "panel_admin/lib/features/reportes/reportes_screen.dart"
      provides: "Pantalla de reportes (rango + cards + DataTable2 por día + top 10)"
      min_lines: 60
    - path: "panel_admin/lib/features/reservas/reservas_screen.dart"
      provides: "Reservas del día + marcar ocupada"
      min_lines: 50
    - path: "panel_admin/lib/features/configuracion/configuracion_screen.dart"
      provides: "Tercer tab Restaurantes (solo super_admin) con switches activo"
    - path: "panel_admin/lib/models/reporte.dart"
      provides: "VentaDia, VentasReporte, TopPlato freezed"
  key_links:
    - from: "panel_admin/lib/core/api_client.dart"
      to: "/staff/reportes/ventas, /staff/reportes/top-platos, /staff/reservas, /admin/restaurantes (PATCH)"
      via: "4 métodos nuevos + listRestaurantes(incluirInactivos); setMesaEstado ya existe de 08-03"
      pattern: "getReporteVentas|getTopPlatos|getReservas|patchRestauranteActivo"
    - from: "panel_admin/lib/features/reservas/reservas_screen.dart"
      to: "setMesaEstado"
      via: "botón 'Marcar ocupada' en reserva confirmada → setMesaEstado(reserva.mesaId, 'ocupada') (nota post-F5: endpoints EXISTENTES)"
      pattern: "setMesaEstado"
    - from: "panel_admin/lib/features/shared/app_shell.dart"
      to: "7 rutas"
      via: "_routes completo sin nulls; _showProximamente eliminado"
      pattern: "_routes"
---

<objective>
Panel: reportes de ventas y top platos (REPO-01/02), pantalla de reservas del día con marcar-ocupada (consume endpoints F5 existentes), tab Restaurantes para el super-admin (PLAT-05) y cierre del sidebar completo (7/7 ítems activos).

Purpose: Último plan de la fase — completa la operación del restaurante desde el panel. Desviación menor documentada del research: reportes usa estado local del screen (queries on-demand sin provider) en vez de reportes_provider.dart — son consultas one-shot por rango, no datos vivos.
Output: 2 models freezed, 4 métodos ApiClient + 1 extendido, 3 pantallas, sidebar completo, suite panel → ~64.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-RESEARCH.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-02-SUMMARY.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-04-SUMMARY.md

@panel_admin/lib/core/api_client.dart
@panel_admin/lib/features/configuracion/configuracion_screen.dart
@panel_admin/lib/features/dashboard/restaurantes_list_provider.dart
@panel_admin/lib/core/token_provider.dart
@panel_admin/lib/core/format.dart
@panel_admin/lib/app.dart
@panel_admin/lib/features/shared/app_shell.dart
@panel_admin/test/cocina/cola_test.dart

<interfaces>
<!-- Contratos backend (08-02 + F5 existentes). Fuente de verdad de las shapes JSON. -->

GET /staff/reportes/ventas?desde&hasta (dates YYYY-MM-DD, ambas opcionales → default últimos 7 días DB-side):
```json
{"desde": "2026-08-08", "hasta": "2026-08-14", "total": 245000.0, "num_pedidos": 7,
 "por_dia": [{"fecha": "2026-08-14", "total": 45000.0, "num_pedidos": 2}]}
```
422 si desde > hasta.

GET /staff/reportes/top-platos?desde&hasta&limit=10 →
```json
[{"producto_id": 5, "nombre": "Patacón", "cantidad": 12, "total": 186000.0}]
```

GET /staff/reservas?fecha=YYYY-MM-DD (endpoint F5 EXISTENTE — no re-crear) → list[ReservaRead]:
```json
{"id": 3, "restaurante_id": 1, "restaurante_nombre": "Demo", "mesa_id": 2, "mesa_numero": 2,
 "fecha": "2026-08-14", "hora_inicio": "19:00:00", "num_personas": 4,
 "estado": "confirmada", "created_at": "..."}
```
Estados reserva: confirmada | pendiente | cancelada (incluye canceladas — el campo discrimina).

POST /staff/mesas/{id}/estado {estado} (F5 EXISTENTE) → 200 MesaRead | 409 inválida.
PATCH /admin/restaurantes/{id} {activo: bool} (08-02) → 200 RestauranteRead (super_admin only, staff 403).
GET /admin/restaurantes?incluir_inactivos=true (08-02) → list[RestauranteRead con campo activo>.

From panel_admin/lib/models/restaurante.dart (freezed EXISTENTE con campo activo — reusar).
From panel_admin/lib/core/api_client.dart: listRestaurantes() existente (08-05 lo extiende con {bool incluirInactivos = false}); setMesaEstado existente de 08-03.
From panel_admin/lib/features/dashboard/mesas_provider.dart: patrón rid/queryRid para providers.
</interfaces>
</context>

<threat_model>
- **Reportes tenant-scoped por construcción**: el backend ya filtra por _resolve_rid (tests 08-02); el panel solo pinta. Validación client-side desde<=hasta ANTES de llamar (ahorra 422), pero el 422 del server también manejado.
- **Rangos**: la fecha default del picker usa DateTime.now() del dispositivo y se envía SIEMPRE explícita tras el primer 'Consultar'; si el usuario no toca nada, NO se envían params (el server aplica su default DB-side — Pitfall 3 de TZ queda del lado del server).
- **PLAT-05 UI**: el switch llama patchRestauranteActivo y refresca con incluir_inactivos=true; desactivar el restaurante ACTUALMENTE seleccionado en el dropdown del topbar NO lo desmonta (el staff sigue operando — alcance v1 del backend); documentar en el SnackBar ('Restaurante desactivado — ya no aparece en la app de clientes').
- **Marcar ocupada**: solo visible en reservas confirmadas; 409 (mesa ya cambió por otra vía) → SnackBar + ref.invalidate del listado del día.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Models + 4 métodos ApiClient + providers + rutas /reportes y /reservas + sidebar 7/7</name>
  <files>panel_admin/lib/models/reporte.dart, panel_admin/lib/models/reserva.dart, panel_admin/lib/core/api_client.dart, panel_admin/lib/features/reservas/reservas_provider.dart, panel_admin/lib/features/configuracion/restaurantes_admin_provider.dart, panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart</files>
  <read_first>panel_admin/lib/core/api_client.dart (estado post-08-04), panel_admin/lib/models/restaurante.dart, panel_admin/lib/features/dashboard/mesas_provider.dart (patrón provider), panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart</read_first>
  <action>
    1. models/reporte.dart — VentaDia {String fecha; double total; @JsonKey(name:'num_pedidos') int numPedidos}; VentasReporte {String desde; String hasta; double total; @JsonKey(name:'num_pedidos') int numPedidos; @JsonKey(name:'por_dia') List<VentaDia> porDia}; TopPlato {@JsonKey(name:'producto_id') int productoId; String nombre; int cantidad; double total}. models/reserva.dart — Reserva {int id; @JsonKey(name:'restaurante_nombre') String restauranteNombre; @JsonKey(name:'mesa_id') int mesaId; @JsonKey(name:'mesa_numero') int mesaNumero; String fecha; @JsonKey(name:'hora_inicio') String horaInicio; @JsonKey(name:'num_personas') int numPersonas; String estado}. dart run build_runner build (SIN --delete-conflicting-outputs).
    2. ApiClient — getReporteVentas({String? desde, String? hasta, int? restauranteId}) → GET /staff/reportes/ventas con query {desde?, hasta?, restaurante_id?} solo no-nulls → VentasReporte; getTopPlatos({String? desde, String? hasta, int? limit, int? restauranteId}) → GET /staff/reportes/top-platos → List<TopPlato>; getReservas({String? fecha, int? restauranteId}) → GET /staff/reservas → List<Reserva>; patchRestauranteActivo(int id, bool activo) → PATCH /admin/restaurantes/$id data {'activo': activo} → Restaurante. EXTENDER listRestaurantes({bool incluirInactivos = false}) agregando query {'incluir_inactivos': 'true'} solo cuando true (firma retrocompatible).
    3. reservas_provider.dart — @riverpod Future<List<Reserva>> reservasDelDia(Ref ref, String fechaYYYYMMDD): patrón rid/queryRid → getReservas(fecha: fecha, restauranteId: queryRid). restaurantes_admin_provider.dart — @riverpod Future<List<Restaurante>> restaurantesAdmin(Ref ref) → client.listRestaurantes(incluirInactivos: true) (watch authState para reactividad al login).
    4. app.dart — GoRoutes '/reportes' → ReportesScreen y '/reservas' → ReservasScreen (ambos dentro del ShellRoute; eliminar el comentario residual de Phase 8).
    5. app_shell.dart — _routes definitivo: ['/', '/mesas', '/cocina', '/reservas', '/clientes', '/reportes', '/configuracion'] (7/7, sin nulls); ELIMINAR _showProximamente, el parámetro onProximamente y todo el flujo 'Próximamente' (ya no hay destinos null).
    6. flutter analyze 0 + flutter test sin regresiones.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; dart run build_runner build; flutter analyze; flutter test (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar sin referencias a 'Próximamente' (grep negativo en app_shell.dart); los 7 ítems navegan.
    - 4 métodos nuevos + listRestaurantes extendido; codegen limpio; analyze 0.
    - Suite existente verde (los tests que stubbeaban 'Próximamente' — si existían — actualizados).
  </acceptance_criteria>
  <done>Infraestructura final del panel: models, API, providers, rutas y sidebar completo.</done>
</task>

<task type="auto">
  <name>Task 2: Reportes screen (rango + cards + por día + top 10) + tests</name>
  <files>panel_admin/lib/features/reportes/reportes_screen.dart, panel_admin/test/reportes/reportes_screen_test.dart</files>
  <read_first>panel_admin/lib/models/reporte.dart (Task 1), panel_admin/lib/core/format.dart, panel_admin/lib/features/dashboard/widgets/stat_card.dart (patrón visual de cards), panel_admin/test/cocina/cola_test.dart</read_first>
  <action>
    1. reportes_screen.dart — ConsumerStatefulWidget con estado local: DateTime? _desde, _hasta; VentasReporte? _ventas; List<TopPlato>? _top; bool _cargando. Layout: fila de controles — OutlinedButton.icon '📅 Desde' / '📅 Hasta' (showDatePicker; label muestra dd/MM/yyyy o 'Inicio'/'Fin' como hint del default del server 'Últimos 7 días') + ElevatedButton 'Consultar' + TextButton 'Limpiar'. Consultar: validación client-side _desde != null && _hasta != null && _desde.isAfter(_hasta) → SnackBar 'La fecha inicial no puede ser mayor que la final' SIN llamar API; si no → _cargando = true, llama getReporteVentas y getTopPlatos (con queryRid derivado como en cocina: user.isSuperAdmin ? ridSeleccionado : null) en paralelo (Future.wait), setState con resultados; DioException 422 → SnackBar del detalle del server. Resultados: 2 StatCard-style cards ('Total vendido' formatCOP(ventas.total), 'Pedidos' ventas.numPedidos) + DataTable2 minWidth 500 (Fecha | Pedidos | Total) con las filas de porDia + sección 'Platos más vendidos': ListView/shrinkWrap numerada 1..N (nombre, '×{cantidad}', formatCOP(total)) — vacío → 'Sin ventas en el rango'. Bounds finitos para el DataTable2 (regla data_table_2).
    2. test/reportes/reportes_screen_test.dart — ProviderScope con apiClientProvider → fake (getReporteVentas/getTopPlatos espías con fixtures: 2 días, total 245000.0, 3 top platos con cantidades 12/7/3): (a) tap 'Consultar' sin tocar pickers → espías llamados SIN desde/hasta (params null — default server); (b) render '$ 245.000' + '7' pedidos + filas de los 2 días con formatCOP; (c) top platos ordenados 1..3 con '×12' en el primero; (d) fijar _desde > _hasta vía pickers (o exponer el validador como función testeable) → SnackBar visible Y espías NO llamados; (e) fixture vacío → 'Sin ventas en el rango'.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/reportes/reportes_screen_test.dart; flutter analyze (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Default sin rango consulta sin params (espía recibe nulls); rangos inclusivos pintados por día.
    - Totales con formatCOP; top platos numerado y ordenado por cantidad del fixture.
    - Validación desde>hasta client-side sin llamada API; 422 del server también manejado.
    - analyze 0; tests (a)-(e) verdes.
  </acceptance_criteria>
  <done>REPO-01/02 en el panel: ventas por rango/día y platos más vendidos consultables.</done>
</task>

<task type="auto">
  <name>Task 3: Reservas del día (marcar ocupada) + tab Restaurantes super-admin (PLAT-05) + tests</name>
  <files>panel_admin/lib/features/reservas/reservas_screen.dart, panel_admin/lib/features/configuracion/configuracion_screen.dart, panel_admin/test/reservas/reservas_screen_test.dart, panel_admin/test/configuracion/restaurantes_test.dart</files>
  <read_first>panel_admin/lib/features/reservas/reservas_provider.dart (Task 1), panel_admin/lib/features/configuracion/configuracion_screen.dart (estado post-08-04: 2 tabs + comentario del tercero), panel_admin/lib/core/token_provider.dart (authStateProvider/isSuperAdmin), panel_admin/lib/models/reserva.dart, panel_admin/test/cocina/cola_test.dart</read_first>
  <action>
    1. reservas_screen.dart — ConsumerStatefulWidget: DateTime _fecha = DateTime.now() (estado local); header con OutlinedButton '📅 {dd/MM/yyyy}' (showDatePicker; al cambiar → ref.invalidate(reservasDelDiaProvider(fechaKey))) + botón 'Hoy'. watch reservasDelDiaProvider(_fecha.toIso8601String().substring(0, 10)) con AsyncValue.when; lista de cards por reserva: hora (horaInicio substring 0,5), 'Mesa {mesaNumero}', '{numPersonas} personas', chip de estado (confirmada verde / cancelada rojo tachado / pendiente ámbar), y si estado == 'confirmada' → ElevatedButton 'Marcar ocupada' → ref.read(apiClientProvider).setMesaEstado(reserva.mesaId, 'ocupada', restauranteId: queryRid) → SnackBar 'Mesa {n} marcada ocupada' + ref.invalidate(reservasDelDiaProvider(fechaKey)); catch DioException 409 → SnackBar 'La mesa ya cambió de estado' + invalidate (refresh). Vacío → 'Sin reservas para este día'.
    2. configuracion_screen.dart — extender a 3 tabs: mantener 'Menú' y 'Restaurante'; agregar tab 'Restaurantes' visible SOLO si ref.watch(authStateProvider).value?.isSuperAdmin == true (DefaultTabController length dinámico 2 o 3 — calcular antes del build; ajustar el comentario reservado de 08-04). Contenido del tab: watch restaurantesAdminProvider AsyncValue.when → Column con Expanded(ListView de Cards: nombre + estado ('Activo'/'Inactivo') + Switch(value: r.activo, onChanged: (v) → patchRestauranteActivo(r.id, v) → SnackBar (desactivar: 'Restaurante desactivado — desaparece de la app de clientes'; reactivar: 'Restaurante reactivado') → ref.invalidate(restaurantesAdminProvider); catch DioException → SnackBar del error).
    3. test/reservas/reservas_screen_test.dart — overrides (apiClientProvider fake con getReservas fixture: 1 confirmada mesa 2 + 1 cancelada; setMesaEstado espía; reservasDelDiaProvider family override): (a) render de las 2 reservas con chip 'confirmada'/'cancelada'; (b) botón 'Marcar ocupada' SOLO en la confirmada; (c) tap → espía setMesaEstado(mesaId, 'ocupada') llamado; (d) fake setMesaEstado lanza DioException 409 → SnackBar visible. test/configuracion/restaurantes_test.dart — (a) con user super_admin fixture (authStateProvider override): tab 'Restaurantes' presente, 2 restaurantes renderizados (1 activo, 1 inactivo) y switch OFF en el inactivo; (b) toggle → espía patchRestauranteActivo(id, true) llamado; (c) con user staff (admin_restaurante): tab 'Restaurantes' AUSENTE (find.text('Restaurantes') findsNothing) y tabs Menú/Restaurante presentes.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/reservas/reservas_screen_test.dart test/configuracion/restaurantes_test.dart; flutter test; flutter analyze; flutter build web --release (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Reservas: selector de fecha + marcar ocupada solo en confirmadas + 409 SnackBar (tests a-d).
    - Configuración: tab Restaurantes solo para super_admin, switches con espía PLAT-05, staff no lo ve (tests a-c).
    - Suite completa del panel verde (~64); analyze 0; build web --release OK (phase gate).
  </acceptance_criteria>
  <done>REPO/RESV/PLAT-05 UI completos y sidebar 7/7 — la fase cierra con el panel operativo de punta a punta.</done>
</task>

</tasks>

<verification>
- flutter test suite completa verde + flutter analyze 0 + flutter build web --release OK (gate de fase antes de /gsd-verify-work).
- uv run pytest -q backend sigue verde (este plan no toca backend — sanity).
- UAT manual documentado en SUMMARY: reportes con datos reales (crear pedidos demo y servirlos), reservas del día con marcar ocupada, super-admin desactivando un restaurante y verificando desaparición de /public.
</verification>

<success_criteria>
- REPO-01 (UI): reporte de ventas por día/rango con total y número de pedidos.
- REPO-02 (UI): top 10 platos más vendidos por cantidad.
- PLAT-05 (UI): lista de restaurantes con inactivos + switch desactivar/reactivar.
- Sidebar 7/7 ítems operativos — ningún 'Próximamente' en el panel.
</success_criteria>

<output>
After completion, create `.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-05-SUMMARY.md`
</output>
