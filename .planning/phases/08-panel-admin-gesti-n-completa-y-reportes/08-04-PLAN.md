---
phase: 08-panel-admin-gesti-n-completa-y-reportes
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - panel_admin/lib/models/categoria_staff.dart
  - panel_admin/lib/models/producto_staff.dart
  - panel_admin/lib/models/cliente_resumen.dart
  - panel_admin/lib/core/api_client.dart
  - panel_admin/lib/features/menu/menu_provider.dart
  - panel_admin/lib/features/menu/menu_screen.dart
  - panel_admin/lib/features/menu/categoria_form_dialog.dart
  - panel_admin/lib/features/menu/producto_form_dialog.dart
  - panel_admin/lib/features/clientes/clientes_provider.dart
  - panel_admin/lib/features/clientes/clientes_screen.dart
  - panel_admin/lib/features/clientes/historial_dialog.dart
  - panel_admin/lib/features/configuracion/configuracion_screen.dart
  - panel_admin/lib/app.dart
  - panel_admin/lib/features/shared/app_shell.dart
  - panel_admin/test/menu/menu_screen_test.dart
  - panel_admin/test/clientes/clientes_screen_test.dart
autonomous: true
requirements: [MENU-01, MENU-02, ADMN-03]
must_haves:
  truths:
    - "El admin gestiona el menú desde Configuración → tab Menú: crea/edita categorías y productos (precio, descripción, URL de imagen, toggle Agotado, toggle Activo) y ve el resultado al instante (invalidate)"
    - "El admin ve la tabla de clientes del restaurante (nombre, pedidos, total gastado, último pedido) y al tocar una fila ve su historial de pedidos"
    - "El ítem Clientes del sidebar navega a la tabla; el ítem Configuración navega a los tabs Menú/Restaurante"
  artifacts:
    - path: "panel_admin/lib/models/categoria_staff.dart"
      provides: "CategoriaStaff freezed (con productos anidados)"
    - path: "panel_admin/lib/models/cliente_resumen.dart"
      provides: "ClienteResumen freezed"
    - path: "panel_admin/lib/features/menu/menu_screen.dart"
      provides: "Gestión de menú (categorías ExpansionTile + productos)"
      min_lines: 60
    - path: "panel_admin/lib/features/clientes/clientes_screen.dart"
      provides: "DataTable2 de clientes + historial"
      min_lines: 50
    - path: "panel_admin/test/menu/menu_screen_test.dart"
      provides: "Suite MENU-01/02 UI"
      min_lines: 50
  key_links:
    - from: "panel_admin/lib/core/api_client.dart"
      to: "/staff/menu, /staff/categorias, /staff/productos, /staff/clientes, /staff/clientes/{id}/historial"
      via: "7 métodos nuevos con queryRid solo super_admin"
      pattern: "getStaffMenu|createCategoria|updateCategoria|createProducto|updateProducto|getClientes|getClienteHistorial"
    - from: "panel_admin/lib/features/menu/*"
      to: "staffMenuProvider"
      via: "FutureProvider; tras cada mutación exitosa ref.invalidate(staffMenuProvider) — menú NO tiene WS (refresh on-demand, decisión research)"
      pattern: "invalidate\\(staffMenuProvider\\)"
    - from: "panel_admin/lib/features/clientes/clientes_screen.dart"
      to: "DataTable2"
      via: "rows desde clientesProvider; DataRow2 onTap → historial_dialog; NUNCA dentro de SingleChildScrollView sin bounds finitos (README data_table_2)"
      pattern: "DataTable2"
---

<objective>
Panel: gestión del menú (categorías y productos con toggles Agotado/Activo) dentro de Configuración, y gestión de clientes (tabla DataTable2 + historial).

Purpose: Cierra MENU-01/02 y ADMN-03 en el panel. Decisiones discretales documentadas: el menú vive como tab de /configuracion (el sidebar de 7 ítems no tiene entrada Menú — research reserva Configuración para administración); la pestaña Restaurante es read-only v1.
Output: 3 models freezed, 7 métodos ApiClient, features menu/ + clientes/ + configuracion/ base, rutas /clientes y /configuracion activas, suite panel → ~52.
</objective>

<execution_context>
@C:/Users/valle/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/valle/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-RESEARCH.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-01-SUMMARY.md
@.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-03-SUMMARY.md

@panel_admin/lib/core/api_client.dart
@panel_admin/lib/models/mesa.dart
@panel_admin/lib/models/pedido_staff.dart
@panel_admin/lib/features/dashboard/mesas_provider.dart
@panel_admin/lib/features/dashboard/restaurante_provider.dart
@panel_admin/lib/core/format.dart
@panel_admin/lib/app.dart
@panel_admin/lib/features/shared/app_shell.dart
@panel_admin/test/cocina/cola_test.dart

<interfaces>
<!-- Contratos backend implementados en 08-01 (fuente de verdad de las shapes JSON). -->

GET /staff/menu → list[CategoriaStaff]:
```json
{"id": 1, "nombre": "Entradas", "orden": 0, "activo": true,
 "productos": [{"id": 5, "categoria_id": 1, "nombre": "Patacón", "descripcion": null,
                "precio": 15500.0, "imagen_url": null, "disponible": true, "activo": true}]}
```
POST /staff/categorias {nombre, orden?} → 201 CategoriaStaff (sin productos) | 409 dup
PATCH /staff/categorias/{id} {nombre?|orden?|activo?} → CategoriaStaff | 404
POST /staff/productos {categoria_id, nombre, descripcion?, precio>0, imagen_url?} → 201 ProductoStaff | 404 cat | 422
PATCH /staff/productos/{id} {nombre?|descripcion?|precio?|imagen_url?|disponible?|activo?} → ProductoStaff | 404
GET /staff/clientes → list[ClienteResumen]:
```json
{"usuario_id": 9, "nombre": "Ana", "email": "a@x.com", "num_pedidos": 3,
 "total_gastado": 84500.0, "ultimo_pedido_at": "2026-08-14T20:15:03"}
```
GET /staff/clientes/{usuario_id}/historial → list[PedidoStaffRead] (misma shape que /staff/pedidos: id, mesa_numero, estado, total float, items[], usuario_nombre, created_at) | 404 sin pedidos en tenant

From panel_admin/lib/models/pedido_staff.dart (freezed EXISTENTE — reusar tal cual en historial).
From panel_admin/lib/features/dashboard/restaurante_provider.dart: restauranteProvider (Future<Restaurante>) + currentRestauranteIdProvider (NotifierProvider<int?>).
From panel_admin/lib/features/dashboard/mesas_provider.dart líneas 33-44: patrón rid = selectedRid ?? user?.restaurantId; queryRid = user?.isSuperAdmin == true ? rid : null.
</interfaces>
</context>

<threat_model>
- **Cliente Dart vs Decimal**: precio/total_gastado viajan como JSON numbers (el backend coercea con field_serializer — implementado 08-01); los models Dart usan double. Si un test revienta con FormatException al parsear, el culpable es el backend, no el panel.
- **Toggles con semántica visible**: 'Agotado' (!disponible) y 'Inactivo' (!activo) son switches/badges DISTINTOS y etiquetados — nunca un solo toggle ambiguo.
- **Sin upload de imágenes v1**: imagen_url es un campo de texto opcional con preview Image.network + errorBuilder (icono placeholder si la URL falla). PROHIBIDO agregar upload/multipart.
- **Mutaciones + refresh**: menú/clientes NO tienen eventos WS — tras cada mutación exitosa ref.invalidate del provider correspondiente (decisión research Pattern 3 vía 2). Errores 409 (nombre dup) → SnackBar accionable.
- **DataTable2 bounds finitos**: nunca dentro de SingleChildScrollView/Column sin Expanded (README data_table_2) — envolver con SizedBox/Expanded.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Models freezed + 7 métodos ApiClient + providers de menú y clientes</name>
  <files>panel_admin/lib/models/categoria_staff.dart, panel_admin/lib/models/producto_staff.dart, panel_admin/lib/models/cliente_resumen.dart, panel_admin/lib/core/api_client.dart, panel_admin/lib/features/menu/menu_provider.dart, panel_admin/lib/features/clientes/clientes_provider.dart</files>
  <read_first>panel_admin/lib/models/mesa.dart (patrón freezed + JsonKey), panel_admin/lib/models/pedido_staff.dart, panel_admin/lib/core/api_client.dart, panel_admin/lib/features/dashboard/mesas_provider.dart (patrón provider con rid/queryRid)</read_first>
  <action>
    1. models/categoria_staff.dart + producto_staff.dart + cliente_resumen.dart (freezed + json_serializable, espejo EXACTO de las shapes del bloque interfaces): ProductoStaff {int id; @JsonKey(name:'categoria_id') int categoriaId; String nombre; String? descripcion; double precio; @JsonKey(name:'imagen_url') String? imagenUrl; bool disponible; bool activo}; CategoriaStaff {int id; String nombre; int orden; bool activo; List<ProductoStaff> productos}; ClienteResumen {@JsonKey(name:'usuario_id') int usuarioId; String nombre; String email; @JsonKey(name:'num_pedidos') int numPedidos; @JsonKey(name:'total_gastado') double totalGastado; @JsonKey(name:'ultimo_pedido_at') DateTime? ultimoPedidoAt}.
    2. dart run build_runner build (SIN --delete-conflicting-outputs — gotcha STATE) con $env:Path += ";C:\src\flutter\bin".
    3. ApiClient — 7 métodos con patrón queryRid de getMesas: getStaffMenu({int? restauranteId}) → GET /staff/menu → List<CategoriaStaff>; createCategoria(String nombre, {int? orden, int? restauranteId}) → POST /staff/categorias data {nombre, orden?} → CategoriaStaff; updateCategoria(int id, {String? nombre, int? orden, bool? activo, int? restauranteId}) → PATCH → CategoriaStaff; createProducto({required int categoriaId, required String nombre, String? descripcion, required double precio, String? imagenUrl, int? restauranteId}) → POST /staff/productos → ProductoStaff; updateProducto(int id, {String? nombre, String? descripcion, double? precio, String? imagenUrl, bool? disponible, bool? activo, int? restauranteId}) → PATCH → ProductoStaff; getClientes({int? restauranteId}) → GET /staff/clientes → List<ClienteResumen>; getClienteHistorial(int usuarioId, {int? restauranteId}) → GET /staff/clientes/$usuarioId/historial → List<PedidoStaff>.
    4. menu_provider.dart — @riverpod Future<List<CategoriaStaff>> staffMenu(Ref ref): watch authStateProvider + currentRestauranteIdProvider; rid null → []; queryRid solo si isSuperAdmin; return await client.getStaffMenu(restauranteId: queryRid). clientes_provider.dart — análogo clientes(Ref ref) → Future<List<ClienteResumen>>.
    5. flutter analyze 0 issues; codegen sin conflictos.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; dart run build_runner build; flutter analyze; flutter test (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Los 3 models generan .freezed.dart/.g.dart sin errores (build_runner exitoso, sin --delete-conflicting-outputs).
    - ApiClient expone los 7 métodos; analyze 0; suite existente verde.
    - Los providers devuelven [] cuando rid == null (no explotan para super_admin sin restaurante seleccionado).
  </acceptance_criteria>
  <done>Capa de datos del menú y clientes lista para consumir desde las screens.</done>
</task>

<task type="auto">
  <name>Task 2: Menú UI — ruta /configuracion (tabs Menú + Restaurante) + MenuScreen + forms + tests</name>
  <files>panel_admin/lib/features/configuracion/configuracion_screen.dart, panel_admin/lib/features/menu/menu_screen.dart, panel_admin/lib/features/menu/categoria_form_dialog.dart, panel_admin/lib/features/menu/producto_form_dialog.dart, panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart, panel_admin/test/menu/menu_screen_test.dart</files>
  <read_first>panel_admin/lib/features/dashboard/restaurante_provider.dart, panel_admin/lib/features/cocina/cocina_screen.dart (patrón SnackBars de error), panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart (estado post-08-03), panel_admin/lib/core/format.dart</read_first>
  <action>
    1. app.dart — GoRoute '/configuracion' → ConfiguracionScreen (dentro del ShellRoute). app_shell.dart — _routes índice 6 (Configuración) = '/configuracion'.
    2. configuracion_screen.dart — ConfiguracionScreen: DefaultTabController con 2 tabs: tab 'Menú' → contenido de MenuScreen (widget embebido, sin Scaffold propio); tab 'Restaurante' → info read-only del restauranteProvider (nombre, tipo_cocina, dirección, descripción en Cards — SIN edición v1; AsyncValue.when). El tab 'Restaurantes' (super_admin, PLAT-05) se agrega en 08-05 — dejar el DefaultTabController con length 2 y un comentario reservando el tercero.
    3. menu_screen.dart — ConsumerWidget: watch staffMenuProvider con AsyncValue.when (loading/error Reintentar/vacío 'Sin categorías'). Lista de categorías: ExpansionTile (título: nombre + chip naranja 'Inactiva' si !activo + IconButton editar → CategoriaFormDialog; trailing 'Nuevo producto' visible SOLO expandido → ProductoFormDialog(categoriaId: c.id)). Hijos: filas de producto con nombre, formatCOP(precio), badges: 'Agotado' (ámbar) si !disponible, 'Inactivo' (gris) si !activo; tap → ProductoFormDialog(producto: p, categoriaId: p.categoriaId). Botón header 'Nueva categoría' → CategoriaFormDialog. NOTA: los productos de una categoría inactiva siguen listados en staff (gestión) — no filtrar client-side.
    4. categoria_form_dialog.dart — AlertDialog: TextFormField nombre (required 1-100) + TextFormField orden (int, default 0). Crear → createCategoria + SnackBar; editar → updateCategoria (solo campos modificados); DioException 409 → SnackBar 'Ya existe una categoría con ese nombre'. Tras éxito: pop + ref.invalidate(staffMenuProvider).
    5. producto_form_dialog.dart — AlertDialog: nombre (1-150), descripcion (multiline, opcional), precio (double > 0 con validador y teclado numérico), imagen_url (opcional, label 'URL de imagen (opcional)' + preview: si el campo tiene texto → SizedBox 120x80 con Image.network(url, errorBuilder: (_) → Icon(Icons.broken_image)) — SIN upload), SwitchListTile 'Agotado' (value: !(producto?.disponible ?? true), onChanged gestiona la negación), SwitchListTile 'Activo' (value: producto?.activo ?? true). Crear → createProducto(categoriaId, ...); editar → updateProducto(id, {...} solo modificados); 422/409 → SnackBar. Tras éxito: pop + ref.invalidate(staffMenuProvider).
    6. test/menu/menu_screen_test.dart — con overrides (apiClientProvider fake + staffMenuProvider override con fixtures): (a) render 2 categorías, la expandida muestra 2 productos con formatCOP del precio y badge 'Agotado' en el que corresponda; (b) 'Nueva categoría' → dialog → llenar 'Bebidas' → guardar → espía createCategoria('Bebidas') llamado + invalidate observable; (c) toggle 'Agotado' ON en un producto → espía updateProducto(id, disponible: false); (d) badge 'Inactiva' visible para categoria fixture activo=false.
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/menu/menu_screen_test.dart; flutter analyze (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar Configuración navega y el TopBar muestra su título (mapa del 08-03 ya lo cubre).
    - CRUD completo de categorías y productos operativo contra el fake (espías) con refresh por invalidate.
    - Badges Agotado/Inactivo distinguibles; preview de imagen con errorBuilder sin crash en URL inválida.
    - analyze 0; tests (a)-(d) verdes.
  </acceptance_criteria>
  <done>MENU-01/02 en el panel: el admin gestiona todo el menú desde Configuración → Menú.</done>
</task>

<task type="auto">
  <name>Task 3: Clientes UI — DataTable2 + historial dialog + ruta /clientes + tests</name>
  <files>panel_admin/lib/features/clientes/clientes_screen.dart, panel_admin/lib/features/clientes/historial_dialog.dart, panel_admin/lib/features/clientes/clientes_provider.dart, panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart, panel_admin/test/clientes/clientes_screen_test.dart</files>
  <read_first>panel_admin/lib/features/clientes/clientes_provider.dart (Task 1), panel_admin/lib/models/pedido_staff.dart, panel_admin/lib/core/format.dart, panel_admin/lib/app.dart, panel_admin/lib/features/shared/app_shell.dart</read_first>
  <action>
    1. app.dart — GoRoute '/clientes' → ClientesScreen; app_shell.dart — _routes índice 4 (Clientes) = '/clientes'.
    2. clientes_provider.dart — agregar family: @riverpod Future<List<PedidoStaff>> clienteHistorial(Ref ref, int usuarioId) con el mismo patrón rid/queryRid → getClienteHistorial(usuarioId, restauranteId: queryRid).
    3. clientes_screen.dart — ConsumerWidget: watch clientesProvider AsyncValue.when; DataTable2(columnSpacing: 12, minWidth: 600, columns: DataColumn2(label: 'Cliente', size: ColumnSize.L), DataColumn('Pedidos', numeric: true), DataColumn('Total gastado', numeric: true), DataColumn('Último pedido')); rows: DataRow2(onSelectChanged: (_) => showHistorialDialog(context, ref, cliente), cells: DataCell(Text('${c.nombre}\n${c.email}')), DataCell(Text(c.numPedidos.toString())), DataCell(Text(formatCOP(c.totalGastado))), DataCell(Text(DateFormat('dd/MM/yyyy').format(c.ultimoPedidoAt)))) con intl import. ENVOLVER el DataTable2 con bounds finitos (Expanded dentro de Column o SizedBox con altura — README data_table_2: nunca en SingleChildScrollView sin límite). Empty state 'Aún no hay clientes con pedidos'.
    4. historial_dialog.dart — showHistorialDialog: showDialog → SizedBox(width: 520, child: AlertDialog 'Historial del cliente') con watch de clienteHistorialProvider(usuarioId): loading spinner / error o vacío → Text('Sin pedidos en este restaurante') / data → SizedBox(height: 400) con ListView de pedidos: 'Mesa {mesaNumero}' + chip de estado + formatCOP(total) + fecha corta + items resumidos ('2× Patacón' por línea, opcional colapsable — mínimo: cantidad×nombre por item en texto secundario).
    5. test/clientes/clientes_screen_test.dart — overrides con fixtures (2 clientes: numPedidos 3 y 1, totalGastado 84500.0/12000.0): (a) tabla renderiza nombres, '$ 84.500' via formatCOP, pedidos; (b) tap en fila → dialog abierto con pedidos del fixture (family provider override) mostrando 'Mesa 2' y total; (c) empty state con lista vacía. OJO ListView lazy en tests: tester.view.physicalSize = Size(800, 1800) + devicePixelRatio 1.0 + addTearDown(tester.view.reset) si hace falta (gotcha STATE).
  </action>
  <verify>
    <automated>$env:Path += ";C:\src\flutter\bin"; flutter test test/clientes/clientes_screen_test.dart; flutter test; flutter analyze (workdir panel_admin)</automated>
  </verify>
  <acceptance_criteria>
    - Tabla con sticky headers y scroll horizontal (minWidth 600) renderizando los fixtures; tap fila → historial con pedidos.
    - Ruta /clientes activa en sidebar (índice 4).
    - Suite completa del panel verde; analyze 0.
  </acceptance_criteria>
  <done>ADMN-03 en el panel: lista e historial de clientes consumiendo los endpoints de 08-01.</done>
</task>

</tasks>

<verification>
- flutter test suite completa verde (~52) + flutter analyze 0 issues.
- flutter build web --release OK.
- Flujo manual para UAT documentado: Configuración → Menú → crear categoría/producto → toggles → Clientes → tap fila → historial.
</verification>

<success_criteria>
- MENU-01 (UI): crear/editar/desactivar categorías con 409 amigable y refresh inmediato.
- MENU-02 (UI): productos con precio validado, descripción, imagen_url con preview, toggles Agotado/Activo diferenciados.
- ADMN-03 (UI): tabla de clientes + historial de pedidos del tenant.
</success_criteria>

<output>
After completion, create `.planning/phases/08-panel-admin-gesti-n-completa-y-reportes/08-04-SUMMARY.md`
</output>
