import 'package:flutter/material.dart';

/// Reportes de ventas (REPO-01/02) — esqueleto Task 1; la implementación
/// completa (rango + cards + DataTable2 por día + top 10) llega en el Task 2
/// de 08-05.
class ReportesScreen extends StatelessWidget {
  const ReportesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Reportes')),
    );
  }
}
