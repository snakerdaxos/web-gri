# Copia este archivo a assets/.env y ajusta los valores.
# API_BASE_URL: origen del backend FastAPI (docker-compose expone :8000).
# PANEL_POLL_SECONDS: intervalo de polling del dashboard (deuda Phase 7 WS).
API_BASE_URL=http://localhost:8000
PANEL_POLL_SECONDS=10
